# `fight.c` ROM Parity Audit

- **Status**: ⚠️ Audited 95% — combat-control regressions closed; 2026-05-23 PERS sweep opened FIGHT-004..008 on the position-change broadcast surface (`_position_change_message`); 2026-05-28 ARITH-004 reclass surfaced the WEAPON_POISON affect divergence (FIGHT-016 ✅ + FIGHT-017 ✅, both fixed); 2026-05-28 INV-025 act()-dispatch sweep surfaced the combat dam_message TRIG_ACT gap (FIGHT-018 ✅ fixed); 2026-05-28 the differential harness (FINDING-008) surfaced the whole-combat hit-model divergence — Python shipped a non-ROM percent model by default (FIGHT-019 ✅ fixed, percent path + `COMBAT_USE_THAC0` flag retired; also fixed the masked NPC-attacker THAC0 branch); 2026-05-28 re-triaging FINDING-008 sub-issue 3 surfaced a real `do_kill` SINGLE-DELIVERY violation (FIGHT-020 ✅ fixed — `do_kill` double-delivered every `kill`-initiated combat line to connected PCs; the FIGHT-019 session had mis-classified this as a harness "capture artifact"); 2026-05-28 the differential harness (FINDING-009) surfaced the combat-tick round divergence at `__tick` (violence_update) — facet 1 is a `multi_hit`/`mob_hit` RNG draw-count desync (FIGHT-021 ✅ fixed — guarded 2nd/3rd-attack `number_percent()` draws made unconditional; FIGHT-022 ✅ fixed — faithful NPC `mob_hit` port adds ROM's `number_range(0,2)`+`number_range(0,8)` draws + off-skill dispatch). Together FIGHT-021+022 close FINDING-009 facet 1; FIGHT-024 ✅ fixed closes facet 3 (combat-tick iteration order — `violence_tick` walks `character_registry` reversed to match ROM's head-first `char_list` walk); FIGHT-023 ✅ fixed closes facet 2 (mob `dam_type` now the ROM attack_table index, not a DamageType class — the drunk renders "beating" not "slice"); FIGHT-025 ✅ fixed closes facet 4 (combat act() output capitalized like ROM `act_new` — "The drunk's beating" not "the drunk's beating"), **closing FINDING-009**; FIGHT-026 ✅ fixed a latent FIGHT-022 crash that facet 3 exposed (NPC `do_dirt`/`do_trip`/`do_disarm` accessed `char.skills` / `perm_stat[DEX]` and crashed when `mob_hit` dispatched them on a flagged mob).
- **Date**: 2026-05-15 (FIGHT-001..003); 2026-05-23 (FIGHT-004..008 opened); 2026-05-28 (FIGHT-016/017 opened); 2026-05-28 (FIGHT-018 opened+closed); 2026-05-28 (FIGHT-019 opened+closed); 2026-05-28 (FIGHT-020 opened+closed); 2026-05-28 (FIGHT-021 opened+closed, FIGHT-022 opened+closed); 2026-05-29 (FIGHT-024 opened+closed, FIGHT-026 opened+closed, FIGHT-023 opened+closed, FIGHT-025 opened+closed)
- **Source**: `src/fight.c` (ROM 2.4b6)
- **Python primaries**:
  - `mud/combat/engine.py`
  - `mud/combat/safety.py`
  - `mud/commands/combat.py`
  - `mud/game_loop.py`

This audit doc was opened to track the combat-path regressions surfaced during the 2026-05-02 death/combat triage. The original two regressions are closed, and a May 15, 2026 ROM-source-first combat→XP sweep closed one more real death-branch gap. `fight.c` remains at 95% in the top-level tracker only because the file still has historical broader-sweep debt, not because a current named implementation gap is open.

## Phase 1 — Function inventory

| ROM function | ROM lines | Python counterpart | Status | Notes |
|--------------|-----------|--------------------|--------|-------|
| `multi_hit` | `src/fight.c:187-247` | `mud/combat/engine.py:312` | ✅ AUDITED | `do_kill()` now enters combat through `multi_hit()` as ROM does. |
| `damage` | `src/fight.c:688-1016` | `mud/combat/engine.py:498` | ✅ AUDITED | `apply_damage()` now re-runs `is_safe()` at entry, matching ROM `damage()`. |
| `is_safe` | `src/fight.c:1018-1124` | `mud/combat/safety.py:14` | ✅ AUDITED | Port exists and the combat damage path now consistently uses it at the ROM-critical entry point. **Sub-bug fixed 2.13.60 (INV-041):** `fight.c:1040` checks `victim->pIndexData->pShop` but Python only checked `victim.pShop` (absent on `MobInstance`) and loaders never set `MobIndex.pShop`. Fixed in `world_state.py`, `shop_loader.py`, and `safety.py`. |
| `is_safe_spell` | `src/fight.c:1126-1221` | `mud/combat/safety.py:75` | ✅ AUDITED | Present; separate from the open melee-damage gap. |
| `do_kill` | `src/fight.c:2758-2819` | `mud/commands/combat.py:94` | ✅ AUDITED | Command now routes through `multi_hit()` instead of a single `attack_round()`. |

## Phase 2 — Verification highlights

### `do_kill` — ROM `src/fight.c:2815-2817`

ROM explicitly enters combat through the full multi-attack entrypoint:

```c
WAIT_STATE (ch, 1 * PULSE_VIOLENCE);
check_killer (ch, victim);
multi_hit (ch, victim, TYPE_UNDEFINED);
```

Python `mud/commands/combat.py:123-125` currently does:

```python
skill_registry._apply_wait_state(char, get_pulse_violence())
check_killer(char, victim)
return attack_round(char, victim)
```

That drops the entire ROM `multi_hit()` chain (`src/fight.c:209-244`): first swing, haste extra swing, second-attack skill roll, third-attack skill roll, and the post-first-hit `check_assist()` path already mirrored by `mud/combat/engine.py:338-341`.

### `damage` entry gate — ROM `src/fight.c:725-733`

ROM re-checks attack safety inside `damage()` itself:

```c
if (victim != ch)
{
    if (is_safe (ch, victim))
        return FALSE;
    check_killer (ch, victim);
```

Python `mud/combat/engine.py:528-620` has no corresponding `is_safe()` call at function entry. That means once combat is underway, subsequent `attack_round()` / `multi_hit()` passes keep landing even if one combatant is moved into a safe room. This is a direct behavioral divergence from ROM and from the already-ported `mud/combat/safety.py:is_safe`.

### Special-attack call chains

ROM applies the same safety contract by routing attack variants through `damage()` (`one_hit`, skills, weapon procs, mobprog damage paths). Python likewise funnels the core physical path through `mud/combat/engine.py:488` `apply_damage(...)`, and weapon special procs call `apply_damage(...)` at `mud/combat/engine.py:1502`, `mud/combat/engine.py:1522`, `mud/combat/engine.py:1532`, and `mud/combat/engine.py:1546`. Because the entry gate is missing there, every caller inherits the bug until `apply_damage()` is fixed.

## Phase 3 — Gaps

| Gap ID | Severity | ROM C | Python | Description | Status |
|--------|----------|-------|--------|-------------|--------|
| `FIGHT-001` | CRITICAL | `src/fight.c:2815-2817`, `src/fight.c:209-244` | `mud/commands/combat.py:123-128`, `mud/combat/engine.py:312-388` | `do_kill()` calls a single `attack_round()` instead of ROM `multi_hit(ch, victim, TYPE_UNDEFINED)`, so `kill` starts combat with one swing instead of the full ROM multi-attack sequence. | ✅ FIXED — `do_kill()` now routes through `multi_hit()` and preserves the first combat message as the command return. Regression test: `tests/integration/test_fight_c_do_kill_parity.py::test_do_kill_uses_rom_multi_hit_sequence`. Commit SHA recorded in session report for this one-gap commit. |
| `FIGHT-002` | CRITICAL | `src/fight.c:725-733`, plus all attack variants that rely on `damage()` | `mud/combat/engine.py:503-535` (`apply_damage`) and downstream callers such as `mud/combat/engine.py:1508`, `mud/combat/engine.py:1528`, `mud/combat/engine.py:1538`, `mud/combat/engine.py:1552` | The ROM `damage()` function early-exits through `is_safe(ch, victim)` before any fighting-state or HP mutation. Python never re-checks `is_safe()` inside the damage path, so combat continues after safe-room transitions. | ✅ FIXED — `apply_damage()` now re-checks `is_safe()` at entry, which also covers weapon special attacks and skill handlers because they already funnel through `apply_damage()`. Regression test: `tests/integration/test_fight_c_safe_room_damage_gate.py::test_attack_round_does_no_damage_after_fight_moves_into_safe_room`. Commit SHA recorded in session report for this one-gap commit. |
| `FIGHT-003` | CRITICAL | `src/fight.c:887-893` | `mud/combat/engine.py:_handle_death` | ROM applies the player death XP penalty inside the combat death branch after `group_gain()` and before `raw_kill()`. Python was killing the player without applying that penalty. | ✅ FIXED — `_handle_death()` now applies `gain_exp(victim, (2 * (exp_per_level * level - exp) / 3) + 50)` before `raw_kill()`, matching ROM control flow. Regression test: `tests/integration/test_character_advancement.py::test_player_kill_applies_rom_death_penalty`. |
| `FIGHT-004` | IMPORTANT | `src/fight.c:837-838` | `mud/combat/engine.py:704-709` | POS_MORTAL TO_ROOM `act("$n is mortally wounded, and will die soon, if not aided.", victim, NULL, NULL, TO_ROOM)`. Python uses `_broadcast_room(room, f"{victim.name} is mortally wounded...", exclude=victim)` — a single fixed string keyed on `victim.name`. ROM's act() macro substitutes `$n` per-listener through `PERS(victim, looker)`, so an invisible victim renders as `"someone"` to room observers without `DETECT_INVIS`. Python leaks the victim's name to every recipient regardless of `can_see`. Same PERS gap pattern as SAY-002/EMOTE-001/TELL-003/SHOUT-003/YELL-001. | ✅ FIXED (2.8.55) — `_broadcast_pos_change` helper renders `pers(victim, listener)` per-recipient. Test: `tests/integration/test_invisibility_combat.py::TestPositionChangeBroadcastPers::test_fight_004_pos_mortal_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-005` | IMPORTANT | `src/fight.c:845-846` | `mud/combat/engine.py:712-717` | POS_INCAP TO_ROOM `act("$n is incapacitated and will slowly die, if not aided.", victim, NULL, NULL, TO_ROOM)`. Same PERS gap as FIGHT-004 — `victim.name` baked into a fixed broadcast string instead of per-listener PERS render. | ✅ FIXED (2.8.56) — INCAP branch now uses `_broadcast_pos_change`. Test: `tests/integration/test_invisibility_combat.py::TestPositionChangeBroadcastPers::test_fight_005_pos_incap_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-006` | IMPORTANT | `src/fight.c:853-854` | `mud/combat/engine.py:720-725` | POS_STUNNED TO_ROOM `act("$n is stunned, but will probably recover.", victim, NULL, NULL, TO_ROOM)`. Same PERS gap as FIGHT-004. | ✅ FIXED (2.8.57) — STUNNED branch now uses `_broadcast_pos_change`. Test: `tests/integration/test_invisibility_combat.py::TestPositionChangeBroadcastPers::test_fight_006_pos_stunned_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-007` | CRITICAL | `src/fight.c:860` | `mud/combat/engine.py:729` | POS_DEAD TO_ROOM `act("{R$n is DEAD!!{x", victim, 0, 0, TO_ROOM)`. Three divergences: (a) same PERS gap as FIGHT-004 (`victim.name` baked into fixed broadcast); (b) missing `{R...{x` red colour wrapping; (c) wording typo — Python emits `"is DEAD!!!"` (3 exclamation marks) where ROM is `"is DEAD!!"` (2). | ✅ FIXED (2.8.58) — DEAD branch uses `_broadcast_pos_change` with `{{R{name} is DEAD!!{{x` template. Legacy `tests/test_combat.py` assertion updated to ROM-exact form. Test: `tests/integration/test_invisibility_combat.py::TestPositionChangeBroadcastPers::test_fight_007_pos_dead_broadcast_uses_pers_and_red_colour_and_two_bangs`. |
| `FIGHT-008` | IMPORTANT | `src/fight.c:861` | `mud/combat/engine.py:730` | POS_DEAD TO_CHAR self-message: ROM `send_to_char("{RYou have been KILLED!!{x\n\r\n\r", victim)`. Python returns `"You have been KILLED!!"` from `_position_change_message`. Two divergences: (a) missing `{R...{x` red colour wrapping; (b) missing the trailing blank-line newline ROM appends after the death notice. | ✅ FIXED (2.8.59) — return now `"{RYou have been KILLED!!{x\n"`. Python's `mud/net/protocol.py:send_to_char` auto-appends one `\r\n`, so the embedded `\n` plus the auto-append produces the same visual blank-line spacing ROM gets from its two `\n\r` pairs. Test: `tests/integration/test_invisibility_combat.py::TestPositionChangeBroadcastPers::test_fight_008_pos_dead_self_message_wraps_red_and_appends_blank_line`. |
| `FIGHT-009` | IMPORTANT | `src/fight.c:614-615` | `mud/combat/engine.py:1496-1500` | WEAPON_POISON TO_ROOM `act("$n is poisoned by the venom on $p.", victim, wield, NULL, TO_ROOM)`. Python emits `_broadcast_room(room, f"{victim.name} is poisoned by the venom on {weapon_name}.", exclude=victim)` — single fixed string keyed on `victim.name`. Same PERS gap as FIGHT-004 — invisible victim leaks identity to room observers without `DETECT_INVIS`. Wording matches ROM (`$n is poisoned by the venom on $p`). | ✅ FIXED (2.8.61) — poison broadcast routed through `_broadcast_pos_change` (extended to accept `**extra` for `$p` weapon name). Test: `tests/integration/test_weapon_proc_pers.py::TestWeaponProcBroadcastPers::test_fight_009_poison_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-010` | IMPORTANT | `src/fight.c:643` | `mud/combat/engine.py:1510` | WEAPON_VAMPIRIC TO_ROOM `act("$p draws life from $n.", victim, wield, NULL, TO_ROOM)`. Python: `_broadcast_room(room, f"{weapon_name} draws life from {victim.name}.", exclude=victim)`. PERS gap on `$n` (victim). Wording matches ROM. | ✅ FIXED (2.8.62) — vampiric broadcast routed through `_broadcast_pos_change`. Test: `tests/integration/test_weapon_proc_pers.py::TestWeaponProcBroadcastPers::test_fight_010_vampiric_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-011` | IMPORTANT | `src/fight.c:654` | `mud/combat/engine.py:1531` | WEAPON_FLAMING TO_ROOM `act("$n is burned by $p.", victim, wield, NULL, TO_ROOM)`. Python: `_broadcast_room(room, f"{victim.name} is burned by {weapon_name}.", exclude=victim)`. PERS gap on `$n`. Wording matches ROM. | ✅ FIXED (2.8.63) — flaming broadcast routed through `_broadcast_pos_change`. Test: `tests/integration/test_weapon_proc_pers.py::TestWeaponProcBroadcastPers::test_fight_011_flaming_broadcast_uses_pers_for_invisible_victim`. |
| `FIGHT-012` | IMPORTANT | `src/fight.c:663` | `mud/combat/engine.py:1541` | WEAPON_FROST TO_ROOM `act("$p freezes $n.", victim, wield, NULL, TO_ROOM)`. Two divergences: (a) PERS gap on `$n`; (b) wording — ROM is `"$p freezes $n."` (e.g. `"the sword freezes Alice."`) but Python emits `f"{victim.name} is frozen by {weapon_name}."` (`"Alice is frozen by the sword."`). | ✅ FIXED (2.8.64) — frost broadcast routed through `_broadcast_pos_change` with ROM-true wording `"{weapon} freezes {name}."`. Test: `tests/integration/test_weapon_proc_pers.py::TestWeaponProcBroadcastPers::test_fight_012_frost_broadcast_uses_pers_and_rom_wording`. |
| `FIGHT-013` | IMPORTANT | `src/fight.c:673-674` | `mud/combat/engine.py:1551-1555` | WEAPON_SHOCKING TO_ROOM `act("$n is struck by lightning from $p.", victim, wield, NULL, TO_ROOM)`. Python: `_broadcast_room(room, f"{victim.name} is struck by lightning from {weapon_name}.", exclude=victim)`. PERS gap on `$n`. Wording matches ROM. | ✅ FIXED (2.8.65) — shocking broadcast routed through `_broadcast_pos_change`. Test: `tests/integration/test_weapon_proc_pers.py::TestWeaponProcBroadcastPers::test_fight_013_shocking_broadcast_uses_pers_for_invisible_victim`. Closes the FIGHT-009..013 weapon-proc sweep. |
| `FIGHT-014` | IMPORTANT | `src/fight.c:961-970` (AUTOSAC dispatch → `do_function(ch, &do_sacrifice, "corpse")`); broadcast text at `src/act_obj.c:1856` (`act("$n sacrifices $p to Mota.", ch, obj, NULL, TO_ROOM)`) | `mud/combat/engine.py:_auto_sacrifice` | AUTOSAC TO_ROOM broadcast pre-renders `$n` from `attacker.name` via `expand_placeholders(...)` baked into a single `_broadcast_room` string, leaking the attacker's identity to room observers even when the attacker is invisible. **Note**: Python's `_auto_sacrifice` re-implements sacrifice logic inline rather than dispatching to `do_sacrifice` like ROM does at `src/fight.c:970`; that structural divergence is tracked separately (FIGHT-015 reserved). This gap addresses only the PERS leak. | ✅ FIXED (2.8.66) — `_auto_sacrifice` broadcast routed through `_broadcast_pos_change(attacker, "{name} sacrifices {corpse} to Mota.", corpse=corpse_name)`. Orphan `SimpleNamespace` import removed as a side effect. Test: `tests/integration/test_auto_sacrifice_pers.py::test_fight_014_auto_sacrifice_broadcast_uses_pers_for_invisible_attacker`. |
| `DAMMSG-001` | CRITICAL | `src/fight.c:2222` (`act(buf1, ch, NULL, victim, TO_NOTVICT)`) | `mud/combat/messages.py:dam_message`, `mud/combat/engine.py:_broadcast_damage_messages` | `dam_message` builds `room_msg` as a pre-rendered string baked with `attacker.name`/`victim.name`, then `engine.py` ships it via `_broadcast_room` once. ROM's `act(..., TO_NOTVICT)` evaluates `PERS(ch, looker)` AND `PERS(victim, looker)` per recipient — both names render independently for each observer. Python leaks both attacker and victim identities to every room observer regardless of `can_see`. Highest-volume PERS leak in combat (fires on every melee swing / spell hit). | ✅ FIXED (2.8.67) — `dam_message` now returns `{attacker}`/`{victim}` templates; `_broadcast_damage_messages` PERS-renders per recipient via `render_for` for TO_NOTVICT iteration over `room.people`. Test: `tests/integration/test_dam_message_pers.py::TestDamMessagePers::test_dammsg_001_to_notvict_renders_attacker_and_victim_per_observer`. |
| `DAMMSG-002` | IMPORTANT | `src/fight.c:2224` (`act(buf3, ch, NULL, victim, TO_VICT)`) | `mud/combat/messages.py:dam_message`, `mud/combat/engine.py:_broadcast_damage_messages` | `dam_message`'s `victim_msg` bakes `attacker.name` for `$n` substitution. ROM's `act(..., TO_VICT)` evaluates `PERS(ch, victim)` so an invisible attacker (no `DETECT_INVIS` on victim) renders as `"someone"` — Python instead leaks the attacker's real name. | ✅ FIXED (2.8.67) — TO_VICT render path uses `render_for(template, attacker, victim, observer=victim)`. Test: `tests/integration/test_dam_message_pers.py::TestDamMessagePers::test_dammsg_002_to_vict_renders_attacker_per_victim`. |
| `DAMMSG-003` | IMPORTANT | `src/fight.c:2223` (`act(buf2, ch, NULL, victim, TO_CHAR)`) | `mud/combat/messages.py:dam_message`, `mud/combat/engine.py:_broadcast_damage_messages` | `dam_message`'s `attacker_msg` bakes `victim.name` for `$N` substitution. ROM's `act(..., TO_CHAR)` evaluates `PERS(victim, ch)` — an invisible victim without `can_see` from attacker (rare but real, e.g. blind attacker) should render as `"someone"`. Python leaks the victim's name. | ✅ FIXED (2.8.67) — TO_CHAR render path uses `render_for(template, attacker, victim, observer=attacker)`; `damage()` return value also rendered for the attacker's view so test/scripted callers see PERS-correct strings. Test: `tests/integration/test_dam_message_pers.py::TestDamMessagePers::test_dammsg_003_to_char_renders_victim_per_attacker`. |
| `FIGHT-016` | IMPORTANT | `src/fight.c:616-624` | `mud/combat/engine.py:1561-1579` (`process_weapon_special_attacks`, WEAPON_POISON branch) | On a WEAPON_POISON hit where the victim fails the save, ROM builds and `affect_join`s a structured poison affect: `af.level = level*3/4`, `af.duration = level/2`, `af.location = APPLY_STR`, `af.modifier = -1`, `af.bitvector = AFF_POISON` (`level = wield->level` for a permanently-poisoned weapon). Python instead calls only `victim.add_affect(AffectFlag.POISON)` — a bare, untimed flag with **no duration** (never wears off), **no STR penalty**, and no `affect_join` merge semantics. So weapon poison in Python is a permanent flag with no stat effect, where ROM is a timed `level/2`-tick STR-reducing affect. Surfaced 2026-05-28 during ARITH-004 reclassification. Reference implementation: `spell_poison` at `mud/skills/handlers.py:6438-6447` already builds a `SpellEffect` + `victim.apply_spell_effect()` (the Python `affect_join` port at `mud/models/character.py:673`); the weapon proc differs only in params (STR -1 not -2, `level*3/4` level, `level/2` duration). | ✅ FIXED (2.9.88) — WEAPON_POISON branch now builds `SpellEffect(name="poison", level=c_div(level*3,4), duration=c_div(level,2), stat_modifiers={Stat.STR:-1}, affect_flag=AffectFlag.POISON, wear_off_message="You feel less sick.")` and calls `victim.apply_spell_effect(...)` (the `affect_join` port), replacing the bare `add_affect(AffectFlag.POISON)`. Test: `tests/integration/test_weapon_poison_affect.py::test_fight_016_weapon_poison_applies_timed_str_reducing_affect` (+ successful-save no-op case). |
| `FIGHT-017` | MINOR | `src/fight.c:605-608, 627-636` | `mud/combat/engine.py:1561` (`process_weapon_special_attacks`, WEAPON_POISON branch) | ROM derives the poison `level` from a **temporary** envenom affect if present — `affect_find(wield->affected, gsn_poison)` → use `poison->level`, else `wield->level` — and after applying the venom, **weakens a temporary poison per hit**: `poison->level = UMAX(0, poison->level - 2)`, `poison->duration = UMAX(0, poison->duration - 1)`, emitting `"The poison on $p has worn off."` to the wielder when either hits 0. Python's weapon-proc branch ignores the weapon's `affected` list entirely (always uses the weapon level) and never weakens an envenom (the `spell_poison`-object path at `handlers.py:6388` adds a `WeaponFlag.POISON` affect with `spell_name == "poison"` that never decays via combat). Depends on FIGHT-016 (shares the `level` variable). Filed 2026-05-28. | ✅ FIXED (2.9.89) — WEAPON_POISON branch now scans `wield.affected` for the temporary envenom affect (`spell_name == "poison"`, set by `spell_poison` on an object at `handlers.py:6397`); when present, `level = poison_af.level` (raw, no floor) instead of `wield.level`, and after the save block the envenom is weakened per hit (`level = max(0, level-2)`, `duration = max(0, duration-1)`) with `"The poison on {weapon} has worn off."` pushed to the **attacker** (TO_CHAR) when either hits 0. Permanent WEAPON_POISON weapons (no envenom affect) are unaffected. Test: `tests/integration/test_weapon_poison_affect.py::test_fight_017_*` (4 cases: level source, weakening-on-save, attacker-not-victim message, duration-zero trigger). |

| `FIGHT-018` | IMPORTANT | `src/fight.c:2215-2226` (`dam_message`) | `mud/combat/engine.py:_broadcast_damage_messages` | ROM `dam_message` emits the combat hit line TO_ROOM (self-inflicted, `ch == victim`) or TO_NOTVICT (normal) via `act()` with no `MOBtrigger = FALSE` wrap. Per `src/comm.c:2384`, every NPC recipient in the room then fires `mp_act_trigger(buf, to, ch, ..., TRIG_ACT)`, so mob ACT-progs respond to combat happening around them. Python's `_broadcast_damage_messages` PERS-renders the per-recipient combat text (DAMMSG-001/002/003) but never dispatched TRIG_ACT, so every combat-watching mobprog silently no-opped — same class of gap as the INV-025 act()-dispatch sweep (do_emote / position-transition / give / wear / get / drop / sacrifice). | ✅ FIXED (2.9.90) — after the TO_NOTVICT room loop, `_broadcast_damage_messages` now renders the room template canonically (real attacker/victim names, matching the `_broadcast_pos_change` pattern) and calls `mp_act_trigger_room(canonical, room, attacker, exclude=victim)`. `exclude=victim` reproduces TO_NOTVICT's actor+victim exclusion; the self-inflicted case (`attacker is victim`) collapses to TO_ROOM's single-actor exclusion. Test: `tests/integration/test_fight_018_dam_message_act_trigger_dispatch.py` (fires on a listening third-party NPC; excludes attacker and victim). |
| `FIGHT-019` | CRITICAL | `src/fight.c:386-516` (`one_hit` attack-roll: `while ((diceroll = number_bits(5)) >= 20); if (diceroll == 0 \|\| (diceroll != 19 && diceroll < thac0 - victim_ac))`) | `mud/combat/engine.py:attack_round` (`COMBAT_USE_THAC0` branch) gated off by `mud/config.py:115` (`COMBAT_USE_THAC0 = False`) | ROM `one_hit` resolves every melee swing through **one** model: the THAC0/`number_bits(5)` attack roll (re-roll loop until `< 20`, miss on `diceroll == 0` or `diceroll != 19 && diceroll < thac0 - victim_ac`). Python ships this faithful path behind `COMBAT_USE_THAC0`, **defaulted to False**, so production combat runs a **non-ROM percent model** instead (`to_hit = 50 + hitroll + c_div(victim_ac, 2)`, miss on `number_percent() > to_hit`). This diverges from ROM on **both** the RNG draw (one `number_percent()` vs the `number_bits(5)` re-roll loop — different stream consumption *and* distribution) and the hit/miss decision (flat 50%+hitroll vs class-thac0-vs-AC interpolation). Surfaced 2026-05-28 by the differential harness (FINDING-008 sub-issue 1): from seed 777 the drunk #3064 first attack is `miss` in ROM C but `scratch` (a hit) in Python; forcing `COMBAT_USE_THAC0=True` converges the outcome to ROM's `miss`. The percent model is pure divergence — ROM has no such path. | ✅ FIXED (2.11.4) — `attack_round` now resolves every swing through the THAC0 / `number_bits(5)` roll only; the percent branch and the `COMBAT_USE_THAC0` flag (`mud/config.py`) are deleted. **Surfaced + fixed a second masked divergence:** the THAC0 path always indexed `class_table[attacker.ch_class]`, which crashes for NPC attackers (`MobInstance` has no `ch_class`) and ignored ROM's NPC branch — `compute_thac0` gained a `thac0_pair` override and `attack_round` now derives an NPC attacker's `(thac0_00=20, thac0_32)` from its ACT class flag (WARRIOR -10 / THIEF -4 / CLERIC 2 / MAGE 6, default -4 "as good as a thief"; ROM `src/fight.c:445-457`). 15 percent-model-dependent combat tests re-baselined ROM-faithfully (guaranteed-hit nat 19 / high hitroll where they verify damage or XP rather than hit chance); fixed a pre-existing `test_room` isolation leak (room 3001 `.people` now restored) that THAC0's lower kill rate exposed. Tests: `tests/integration/test_fight_019_thac0_attack_roll.py` (seed-777 miss reproduction + NPC ACT-flag→thac0 selection), `tests/test_combat_thac0.py::test_thac0_npc_pair_overrides_class_table`. Closes FINDING-008 sub-issue 1. Sub-issue 2 (color-normalization) remains harness-side on `diff-harness`; **sub-issue 3 was re-triaged 2026-05-28 as a real engine bug — FIGHT-020 below — NOT the "harness capture double-count" the FIGHT-019 session recorded.** |
| `FIGHT-020` | CRITICAL | `src/fight.c:2771-2817` (`do_kill` → `multi_hit`, void; no return channel — all output via `act()`/`send_to_char` straight to the descriptor) + `src/fight.c:859-862` (death branch sends the killer NOTHING) | `mud/commands/combat.py:do_kill` returned `multi_hit(...)[0]` | **SINGLE-DELIVERY violation (INV-001 class) — nearly every other `multi_hit` caller (`do_murder`, `violence_tick`, `assist`, aggressive AI, `spec_funs`) discards the return and relies on `_push_message`** (the exception, `do_surrender`'s NPC-ignores branch, is a second instance of the same bug — filed under INV-001 follow-ups). `do_kill` returned `multi_hit(...)[0]` — the attacker's TO_CHAR dam_message, which `apply_damage` had ALREADY pushed via `_push_message`. The connection read loop (`mud/net/connection.py:1980-2000`) sends the command's return value AND drains the push, so a connected PC received every `kill`-initiated combat line **twice** (async `_push_message` send + `send_to_char(char, response)`) — the same class as the WS death-path double-message bug INV-001 was written for. Proven end-to-end: a mock-connection PC running `kill drunk` receives `['{2You miss drunk.{x', '{2You miss drunk.{x']`. Also delivered a **non-ROM "You kill X." line** (`_handle_death`'s return) only on the `do_kill` first strike — ROM `src/fight.c:859-862` sends the killer nothing on death. Surfaced 2026-05-28 re-triaging FINDING-008 sub-issue 3; the FIGHT-019 session had mis-classified it as a "capture artifact" because the prior triage checked only `multi_hit`'s return (one line) without tracing the connected-PC push+return double-channel. | ✅ FIXED (2.11.5) — `do_kill` runs `multi_hit(char, victim)` and `return ""` (ROM's void `do_kill`); all combat output flows through `_push_message`, single delivery. The non-ROM "You kill X." is no longer delivered (the killing-blow dam_message, pushed before the death branch, is the killer's last line — ROM-faithful). Regression: `tests/integration/test_kill_command_single_delivery.py` (connection-loop delivery harness: non-fatal swing delivered once; fatal blow delivers the dam_message once + no "You kill" line). 11 combat-content-return assertions across `tests/test_combat.py` / `test_combat_thac0_engine.py` / `test_combat_defenses_prob.py` re-baselined to read the pushed line from `char.messages` via a `deliver_kill` helper; `test_fight_c_do_kill_parity` asserts `do_kill` returns `""`. Broadens INV-001 (see CROSS_FILE_INVARIANTS_TRACKER). |
| `FIGHT-021` | CRITICAL | `src/fight.c` `multi_hit` (PC) + `mob_hit` (NPC): `chance = get_skill(ch, gsn_second_attack)/2; ... if (number_percent() < chance) {...}` and the same for `gsn_third_attack`/4 | `mud/combat/engine.py:multi_hit` (second/third-attack checks) | **Combat-tick RNG draw-count desync (FINDING-009 facet 1).** ROM resolves the 2nd/3rd extra attacks with `if (number_percent() < chance)`. `number_percent()` is the LEFT operand, so the RNG draw **fires unconditionally** — even when `chance == 0` for a skill-less attacker (every mob without a 2nd/3rd-attack skill, every low-level mage). Python guarded the draw behind `if second_attack_skill > 0:` / `if third_attack_skill > 0:`, so a 0-skill attacker drew **two fewer** `number_percent()` values than ROM, shifting the shared combat RNG stream for every later swing in the tick. Surfaced by the differential harness: with the drunk swinging first (FINDING-009 facet 3), its `mob_hit` consumed fewer draws than ROM, so the player's follow-up swing read a shifted stream and resolved hit/miss differently (`You scratch` vs ROM `You miss`). | ✅ FIXED (2.11.9) — both extra-attack checks now compute `chance` and draw `number_percent()` unconditionally (guards removed); `chance` stays 0 for a skill-less attacker so no extra swing lands, but the draw matches ROM. `chance = skill // 2` / `// 4` is bare `//` (skill is 0..100, non-negative → bit-identical to C truncation). Regression: `tests/integration/test_fight_021_multi_hit_unconditional_draw.py` (a skill-less attacker consumes exactly 2 unconditional 2nd/3rd-attack draws). Re-baselined `tests/integration/test_character_advancement.py::test_kill_mob_grants_xp_integration` (pinned the THAC0 roll to nat-19 so the kill→XP flow does not depend on the now-resequenced combat RNG stream / room-3001 Hassan interference). **Partial close of FINDING-009 facet 1** — the NPC `mob_hit` path still omits ROM's `number_range(0,2)` mob-cast + `number_range(0,8)` mob-skill draws (FIGHT-022). |
| `FIGHT-022` | CRITICAL | `src/fight.c` `mob_hit` (NPC path): after the 2nd/3rd-attack checks, when `ch->wait == 0` it draws `number = number_range(0, 2)` (mob spell-cast check) then `number = number_range(0, 8)` (mob off-skill dispatch: bash/berserk/disarm/kick/etc.) | `mud/combat/engine.py:multi_hit` (no NPC `mob_hit` branch — PC and NPC share one path) | **NPC combat-tick RNG draw-count desync (FINDING-009 facet 1, second sub-cause).** ROM routes NPC attackers through `mob_hit` (`multi_hit` calls `mob_hit(ch,victim,dt); return;` when `IS_NPC(ch)`), which after the unconditional 2nd/3rd-attack `number_percent()` draws (now matched by FIGHT-021) draws **two more** values when the mob is not waiting: `number_range(0,2)` (would mob_cast if ACT_MAGE/CLERIC) and `number_range(0,8)` (would fire an off-skill if the matching OFF_ flag is set). For a plain mob like the drunk #3064 (no spell/off flags) neither draw produces an action, but **both draws still happen**, so a faithful port must consume them or every NPC combat round leaves the shared RNG stream 2 draws ahead of ROM. Python has no `mob_hit`; the NPC path stops after FIGHT-021's 2nd/3rd checks, drawing 2 fewer than ROM. Surfaced by the differential harness `combat_melee_rounds`: after FIGHT-021 the drunk's swing still consumes 2 fewer draws than ROM `mob_hit`, so the player's follow-up swing reads a shifted stream. | ✅ FIXED (2.11.10) — added a faithful `mob_hit(attacker, victim, dt)` to `mud/combat/engine.py`; `multi_hit` now dispatches `if is_npc: return mob_hit(...)` after the position check (ROM `multi_hit`'s `if (IS_NPC(ch)) { mob_hit(...); return; }`). `mob_hit` mirrors ROM: one_hit → OFF_AREA_ATTACK sweep → haste/OFF_FAST extra swing → unconditional 2nd/3rd `number_percent()` draws (slow halves/zeroes only when not OFF_FAST) → `if wait>0 return` → `number_range(0,2)` (mob spell-cast check; casts stubbed in ROM) → `number_range(0,8)` (off-skill dispatch via `_mob_offensive_skill`: bash/berserk/disarm/kick/kick-dirt/trip/backstab by OFF_ flag; tail+crush no-op as in ROM). The two `number_range` draws fire even for a flag-less mob (the drunk) — they are part of the shared RNG stream. Regression: `tests/integration/test_fight_022_mob_hit_skill_rolls.py` (draws 2 number_percent + 2 number_range when `wait==0`; the two number_range gated out when `wait>0`). Full combat+integration suite green (2594 passed) — no flagged-mob `do_X`-dispatch fallout. **Closes FINDING-009 facet 1** (with FIGHT-021); the differential convergence check (Tester's swing flips `scratch`→`miss`) is the end-to-end verifier. The `do_X`-on-mob invocation is faithful to ROM's structure but its per-command draw fidelity is unverified for flagged mobs — file a follow-up if any `do_X` is later found to diverge. |
| `FIGHT-026` | CRITICAL | `src/fight.c` `do_dirt`/`do_trip`/`do_disarm` NPC gate (`if (IS_NPC(ch)) { if (!IS_SET(ch->off_flags, OFF_*)) return; }`) + `do_trip` DEX modifier (`get_curr_stat(ch/victim, STAT_DEX)`) | `mud/commands/combat.py:do_dirt`/`do_trip`/`do_disarm` | **NPC offensive-skill commands crashed when invoked by `mob_hit` (FIGHT-022 latent fallout).** FIGHT-022's `_mob_offensive_skill` dispatches the real `do_bash`/`do_kick`/`do_berserk`/`do_dirt`/`do_trip`/`do_disarm` on the NPC attacker (as ROM does). `do_bash`/`do_kick`/`do_berserk` already gated NPCs on their `OFF_` flag, but `do_dirt`/`do_trip`/`do_disarm` accessed `char.skills.get(...)` directly — `MobInstance` has no `skills` dict → `AttributeError`. `do_trip` had a **second** crash: its DEX modifier read `getattr(victim, "perm_stat", [13]*5)[1]`, which `IndexError`s on the empty-list `perm_stat` default (the `[13]*5` fallback only applies when the attribute is *missing*, not empty). Latent at FIGHT-022 (no test rolled a flagged mob into those branches); surfaced when FIGHT-024 reordered the combat tick and the mayor #3143 (OFF_TRIP) rolled into `do_trip` during `test_kill_mob_grants_xp_integration`. | ✅ FIXED (2.11.11) — `do_dirt`/`do_trip` now mirror the `do_kick` NPC pattern (gate on `OFF_KICK_DIRT`/`OFF_TRIP`, treat skill as learned=100 for NPCs); `do_disarm` skips the PC percent gate for NPCs (the `mob_hit` dispatch + weapon checks gate it) and reads hand-to-hand via the NPC-safe `_character_skill_percent`. `do_trip`'s DEX modifier now uses the canonical `get_curr_stat(Stat.DEX)` (bounds-safe + ROM-faithful: current vs permanent stat). Added `_npc_off_flags` helper. Regression: `tests/integration/test_fight_026_npc_offensive_skill_no_crash.py`. **Closes only the crash** — per-command RNG-draw fidelity for flagged-mob `do_X` is still unverified (see Follow-ups below). |

| `FIGHT-024` | CRITICAL | `src/fight.c:76` `violence_update` (`for (ch = char_list; ...; ch = ch_next)` — head-first walk) + `src/db.c:2256-2257` `create_mobile` + `src/nanny.c:757-758` PC login (both **prepend** new chars to `char_list`) | `mud/game_loop.py:violence_tick` (`for ch in list(character_registry)` — forward/append order) | **Combat-tick iteration-order divergence (FINDING-009 facet 3).** ROM `violence_update` walks `char_list` from the head, and every newly created actor is prepended to `char_list`, so the list is in **reverse-creation order**: the most-recently-loaded actor swings first. Python's `character_registry` is append-order (creation order), and `violence_tick` walked it forward — the exact reverse of ROM's swing order. Because the combat RNG stream is shared across all swings in a tick, *who swings first* determines who consumes the stream first; the forward walk desynced every downstream swing's hit/miss vs ROM. Surfaced by the differential harness `combat_melee_rounds`: the drunk (loaded after the player) must swing first as in ROM, but Python swung the player first. | ✅ FIXED (2.11.12) — `violence_tick` now iterates `list(reversed(character_registry))`, matching ROM's head-first `char_list` walk; `reversed` over a snapshot list mirrors ROM's `ch_next = ch->next` guard so mid-tick deaths can't perturb iteration. Regression: `tests/integration/test_fight_024_violence_tick_order.py` (two combatants registered A-then-B; ROM swings B first). With FIGHT-021+022 this completes the combat-tick RNG draw-order side of FINDING-009 (remaining step-5 diffs are render-only: facet 2 dam_type noun, facet 4 act capitalization). Exposed a latent FIGHT-022 crash (FIGHT-026, fixed first). |

| `FIGHT-023` | CRITICAL | `src/db2.c:270` `attack_lookup(fread_word)` + `src/handler.c:165` `attack_lookup` + `src/db.c:2086-2099` `create_mobile` random damtype (indices 3/7/11) + `src/fight.c:431,2176` (`attack_table[dam_type].damage`/`.noun`) | `mud/spawning/templates.py:_resolve_damage_type` / `from_prototype` random-default block | **Mob `dam_type` stored as a DamageType class, not the ROM attack_table index (FINDING-009 facet 2).** ROM `ch->dam_type` is an attack_table INDEX (`attack_lookup(word)`); the noun is `attack_table[index].noun` and the damage class is `attack_table[index].damage`. `_resolve_damage_type` collapsed the index to `int(attack_damage_type(index))` (a DamageType class), but `mud/combat/engine.py:537` reads `attacker.dam_type` as an attack index — so the drunk #3064 (damtype "beating" = index 13) rendered `attack_table[1].noun = "slice"`. The random-default block likewise assigned DamageType values `3/1/2` where ROM assigns indices `3/7/11`. | ✅ FIXED (2.11.13) — `_resolve_damage_type` returns the attack-table index (via `attack_lookup`), dropping the DamageType-name fallback (which could resolve a damtype word to a nonzero class and suppress ROM's `number_range(1,3)` default roll). Random default assigns ROM's literal `3/7/11`. `MobInstance.dam_type` now holds the attack index throughout; `one_hit` derives the class via `attack_damage_type(index)` (unchanged), so the drunk resolves DAM_BASH (was DAM_SLASH) — correct noun *and* AC-index/THAC0/RIV. Regression: `tests/integration/test_fight_023_mob_dam_type_attack_index.py`. Closes FINDING-009 facet 2. |

| `FIGHT-025` | CRITICAL | `src/comm.c:2373-2379` `act_new` first-char capitalization (`if (buf[0] == '{') buf[2] = UPPER(buf[2]); else buf[0] = UPPER(buf[0])`) | `mud/combat/messages.py:render_for` | **Combat act() output not capitalized (FINDING-009 facet 4).** ROM `act_new` upper-cases the first rendered character of every act() line (skipping a leading `{X` colour code). `render_for` (the combat dam_message render chokepoint, "mirrors ROM act()") substituted PERS placeholders but never capitalized, so an NPC-initiated swing rendered "the drunk's beating hits you." where ROM renders "The drunk's beating hits you." | ✅ FIXED (2.11.14) — `render_for` applies the ROM `act_new` capitalization via `_capitalize_act` (colour-code-aware: index 2 after a leading `{X`, else index 0). Regression: `tests/integration/test_fight_025_act_capitalization.py`. With FIGHT-021/022/023/024 this closes FINDING-009. |

| `FIGHT-028` | IMPORTANT | `src/fight.c:2171-2211` `dam_message` — for `dt != TYPE_HIT` the miss/hit line uses the attack noun: `sprintf(buf3, "{4$n's %s %s you%c{x", attack, vp, punct)` where `vp = "misses"` for `dam == 0` and `attack = attack_table[dt - TYPE_HIT].noun`. Only the bare `dt == TYPE_HIT` case (`src/fight.c:2157-2169`) renders `"$n %s you"` without a noun. | `mud/combat/messages.py:dam_message` / `mud/combat/engine.py` miss path | **Combat miss line drops the attack noun (FINDING-011).** When an NPC with a resolved attack type (e.g. the drunk #3064, `dt = TYPE_HIT + 13` "beating") **misses**, ROM renders `"The drunk's beating misses you."` (noun + "misses"), but Python renders `"The drunk misses you."` — the noun is dropped on the miss path only (a *hit* by the same mob correctly renders `"The drunk's beating hits you."`). So Python's miss branch routes through the no-noun `TYPE_HIT` template instead of the attack-noun template that its own hit path uses. Pre-existing; **surfaced by the differential harness `combat_melee_rounds` step 7** once FIGHT-027 advanced convergence past the round-2 damage divergence (C `"The drunk's beating misses you."` vs py `"The drunk misses you."`). | ✅ FIXED (2.11.16) — `dam_message` had a `percent <= 0` early-return that forced the no-noun template for *any* miss (and any low-damage hit rounding to percent 0), regardless of `dt`. ROM `src/fight.c:2157` chooses its template purely on `dt == TYPE_HIT` vs not — `dam == 0` only swaps `vs/vp` to "miss"/"misses" via `_severity_terms`. Deleted the block so the no-noun output is keyed solely on `attack is None` (i.e. `dt == TYPE_HIT`); a resolved-noun miss now renders `"$n's beating misses you"` like its own hit path. Regression: `tests/integration/test_fight_028_miss_attack_noun.py`. Re-baselined 5 stale unit assertions (test_combat/test_combat_thac0_engine/test_skills kick & BASH-`dam_type` misses) that asserted the noun-less form ROM never produces for `dt != TYPE_HIT`. Distinct root cause from FIGHT-027 (miss-line rendering, not damage calc). |
| `FIGHT-027` | CRITICAL | `src/fight.c:522-560` `one_hit` damage calc — the `if (IS_NPC(ch) && (!ch->pIndexData->new_format \|\| wield == NULL))` branch: `dam = dice(ch->damage[DICE_NUMBER], ch->damage[DICE_TYPE])` (new_format) / `dam = number_range(ch->level/2, ch->level*3/2)` (old_format) — vs the `else` clause's PC-unarmed `number_range(1 + 4*skill/100, 2*ch->level/3*skill/100)` | `mud/combat/engine.py:calculate_weapon_damage` (1390-1420) | **Unarmed-NPC damage uses the PC unarmed formula, not the mob damage dice (FINDING-010).** ROM `one_hit` routes an NPC attacker with no wielded weapon through a dedicated branch that rolls its **mob damage dice** `dice(damage[DICE_NUMBER], damage[DICE_TYPE])` (ROM `convert_mobile`, `src/db.c`, upgrades *every* mob to `new_format` at load, so the dice path is universal at runtime; the `!new_format` `number_range(level/2, level*3/2)` sub-branch is dead code). `calculate_weapon_damage` has **no NPC branch** — an unarmed mob falls through to the PC-unarmed `else` clause `number_range(1 + 4*skill/100, 2*level/3*skill/100)`. For the drunk #3064 (level 2, skill_total ≈ 50) that resolves to a **degenerate `number_range(3, 0)`** (from>to → ROM returns `from` = constant 3, consuming **zero** `number_mm` draws), where ROM rolls `dice(1, 6)` (range 1–6, **one** `number_mm` draw). So the Python drunk dealt a constant 3 every hit while ROM varied 1–6 — *and* the missing draw desynced the shared combat RNG stream from round 2 onward. Surfaced by the differential harness `combat_melee_rounds` step 6: C `"beating scratches you"` (1 dmg, ≤5% tier) vs py `"beating hits you"` (3 dmg, ≤15% tier); the C drunk's round-4 hit of **5** is unreachable by `number_range(level/2,level*3/2)`=1–3, proving the dice path. | ✅ FIXED (2.11.15) — `calculate_weapon_damage` now resolves an unarmed NPC (`is_npc and wield is None`) through `rng_mm.dice(damage[DICE_NUMBER], damage[DICE_TYPE])` *before* the PC-unarmed `else`, using the `MobInstance.damage` tuple (`(dice_number, dice_type, bonus)`; the bonus is applied later via `damroll`). Cited `convert_mobile`'s universal `new_format` upgrade in the comment (no dead `!new_format` branch — the Python `new_format` proto flag is unreliable, `False` even on the dice-carrying drunk). Regression: `tests/integration/test_fight_027_npc_unarmed_damage_dice.py`. End-to-end verifier: the `combat_melee_rounds` differential now converges on all 8 steps (step 5 stays matched; the added draw realigns the stream). Closes FINDING-010. |
| `FIGHT-029` | IMPORTANT | `src/fight.c` `do_rescue` (ROM writes all output via `act()`/`send_to_char` straight to the descriptor and returns void — no return-value channel) | `mud/commands/combat.py:do_rescue` (~263, `return message`) + `mud/skills/handlers.py:rescue` (7081-7091) | **SINGLE-DELIVERY violation (INV-001 class) — `do_rescue` double-delivers the rescuer's success line to a connected PC, and delivers the victim/room lines on the wrong channel.** `rescue()` does `caster.messages.append(char_msg)` / `target.messages.append(vict_msg)` / per-occupant `occupant.messages.append(room_msg)` AND `return char_msg`; `do_rescue` does `message = skill_handlers.rescue(...)` then `return message`. The connection read loop (`mud/net/connection.py:1980-2000`) sends a command's return value AND drains `char.messages`, so a connected PC rescuer receives `char_msg` ("You rescue X!") **twice** — the exact INV-001 shape fixed for `do_kill` (FIGHT-020) and `do_surrender`. On top of that, the `vict_msg`/`room_msg` legs use the raw `.messages` mailbox (the MAGIC-003 wrong-channel shape) so a connected target/bystander sees them late instead of immediately. Surfaced 2026-05-29 by the advisor while closing MAGIC-003 (the function directly above `sanctuary`). Fix: discard the return like `do_kill`/`do_surrender` (have `do_rescue` `return ""`) and migrate `rescue`'s three legs onto `_push_message`/`_send_to_char` (self via push, room via the per-occupant `_send_to_char` loop). **Needs a connection-loop double-delivery test** — template `tests/integration/test_kill_command_single_delivery.py` / `test_surrender_single_delivery.py`. Cross-ref INV-001 (SINGLE-DELIVERY). NOT folded into MAGIC-003 (channel-only) — separate gap. **FIXED (2.11.25, this session):** `rescue()` now delivers all three legs (`src/fight.c:3089-3091`) via `_send_to_char`; `do_rescue` returns `""` on success and the fail-path `"You fail the rescue."` `char.messages.append` was dropped (return-channel only). Test `tests/integration/test_rescue_single_delivery.py` splits the shape (advisor catch): rescuer line **count-once** (the kill/surrender double) + victim/bystander **push-present & mailbox-empty** (the MAGIC-003 wrong-channel — a pure count test would false-green here). Existing tests realigned to the void contract (`test_skills.py`, `test_skill_combat_rom_parity.py`, `test_skills_integration.py`). Out-of-scope sibling filed: the `"You are still recovering."` cross-command double (7 combat.py commands + registry.py) → INV-001 (d) OPEN. | ✅ FIXED (2.11.25) |
| `FIGHT-030` | IMPORTANT | `src/fight.c:3097` `check_killer (ch, fch)` inside `do_rescue`, between the two `stop_fighting(fch/victim, FALSE)` and the two `set_fighting(ch/fch)` calls (`:3094-3099`) | `mud/skills/handlers.py:rescue` (the `stop_fighting`/`set_fighting` block) — **no `check_killer(caster, foe)` call** | **`do_rescue` omits the rescuer's PK (KILLER) flagging.** ROM `do_rescue` flags the rescuer as a killer when it joins combat against `fch` via `check_killer(ch, fch)` (`src/fight.c:3097`). Python `rescue()` performs the two `stop_fighting` + two `set_fighting` calls but skips `check_killer`. The kill-stealing guard (`src/fight.c:3075` / Python `combat.py:do_rescue` `is_same_group` check) only fires when the opponent `fch` **is an NPC**, so a PC *may* rescue an ally fighting **another PC** — exactly the PvP case ROM flags via `check_killer` (sets `PLR_KILLER` + killer timer on the rescuer). `check_killer` is a real, wired function (`mud/combat/engine.py:1251`; used by `do_kill` `combat.py:124`, `do_murder` `murder.py:86`, and other combat paths), not a stub — so in the Python port the rescuer escapes the PK consequences ROM imposes. Surfaced 2026-05-29 by the advisor while closing FIGHT-029 (full `do_rescue` read, `src/fight.c:3032-3101`). Fix: add `check_killer(caster, foe)` in `rescue()` between the `stop_fighting` and `set_fighting` pairs (ROM ordering). Needs a test asserting the rescuer gains `PlayerFlag.KILLER` when rescuing an ally fighting another non-clan/non-killer PC. NOT folded into FIGHT-029 (delivery-channel only). **FIXED (2.11.26, this session):** `rescue()` now calls `check_killer(caster, foe)` between the two `stop_fighting` and the two `set_fighting` calls (`src/fight.c:3094-3099`). Placement is load-bearing — `check_killer` early-returns once `attacker.fighting is foe` (`engine.py:1291`), so it must precede `set_fighting(caster, foe)`. Test `tests/integration/test_rescue_killer_flag.py` (2 cases): PC foe → clan rescuer flagged `PlayerFlag.KILLER` (anchored on the state bit + the swap actually ran, so a missing flag can't masquerade as a bailed rescue); NPC foe → rescuer NOT flagged (the common ally-vs-mob case the PvP-only placement protects). | ✅ FIXED (2.11.26) |
| `FIGHT-031` | IMPORTANT | `src/comm.c:2376-2379` `act_new` first-char cap applied to the `src/fight.c` act() sites that bypass `dam_message`/`render_for`: position-change TO_ROOM (`:837-861` mortal/incap/stunned/DEAD), weapon-special TO_ROOM (`:614,643,654,663,673`), defense TO_CHAR (`check_parry :1318`, `check_shield_block :1345`, `check_dodge :1370`), flaming victim TO_CHAR (`:655` `act("$p sears your flesh.", ...)`) | `mud/combat/engine.py:_broadcast_pos_change` (per-listener PERS render) + `check_parry`/`check_dodge`/`check_shield_block` TO_CHAR `_push_message` f-strings + `process_weapon_special_attacks` flaming victim line | **Combat act() output that bypasses `render_for` (FIGHT-025's chokepoint) is not capitalized like ROM `act_new` (FINDING-009 facet 4 remainder).** FIGHT-025 capped only the `dam_message` render chokepoint. The position-change room broadcasts (mortal/incap/stunned/DEAD) and all 5 weapon-special room broadcasts funnel through `_broadcast_pos_change`, which PERS-renders per listener but never caps; the defense TO_CHAR lines (`{victim_name} parries/dodges/blocks your attack`) and the flaming victim line (`{weapon_name} sears your flesh.`) are direct f-string `_push_message` sites. So an NPC defender renders `"the guard parries your attack."` where ROM caps `"The guard parries your attack."`, and a dying NPC renders `"{Rthe orc is DEAD!!{x"` where ROM's `{`-kludge caps `buf[2]` → `"{RThe orc is DEAD!!{x"`. The POISON victim line (`src/fight.c:612` `send_to_char`, NOT `act()`) is correctly left uncapped. Completes the combat side of ACT-CAP-001 (`broadcast_room`/`act_format` wiznet remain) and the INV-029 combat cousin. | ✅ FIXED (2.11.39) — `_broadcast_pos_change` caps each per-listener PERS-rendered line + the `canonical` fed to `mp_act_trigger_room` (ROM caps `buf` at `src/comm.c:2376` *before* the TRIG_ACT dispatch at `:2384`); the three defense TO_CHAR f-strings + the flaming victim line (push + test-fallback) wrap in `capitalize_act_line`. The POISON victim `send_to_char` line + the already-capital "You…"/"The…" lines are left untouched (verified `act()` vs `send_to_char` per site against `src/fight.c:595-682`). Test: `tests/integration/test_fight_031_combat_act_capitalization.py` (5 — chokepoint room line + DEAD `{R`-kludge + parry/dodge/shield-block, asserting the cap *property* not the raw name so the FIGHT-032 PERS fix won't break them). Full-suite sweep re-baselined 3 stale lowercase asserts (`test_weapon_special_attacks.py` flaming ×2 "test weapon"→"Test weapon", `test_invisibility_combat.py` FIGHT-007 "someone is DEAD!!"→"Someone is DEAD!!" — the INV-027 mask + INV-029 cap composing). Surfaced FIGHT-032 (defense PERS gap) + FIGHT-033 (FROST/SHOCKING drop `$p`), filed in Follow-ups. |
| `FIGHT-035` | MEDIUM | `src/fight.c:2245-2255` `disarm` — fail path emits ONLY `act("{5$n tries to disarm $N, but fails.{x", ch, NULL, victim, TO_NOTVICT)` (`:2247`, excludes BOTH ch and victim; the `:2245` "won't budge" TO_VICT fires only on a NOREMOVE weapon); success emits `act("{5You disarm $N!{x", ch, NULL, victim, TO_CHAR)` (`:2254`) + `act("{5$n disarms $N!{x", ch, NULL, victim, TO_NOTVICT)` (`:2255`) | mud/skills/handlers.py:`disarm` (~3366, ~3380, ~3392) | **disarm double-broadcasts the fail line and drops ROM's TO_CHAR/TO_VICT/TO_NOTVICT structure + `{5..{x` colour.** Python's fail path calls `_act_room` TWICE with the same `"{caster} tries to disarm {victim}, but fails."` string — once `exclude=caster`, once `exclude=victim` — so the caster AND victim each receive it (ROM sends the fail line to NEITHER, only TO_NOTVICT bystanders) and every other bystander receives it TWICE. The success line `"{caster} disarms {victim}!"` bakes both names with a single room broadcast (no TO_CHAR "You disarm $N!" split, no TO_NOTVICT victim-exclusion). All sites bake names (no per-recipient PERS, INV-027) and drop the `{5`/`{x` colour codes. Surfaced 2026-06-01 during the INV-025 single-actor PERS sweep (excluded as a two-actor structural divergence). **ROM-ref correction (verified 2026-06-01):** the skill-roll fail path is `do_disarm` `src/fight.c:3211-3215` (THREE legs — TO_CHAR `"{5You fail to disarm $N.{x"`, TO_VICT `"{5$n tries to disarm you, but fails.{x"`, TO_NOTVICT `"{5$n tries to disarm $N, but fails.{x"`), **not** `:2245-2255` (those are the `disarm()` helper's NOREMOVE branch `:2244-2248` + success `:2252-2255`). The original row conflated the two. The double-broadcast bug was real but lived in the single-actor-excluded `act_to_room`/`_act_room` calls across all three paths. **Fix (2.12.38):** rebuilt all three paths in `handlers.py:disarm` to ROM's TO_CHAR/TO_VICT/TO_NOTVICT split — TO_CHAR/TO_VICT via `act_format` (PERS `$n`/`$N`, `$S` gendered possessive for the NOREMOVE "won't budge" line, `{5..{x` colour, "DISARMS" caps restored), TO_NOTVICT via `act_to_room(actor=caster, exclude=victim)` so the caster is auto-excluded AND the victim excluded (neither actor double-receives; bystanders get it once). Removed the dead `caster_name`/`victim_name` locals. Re-baselined `test_skills_combat.py:164` ("disarms you"→"DISARMS you and sends your weapon flying"). Test: `tests/integration/test_fight035_disarm_act_structure.py` (4 — fail TO_NOTVICT excludes both actors, invisible-caster `$n` masking, success colour/caps/exclusion, NOREMOVE `$S` possessive + caster exclusion). | ✅ FIXED (2.12.38) |
| `FIGHT-036` | LOW | `src/fight.c:2614` dirt-kick blind — `act("{5$n is blinded by the dirt in $s eyes!{x", victim, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`dirt_kicking` (~3274) | **dirt-kick blind room line uses "their eyes" instead of ROM `$s eyes`, bakes the victim name, and drops the `{5..{x` colour.** ROM renders the possessive via `$s` (his/her/its) and `$n` per-recipient through PERS, wrapped in `{5`/`{x`. Python emits `f"{victim_name} is blinded by the dirt in their eyes!"` — baked name (no INV-027 mask), literal "their" (no gendered possessive), no colour codes. Surfaced 2026-06-01 during the INV-025 sweep (excluded as a pronoun/text/colour divergence). **Fix (2.12.35):** the room leg in `handlers.py:dirt_kicking` converted from baked `_act_room(f"{victim_name} ... their eyes!")` to `act_to_room("{5$n is blinded by the dirt in $s eyes!{x", victim, exclude=victim)` — `$n` masks an invisible victim to "Someone" per recipient (INV-027), `$s` renders the gendered possessive (his/her/its), and the `{5..{x` colour codes are preserved. Test: `tests/integration/test_fight036_dirt_kick_pronoun_masking.py` (male→"his", female→"her", invisible victim→"Someone"). **Out-of-scope siblings filed → FIGHT-037.** | ✅ FIXED (2.12.35) |
| `FIGHT-037` | LOW | `src/fight.c:2616` dirt-kick TO_VICT — `act("{5$n kicks dirt in your eyes!{x", ch, NULL, victim, TO_VICT)`; `:2618` `send_to_char("{5You can't see a thing!{x\n\r", victim)`; **and the success branch (`:2611-2631`) has NO caster-facing message** | mud/skills/handlers.py:`dirt_kicking` (~3267-3269) | **dirt-kick victim legs drop the `{5..{x` colour, and Python invents a caster "You kick dirt…" line ROM never emits.** (1) Python `_send_to_char(victim, f"{caster_name} kicks dirt in your eyes!")` (`:3267`) bakes the caster name and drops `{5..{x` (ROM's `$n` here is per-recipient PERS — should mask an invisible kicker — plus colour). (2) `_send_to_char(victim, "You can't see a thing!")` (`:3268`) drops the `{5..{x` colour ROM applies at `:2618`. (3) `_send_to_char(caster, f"You kick dirt in {victim_name}'s eyes!")` (`:3269`) is **invented** — ROM's dirt-kick success branch sends the kicker NO self message (the kicker, being in the room, only sees the `:2614` TO_ROOM blind line, which now reaches them via the FIGHT-036 `act_to_room` since the caster is not excluded). Surfaced 2026-06-01 while closing FIGHT-036 (full `do_dirt` read, `src/fight.c:2560-2639`). Fix: render the TO_VICT leg via `act_format`/colour, colour the victim self-message, and drop the invented caster line. **Fix (2.12.37):** (1) the TO_VICT leg in `handlers.py:dirt_kicking` now renders via `act_format("{5$n kicks dirt in your eyes!{x", recipient=victim, actor=caster)` — `$n` masks an invisible kicker to "Someone" (INV-027), `{5..{x` preserved; (2) victim self-line now `"{5You can't see a thing!{x"`; (3) the invented `"You kick dirt in …'s eyes!"` caster line removed — the kicker only sees the `:2614` blind line (delivered via FIGHT-036's `act_to_room`, which excludes only the victim). Removed the now-dead `victim_name`/`caster_name` locals. Test: `tests/integration/test_fight037_dirt_kick_victim_legs.py` (4). | ✅ FIXED (2.12.37) |
| `FIGHT-038` | LOW | `src/fight.c:3202-3217` `do_disarm` — on ANY successful roll (`number_percent() < chance`, `:3202`) the success branch calls `disarm(ch, victim)` then `check_improve(ch, gsn_disarm, TRUE, 1)` (`:3206`). The NOREMOVE "won't budge" case is reached **through** that success branch (`disarm()` `:2242-2249` emits the budge messages and `return`s, control falls back to `:3206`), so a won't-budge disarm still credits skill improvement with **TRUE**. | mud/skills/handlers.py:`disarm` NOREMOVE branch (`:3391`) | **disarm NOREMOVE branch credits skill-improvement with `False` instead of ROM's `TRUE`.** Python's NOREMOVE branch (the `extra_flags & ITEM_NOREMOVE` early-return) calls `check_improve(caster, "disarm", False, 1)`, but ROM reaches the NOREMOVE case via the *successful*-roll branch and unconditionally calls `check_improve(..., TRUE, 1)` afterward — so ROM treats a won't-budge disarm as a skill success for improvement purposes, Python as a failure. Pre-existing (not introduced by FIGHT-035, which only rebuilt the message legs). Surfaced 2026-06-01 by advisor review while closing FIGHT-035. Fix: change the NOREMOVE branch's `check_improve` success flag `False`→`True` (mirroring ROM `:3206`), and add a test asserting a NOREMOVE disarm calls `check_improve` with `success=True`. **Fix (2.12.39):** `handlers.py:disarm` NOREMOVE branch (`:3391`) now calls `check_improve(caster, "disarm", True, 1)`. Test: `tests/integration/test_fight038_disarm_noremove_improve.py` (1). | ✅ FIXED (2.12.39) |
| `FIGHT-039` | LOW | `src/fight.c:2701` `do_trip` self-trip branch — `act("{5$n trips over $s own feet!{x", ch, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`trip` self-trip branch (`~7981`) | **trip self-trip room line drops the `{5..{x` colour, bakes the actor name (no `$n` PERS masking), and uses literal "their own feet" instead of ROM's `$s` gendered possessive — and delivers via the `messages.append` mailbox channel.** When `victim is caster` (you trip yourself), ROM broadcasts the coloured `act("{5$n trips over $s own feet!{x", ch, …, TO_ROOM)`; Python emits `f"{_character_name(caster)} trips over their own feet!"` through a hand-rolled `for occupant … occupant.messages.append(...)` loop. Surfaced 2026-06-01 during the MAGIC-014 INV-025 manual-room-loop sweep (split out because, unlike the magic batch's `$n`-only lines, this carries colour + `$s`). Fix: `act_to_room(room, "{5$n trips over $s own feet!{x", caster, exclude=caster)` (per-recipient PERS + `$s` possessive + colour preserved); re-baseline any test asserting "their own feet"/the baked name. Mirror the FIGHT-036/037 dirt-kick colour/`$s` fixes. **Fix (2.12.46):** room leg → `act_to_room(room, "{5$n trips over $s own feet!{x", caster, exclude=caster)`; also added the missing `{5..{x` colour to the self line (ROM `:2699` `send_to_char("{5You fall flat on your face!{x\n\r", ch)` — Python had emitted it uncoloured, same gap area). No existing test asserted these lines (no re-baseline). Test: `tests/integration/test_fight039_trip_self_room_pers.py` (2: visible male→"his"+colour+self-colour, invisible female→"Someone"+"her"). | ✅ FIXED (2.12.46) |
| `FIGHT-041` | LOW | `src/fight.c:1640` `death_cry` — the selected gore/death line is broadcast via `act(msg, ch, NULL, NULL, TO_ROOM)` (`msg` defaults to `"$n hits the ground ... DEAD."`, or a gore variant carrying `$n`/`$s`), so `comm.c` renders `$n` through `PERS(ch, to)` per recipient. | mud/combat/death.py:`death_cry` (`~330`) | **death_cry's in-room gore line baked the victim name once, leaking it to a sightless witness (INV-025 class).** Python called `room.broadcast(expand_placeholders(message_template, victim), exclude=victim)` — `expand_placeholders` substitutes `$n`→`victim.name` and `$s`→possessive **once**, so an invisible corpse-to-be rendered its real name to every listener (no per-recipient PERS masking) and an NPC rendered `name` instead of `short_descr`. Surfaced 2026-06-01 during the INV-025 `mud/combat/` re-probe (the next-task pass after the 2.12.48 command-layer sweep). **Fix (2.12.49):** room leg → `act_to_room(room, message_template, victim, exclude=victim)` — `$n` masks an invisible victim to "Someone" per recipient (INV-027), `$s` renders the gendered possessive, and a visible NPC now renders short_descr; `act_to_room` auto-excludes the actor (victim) matching ROM's TO_ROOM. The static neighbor-room cry (`_broadcast_neighbor_cry`, `"You hear someone's death cry."`) has no `$n`/name so PERS rendering is identical — but ROM emits it via `act(TO_ROOM)` too, so its missing TRIG_ACT dispatch is tracked separately as **FIGHT-042** (not a PERS gap). Test: `tests/integration/test_fight041_death_cry_pers_masking.py` (2: invisible victim→"Someone", sighted witness→name; `number_bits→0` pins the clean `$n`-only template). | ✅ FIXED (2.12.49) |
| `FIGHT-042` | LOW | `src/fight.c:1677-1688` `death_cry` neighbor loop — for each exit, with `ch->in_room` temporarily relocated to the adjacent room, `act(msg, ch, NULL, NULL, TO_ROOM)` (`:1685`) fires the cry. `death_cry` has **no** `MOBtrigger = FALSE` wrap, so per `src/comm.c:2384` every NPC in an adjacent room with a matching `HAS_TRIGGER(TRIG_ACT)` receives `mp_act_trigger`. | mud/combat/death.py:`_broadcast_neighbor_cry` (`~234`) | **death_cry's neighbor-room cry delivered visible text but never dispatched TRIG_ACT.** Python's `_broadcast_neighbor_cry` called `target.broadcast(message)` per exit — visible-text delivery only, no actor threaded through, so an NPC in an adjacent room with a `TRIG_ACT` mprog matching the death cry never fired (INV-025's primary contract). Sibling of FIGHT-041 (same `death_cry`, same `act(TO_ROOM)` without a MOBtrigger wrap); surfaced 2026-06-01 by advisor review while closing FIGHT-041, which had over-claimed the neighbor cry was "correctly a plain broadcast" (true for PERS, false for TRIG_ACT). The message has no `$n`/name, so PERS rendering is unchanged. **Fix (2.12.50):** `_broadcast_neighbor_cry` per-exit leg → `act_to_room(target, message, victim)` (per-recipient delivery + per-NPC TRIG_ACT dispatch). Test: `tests/integration/test_fight042_death_cry_neighbor_trig_act.py` (NPC listener in adjacent room → `mp_act_trigger` fires with "death cry"). | ✅ FIXED (2.12.50) |
| `FIGHT-040` | LOW | `src/fight.c:2521-2528` `do_dirt` — the ONLY already-affected check is `if (IS_AFFECTED(victim, AFF_BLIND)) act("$E's already been blinded.", ch, NULL, victim, TO_CHAR)` (`:2521-2524`), and it runs **before** the `victim == ch` "Very funny." check (`:2528`). | mud/skills/handlers.py:`dirt_kicking` early checks (`~3194-3203`) | **dirt-kick already-blinded check: wrong message + invented guard + wrong order.** Three divergences: (1) the AFF_BLIND branch baked the victim name (`"{name} is already blinded."`) instead of ROM's gendered `act("$E's already been blinded.")` (`$E`=He/She/It); (2) a **Python-invented** second guard `if victim.has_spell_effect("dirt kicking"): "{name} already has dirt in their eyes."` — no ROM equivalent, and dead code (the dirt-kick `SpellEffect` sets `affect_flag=AFF_BLIND` at `handlers.py:3289`, so the AFF_BLIND check above already catches a re-kick); (3) Python checked `victim == ch` ("Very funny.") **before** the AFF_BLIND check, reversing ROM's order (a blind caster self-targeting gets "Very funny." instead of ROM's "$E's already been blinded."). Surfaced 2026-06-01 as the MAGIC-013 "verify dirt-kicking line" carry-over. **Fix (2.12.47):** reordered AFF_BLIND before `victim == ch`; AFF_BLIND message → `act_format("$E's already been blinded.", recipient=caster, actor=caster, arg2=victim)`; deleted the invented `has_spell_effect("dirt kicking")` guard. No existing test asserted these lines. Test: `tests/integration/test_fight040_dirt_already_blinded.py` (gendered He/She/It ×3, re-kick→AFF_BLIND, blind-self→ROM order). | ✅ FIXED (2.12.47) |

| `FIGHT-044` | MEDIUM | `src/fight.c:2486` `do_bash` — after both the success and failure branches, unconditionally: `check_killer(ch, victim)` | `mud/commands/combat.py:do_bash` (~`:388-514`) | **`do_bash` missing unconditional `check_killer` call.** ROM `src/fight.c:2486` calls `check_killer(ch, victim)` as the last statement of `do_bash`, after both the success `damage()` branch and the failure `ch->position = POS_RESTING` branch. Python `do_bash` calls `check_improve` + returns but never calls `check_killer`. Note: `apply_damage()` sets `attacker.fighting = victim` before `check_killer` runs, so `check_killer`'s `attacker.fighting is victim` guard fires and the KILLER flag is never actually set — same behaviour in ROM C. The divergence is structural (ROM calls it; Python must too). **Fix (2.13.88):** added `check_killer(char, victim)` after `skill_registry._check_improve` in `do_bash`. Test: `tests/integration/test_fight044_bash_killer_flag.py` (2: success path calls check_killer, failure path calls check_killer — verified via monkeypatch; KILLER-flag assertion would be incorrect since fighting is already set by apply_damage). | ✅ FIXED (2.13.88) |
| `FIGHT-045` | MEDIUM | `src/fight.c:2753` `do_trip` — `check_killer(ch, victim)` is the last statement after BOTH the success and failure branches (unconditional) | `mud/commands/combat.py:do_trip` (~`:1054-1142`) | **`do_trip` calls `check_killer` only on success, not unconditionally.** ROM `src/fight.c:2753` calls `check_killer(ch, victim)` unconditionally after the entire success/failure if/else block. Python `do_trip` called `check_killer` only inside the success branch — the failure branch had no call. Note: like FIGHT-044, `apply_damage` starts the fight, so the `fighting` guard fires and KILLER is never set in practice (same in ROM C). **Fix (2.13.89):** refactored the if/else to collect message in a variable instead of returning early; `check_killer(char, victim)` moved after the block (unconditional). Test: `tests/integration/test_fight045_trip_killer_flag.py` (2: success path calls check_killer, failure path calls check_killer — failure test uses `skill_level=10` to force the failure branch). | ✅ FIXED (2.13.89) |
| `FIGHT-046` | MEDIUM | `src/fight.c:2951` `do_backstab` — `check_killer(ch, victim)` called BEFORE `WAIT_STATE` and before the success/failure roll, after all guard checks | `mud/commands/combat.py:do_backstab` (~`:328-385`) | **`do_backstab` missing `check_killer` call entirely.** ROM `src/fight.c:2951` calls `check_killer(ch, victim)` unconditionally after all the early-exit guards (weapon check, hurt-suspicious check) and BEFORE `WAIT_STATE`/the roll. Python `do_backstab` performed all the guard checks, applied wait state, rolled, and attacked — but never called `check_killer`. Since `do_backstab` requires `char.fighting is None` (returns early otherwise), `check_killer`'s `attacker.fighting is resolved_victim` guard does NOT fire — this is a real behavioral gap where a clan PC backstabbing another clan PC escapes the KILLER flag. **Fix (2.13.90):** added `check_killer(char, victim)` after the `wait > 0` guard and before the roll, matching ROM's pre-WAIT position. Test: `tests/integration/test_fight046_backstab_killer_flag.py` (2: success → KILLER set + message in `.messages`; failure with `skill=10` + `roll=99` → KILLER still set because check_killer fires before the roll). | ✅ FIXED (2.13.90) |
| `FIGHT-047` | MEDIUM | `src/fight.c:3138` `do_kick` — `check_killer(ch, victim)` called unconditionally after the success and failure branches | `mud/commands/combat.py:do_kick` (~`:144-203`) | **`do_kick` missing unconditional `check_killer` call.** ROM `src/fight.c:3138` calls `check_killer(ch, victim)` as the last statement of `do_kick`. Python `do_kick` dispatched to `skill_handlers.kick`, called `check_improve`, and returned — no `check_killer`. Note: like FIGHT-044/045, `do_kick` requires `char.fighting` to be set (`opponent = getattr(char, "fighting", None)` → return early if None), so `check_killer`'s `attacker.fighting is resolved_victim` guard fires and KILLER is never set — structural parity divergence only. **Fix (2.13.91):** added `check_killer(char, opponent)` after `_check_improve` in `do_kick`. Test: `tests/integration/test_fight047_kick_killer_flag.py` (2: success/failure both call check_killer via monkeypatch; test sets `ch_class=3` + `level=50` to pass the level guard). | ✅ FIXED (2.13.91) |
| `KICK-001` | MEDIUM | `src/fight.c:3109-3124` `do_kick` guard order | `mud/commands/combat.py:do_kick` | **`do_kick` checked `fighting == NULL` BEFORE the PC level gate — wrong message.** ROM `do_kick` evaluates guards in order: (1) `!IS_NPC && level < skill_table[gsn_kick].skill_level[class]` → "You better leave the martial arts to fighters."; (2) `IS_NPC && !OFF_KICK` → silent; (3) `fighting == NULL` → "You aren't fighting anyone." The port checked `fighting is None` **first**, so a sub-level PC who is not in combat saw "You aren't fighting anyone." where ROM shows the martial-arts message. Relocated the `fighting is None` check to after the level/OFF_KICK gates. Three existing tests (`test_combat.py::test_kick_command_requires_fighting`, `test_skill_combat_rom_parity.py::TestKickRomParity::test_kick_requires_fighting`, `test_skills_integration_combat_specials.py::TestKickSkill::test_kick_requires_fighting`) encoded the pre-fix order with a **below-level** char and were corrected to level the char past the kick requirement (ROM reaches the fighting branch only after clearing the level gate). Surfaced 2026-07-10 by a combat-skill hunter sweep, verified against ROM C. Test: `tests/integration/test_kick001_guard_order.py` (2). | ✅ FIXED (2.14.295) |
| `TRIP-001` | MINOR | `src/fight.c:2654` `do_trip` no-skill message | `mud/commands/combat.py:do_trip` | **`do_trip`'s no-skill message dropped a space.** ROM sends `"Tripping?  What's that?"` — TWO spaces after `Tripping?` (ROM strings frequently double-space); the port had one. Same byte-fidelity class as PASSWORD-002. Surfaced 2026-07-10 by a combat-skill hunter sweep, verified against ROM C. Test: `tests/integration/test_trip001_message_fidelity.py` (1, exact-byte `==`). | ✅ FIXED (2.14.296) |
| `FIGHT-048` | HIGH | `src/fight.c:2888` `do_murder` — `do_function(victim, &do_yell, buf)` — victim yells "Help! I am being attacked by X!" area-wide; no message sent to attacker from do_murder itself | `mud/commands/murder.py:do_murder` (~`:29-92`) | **`do_murder` victim yell never delivered — yell built as string but returned to attacker instead of broadcast.** ROM `src/fight.c:2888` calls `do_function(victim, &do_yell, buf)` which makes the VICTIM yell the help message to the entire area (every character in the same area with COMM_NOSHOUT unset sees "$n yells 'Help! I am being attacked by X!'" where $n = victim). Python builds `yell_msg = f"Help! I am being attacked by {attacker_name}!"` but returns it as part of the `do_murder` command response string (sent to attacker) and never calls `do_yell(victim, ...)`. Also: Python returns an extra "You attack {victim_name}!" message that ROM never sends (attacker only sees combat output from `multi_hit`). **Fix (2.13.92):** added `from mud.commands.communication import do_yell` + `_push_message` import; replaced the return-string approach with `to_char_yell = do_yell(victim, yell_msg)` + `_push_message(victim, to_char_yell)` before `check_killer`; return changed to `""`. Test: `tests/integration/test_fight048_murder_yell.py` (2: victim receives "You yell '...'" in messages, return value does not contain yell text). | ✅ FIXED (2.13.92) |
| `FIGHT-049` | MEDIUM | `src/fight.c:2861` `do_murder` — `is_safe(ch, victim)` called before combat setup; ROM `is_safe` PC-vs-PC branch checks: `!is_clan(ch)`, `PLR_KILLER/THIEF` exception, `!is_clan(victim)`, `ch->level > victim->level + 8` | `mud/commands/murder.py:_murder_safety_check` | **`_murder_safety_check` missing level-difference check and other `is_safe` PC-vs-PC guards.** ROM `is_safe` line 1116 checks `ch->level > victim->level + 8` → "Pick on someone your own size." and sends it back. Python `_murder_safety_check` checks ROOM_SAFE, kill-stealing, and the charm-master guard, but never checks level difference. Also missing from Python `is_safe` (mud/combat/safety.py): ACT_PET check, charm-ownership check for non-owner, NPC charmed mob PC-attack guard — these are separate from the PC-vs-PC clan path handled by `_murder_safety_check`. **Fix (2.13.95):** added PC-vs-PC block to `_murder_safety_check` — when `not char.is_npc and not victim.is_npc`: `is_clan_member(char)` gate → KILLER/THIEF bypass → `is_clan_member(victim)` gate → `char.level > victim.level + 8` gate. Imported `is_clan_member` and `PlayerFlag`. FIGHT-050 filed for remaining `mud/combat/safety.py:is_safe` gaps (ACT_PET, charm-ownership, NPC-charmed-PC-attack). Test: `tests/integration/test_fight049_murder_safety_pc_vs_pc.py` (6: non-clan attacker, KILLER bypass, THIEF bypass, non-clan victim, level-diff blocked, equal-clan baseline). | ✅ FIXED (2.13.95) |
| `FIGHT-050` | MEDIUM | `src/fight.c:1040-1094` `is_safe` NPC-attacker-vs-PC sub-branch — three guards missing from `mud/combat/safety.py:is_safe`: (1) `IS_SET(victim->act, ACT_PET)` → "But $N looks so cute and cuddly..." (`act` TO_CHAR); (2) `IS_AFFECTED(victim, AFF_CHARM) && ch != victim->master` → "You don't own that monster."; (3) NPC charmed/owned mob: `IS_AFFECTED(ch, AFF_CHARM) && ch->master != NULL && ch->master->fighting != victim` → "Players are your friends!" | `mud/combat/safety.py:is_safe` | **Python `is_safe` missing three victim-NPC-PC and NPC-attacker-PC-victim guards.** Surfaced during FIGHT-049 investigation: (1) ACT_PET guard (ROM `:1059`) — no `ACT_PET` check in Python; a pet NPC will not be blocked when being attacked. (2) Charm-ownership guard for non-owner (ROM `:1067`) — Python `is_safe` has no `AFF_CHARM` check on victim for attacker-is-not-owner; the guard in `_murder_safety_check` only covers the attacker being charmed, not the victim. (3) NPC charmed-mob-PC-attack guard (ROM `:1087-1093`) — `IS_AFFECTED(ch, AFF_CHARM) && ch->master != NULL && ch->master->fighting != victim` → "Players are your friends!" — Python `is_safe` has no such guard; `_murder_safety_check` has a similar but less precise guard (checks `master is victim` rather than `master.fighting != victim`). These are MEDIUM severity because the `_murder_safety_check` in murder.py partially compensates for some paths, but `is_safe` in `safety.py` is called from many other combat entry points. Scoped out of FIGHT-049 due to CRITICAL blast radius of `is_safe` (46 transitive impacts). **Fix (2.13.96):** added PC-attacker sub-block inside the victim-is-NPC branch: `ActFlag.PET` check (ROM :1059) and `AffectFlag.CHARM` non-owner check (ROM :1067). Added charmed-mob guard inside the NPC-attacker-vs-PC branch: `AffectFlag.CHARM && master is not None && master.fighting is not victim` (ROM :1087-1093). Imported `AffectFlag` in lazy import block. Test: `tests/integration/test_fight050_is_safe_npc_guards.py` (6: PC blocked from pet, NPC can attack pet, PC blocked from non-owned charmed NPC, owner can attack own charmed NPC, charmed NPC blocked from non-master target, charmed NPC allowed to attack master's target). | ✅ FIXED (2.13.96) |
| `FIGHT-051` | MEDIUM | `src/fight.c:2861` `do_murder` → `is_safe(ch, victim)` — ROM calls `is_safe` first, which runs the NPC-victim PC-attacker sub-block: (1) `IS_SET(victim->act, ACT_PET)` → return TRUE; (2) `IS_AFFECTED(victim, AFF_CHARM) && ch != victim->master` → return TRUE | `mud/commands/murder.py:_murder_safety_check` | **`_murder_safety_check` missing victim-NPC ACT_PET and AFF_CHARM non-owner guards.** Python `do_murder` bypasses `is_safe` entirely and calls `_murder_safety_check` directly. `_murder_safety_check` has the charm-master guard for the ATTACKER (`char.affected_by & CHARM and master is victim`) but not the symmetric victim-side guards. ROM `do_murder` calls `is_safe(ch, victim)` at line 2861, so a PC trying to murder a pet NPC would be blocked (ROM `:1059`), and a PC trying to murder a charmed NPC they don't own would be blocked (ROM `:1067`). Neither block exists in Python `_murder_safety_check`. Surfaced as a candidate during FIGHT-050 investigation. **Fix (2.13.97):** added `if victim_is_npc and not getattr(char, "is_npc", False)` sub-block after the kill-stealing check: (1) `victim_act & ActFlag.PET` → "But {name} looks so cute and cuddly..." (ROM :1059); (2) `victim_affected & AffectFlag.CHARM and victim.master is not char` → "You don't own that monster." (ROM :1067). Test: `tests/integration/test_fight051_murder_safety_npc_guards.py` (4: pet blocked, plain NPC allowed, charmed-non-owned blocked, owner-charmed allowed). | ✅ FIXED (2.13.97) |
| `FIGHT-052` | LOW | `src/fight.c:1080-1093` `is_safe` NPC-attacker-vs-PC sub-branch — guard ordering: ROM checks safe-room (`:1083`) BEFORE charmed-mob guard (`:1087`). | `mud/commands/combat.py:_kill_safety_message` | **`_kill_safety_message` inverts the guard order for NPC-attacker-vs-PC.** ROM `is_safe` NPC-attacker branch (`:1080-1093`) checks `ROOM_SAFE` first, then the charmed-mob guard. `_kill_safety_message` (lines 70-76) checks charmed-mob guard first, then safe-room. Corner case: charmed mob in safe room attacking a PC whose master is not fighting that PC — ROM returns "Not in this room." (safe-room wins); Python returns "Players are your friends!" (charmed-mob wins). The player sees the wrong message and may misunderstand why the attack was blocked. LOW severity because both paths return a message and block the attack; the behavioral difference is only which message is displayed. **Fix (2.13.98):** swapped the two checks in the `if getattr(attacker, "is_npc", False)` branch — safe-room check moved before the charmed-mob guard, with ROM citation comments. Test: `tests/integration/test_fight052_kill_safety_npc_guard_order.py::test_fight052_charmed_npc_in_safe_room_returns_safe_room_message`. | ✅ FIXED (2.13.98) |
| `FIGHT-053` | LOW | `src/fight.c:155-170` `check_assist` NPC-assist target-selection loop — `number++` is inside the `if (number_range(0, number) == 0)` block; it only increments when a new target is chosen | `mud/combat/assist.py:check_assist` | **`check_assist` incremented `number` unconditionally, desyncing the MM RNG stream.** ROM's target-selection loop calls `number_range(0, number)` for each visible group member and only increments `number` when that member is chosen as the new target (ROM `:165`). Python incremented `number` unconditionally after each visible group member, regardless of selection. With 3+ visible group members where the 2nd is not chosen, ROM makes the 3rd call with `number_range(0, 1)` (number stayed at 1); Python made the call with `number_range(0, 2)` — a different upper bound from the same MM RNG state, desyncing the shared combat RNG stream for all subsequent events. Secondary effect: Python's algorithm produced a uniform distribution over group members; ROM's biased algorithm weights later members more heavily. **Fix (2.13.99):** moved `number += 1` inside the `if rng_mm.number_range(0, number) == 0:` block — single line move, matching ROM src/fight.c:165. Test: `tests/integration/test_fight053_check_assist_rng_increment.py::test_fight053_check_assist_number_increments_on_selection_only` — patches `can_see_character`, `number_bits`, and `number_range`; verifies the 3rd call receives `(0, 1)` not `(0, 2)` after the 2nd group member is skipped. | ✅ FIXED (2.13.99) |
| `FIGHT-054` | HIGH | `src/fight.c:2986-3003` `do_flee` — exit-selection loop: 6 attempts of `number_door()` (0–5), per-attempt `EX_CLOSED` check (`exit_info & EX_CLOSED = 1<<1`), daze block (`number_range(0, ch->daze) != 0`), and NPC `ROOM_NO_MOB` guard; position set to `POS_STANDING` when not fighting | `mud/commands/combat.py:do_flee` | **`do_flee` used a fake dex-chance roll not present in ROM.** Python replaced ROM's 6-attempt `number_door()` loop with a dexterity-based `number_percent()` success check (`chance = 50 + (dex - 13) * 5`) that does not exist in ROM. Consequences: (1) RNG stream desync — ROM makes up to 6 `number_door()` draws per flee attempt; Python made 1 `number_percent()` draw; (2) no daze check — ROM's `number_range(0, ch->daze) != 0` (`:2994`) could block a flee even when a valid exit exists; Python never consulted `daze`; (3) no `ROOM_NO_MOB` guard — NPC characters could flee into flagged rooms (ROM `:2996-2998`); (4) wrong EX_CLOSED bit — Python checked `exit_info & 1` (EX_ISDOOR) instead of `& 2` (EX_CLOSED); (5) no `POS_STANDING` reset when not fighting (ROM `:2979-2980`). Also updated `test_fight043_flee_xp_loss.py` fixtures to use canonical list-of-6 exits and replaced `number_percent` flee-success patches with `number_door` patches. **Fix (2.14.0):** replaced the dex-chance block with ROM's 6-attempt `number_door()` loop; added per-attempt `EX_CLOSED` (`1<<1`) check, daze check (`rng_mm.number_range(0, daze) != 0`), and NPC `RoomFlag.ROOM_NO_MOB` guard; added `position = STANDING` reset in the not-fighting path; imported `EX_CLOSED` from constants. Tests: `tests/integration/test_fight054_do_flee_mechanism.py` (5: loop calls `number_door()` 6 times then panics, daze blocks all attempts, ROOM_NO_MOB blocks NPC, successful flee uses `number_door` not `number_percent`, not-fighting sets STANDING). All 5 mutation-sensitive tests verified red before fix, green after. | ✅ FIXED (2.14.0) |
| `FIGHT-055` | MEDIUM | `src/fight.c:1878-1914` `xp_compute` — (A) alignment is mutated regardless of `base_exp` value: `UMAX(1, change)` forces a minimum shift of 1 when `align > 500` or `align < -500` even when `base_exp == 0`; (B) XP multiplier (`src/fight.c:1916+`) reads `gch->alignment` AFTER mutation | `mud/groups/xp.py:xp_compute` | **Two divergences in `xp_compute`.** (A) **Early-return skips alignment on zero-XP kills.** Python line 169 `if base_exp <= 0: return 0` exits before the alignment block. ROM has no such early return — when `level_range < -9` gives `base_exp == 0`, the alignment computation still runs. `UMAX(1, change)` forces `change = 1` when `align_delta > 500` or `< -500`, so even a trivially weak kill nudges alignment by 1. Python leaves alignment unchanged. (B) **XP multiplier uses pre-mutation alignment snapshot.** Python captured `gch_alignment = _resolve_level(getattr(gch, "alignment", 0))` (line 173) before the alignment mutation block, then used this snapshot in the XP multiplier branch conditions (lines 194–238). ROM `src/fight.c:1916` reads `gch->alignment` which is already post-mutation. A character whose alignment crosses a threshold during mutation (e.g. 510→426 crossing the 500 boundary) gets the wrong XP rate: Python's "goodie two shoes" path (`gch_alignment=510>500`) gives `xp=base_exp/4`; ROM's "a little good" path (`gch->alignment=426>200`) gives `xp=base_exp/2`. **Fix (2.14.1):** (A) removed the `if base_exp <= 0: return 0` early return — alignment runs unconditionally, then XP naturally computes to 0 when `base_exp==0`. (B) added `post_alignment = gch.alignment` after the alignment block; replaced all four `gch_alignment` references in the multiplier (`elif gch_alignment > 500`, `elif gch_alignment < -500`, `elif gch_alignment > 200`, `elif gch_alignment < -200`) with `post_alignment`. Tests: `tests/integration/test_fight055_xp_compute_alignment_gaps.py` (2: zero-base-exp aligns by 1; multiplier uses post-mutation 426 not snapshot 510). Both tests verified red before fix, green after. | ✅ FIXED (2.14.1) |
| `FIGHT-056` | HIGH | `src/fight.c:717-720` `damage` — two sequential soft-caps before any other modifier: `if (dam > 35) dam = (dam-35)/2+35; if (dam > 80) dam = (dam-80)/2+80;` | `mud/combat/engine.py:apply_damage` | **Damage soft-cap entirely missing from Python `apply_damage`.** ROM's `damage()` applies the two-tier cap before `is_safe`, fighting setup, drunk/sanctuary, parry/dodge, and RIV. Python had none of it — a single weapon hit or spell dealing 200 raw damage would land as 200 rather than ROM's capped 98, causing significantly inflated lethal damage spikes. Affects ALL callers of `apply_damage` (weapons, spells, kicks, mob programs). **Fix (2.14.2):** added cap block at the top of `apply_damage`, immediately after the `DEAD` position guard and before `is_safe`. Used `c_div` for ROM-faithful integer truncation. Tests: `tests/integration/test_fight056_damage_soft_cap.py` (4: below-35 unchanged, first-cap at 50→42, both-caps at 200→98, boundary-80 skip at 125→80). All 4 verified red before fix, green after. | ✅ FIXED (2.14.2) |
| `FIGHT-057` | HIGH | `src/fight.c:804-816` `damage` — `check_immune(victim, dam_type)` called exactly once inside `damage()`: IS_IMMUNE → `dam=0`; IS_RESISTANT → `dam -= dam/3`; IS_VULNERABLE → `dam += dam/2` | `mud/combat/engine.py:attack_round` | **Double-RIV: resistance/vulnerability applied twice for weapon attacks.** Python split `damage()` across `attack_round` + `apply_damage`. `attack_round` applied RIV (lines 615-623) then passed the scaled damage to `apply_damage` which applied RIV again (lines 707-723). An IS_RESISTANT victim took `(1-1/3)^2 ≈ 44%` of raw damage instead of ROM's `1-1/3 ≈ 67%`. Confirmed empirically: 50 raw → 23 actual vs expected 33 (ratio 0.460 vs 0.667). **Fix (2.14.3):** removed the RIV block from `attack_round` (the pre-application at former lines 615-623) and dropped `immune=immune` from the `apply_damage` call — `apply_damage` already handles RIV correctly via the single check at lines 707-723. Tests: `tests/integration/test_fight057_double_riv.py` (2: resistant victim gets single-RIV not double-RIV; vulnerable victim gets single-VULN). Both verified red before fix, green after. | ✅ FIXED (2.14.3) |
| `FIGHT-058` | HIGH | `src/fight.c:775-785` `damage` — damage modifiers (drunk/sanctuary/protection) applied inside `damage()` for ALL callers: `if (dam > 1 && !IS_NPC(victim) && victim->pcdata->condition[COND_DRUNK] > 10) dam = 9 * dam / 10; if (dam > 1 && IS_AFFECTED(victim, AFF_SANCTUARY)) dam /= 2; if (dam > 1 && (IS_AFFECTED(victim, AFF_PROTECT_EVIL) && IS_EVIL(ch) || IS_AFFECTED(victim, AFF_PROTECT_GOOD) && IS_GOOD(ch))) dam -= dam / 4;` | `mud/combat/engine.py:apply_damage_reduction` (called only from `one_hit`) | **Damage modifiers (drunk/sanctuary/protection) bypass `apply_damage` for spells and skills.** `apply_damage_reduction` was factored out of the damage pipeline and only called from `one_hit` (the melee weapon path). Every spell handler in `mud/skills/handlers.py` that calls `apply_damage` directly (fireball, flamestrike, earthquake, energy_drain, chain_lightning, lightning_bolt, magic_missile, ray_of_truth, cause_*, etc.) bypassed sanctuary/drunk/protection entirely — a sanctuary-buffed PC took full fire damage from a fireball, a drunk PC took unmodified damage, etc. ROM applies all three modifiers unconditionally inside `damage()` before any other post-is_safe step. **Fix (2.14.4):** moved `apply_damage_reduction` call INTO `apply_damage` (after `is_safe`, before parry/dodge, matching ROM order at fight.c:775-785 vs fight.c:793-801) and removed the pre-call from `one_hit` to prevent double-reduction on the melee path. All 25+ `apply_damage` callers (spells, skills, weapon procs, mob programs, tick effects) now automatically get drunk/sanctuary/protection reductions. Tests: `tests/integration/test_fight058_damage_reduction_bypass.py` (4: spell sanctuary halves damage; spell drunk reduces to 9/10; spell protect_evil reduces by 1/4; melee no double-reduce). All 4 verified red before fix, green after. | ✅ FIXED (2.14.4) |
| `FIGHT-060` | MEDIUM | `src/fight.c:139-150` `check_assist` NPC-assist block — flat `||` OR chain: if `ASSIST_RACE` flag set but race differs, C continues evaluating `ASSIST_ALIGN` and `ASSIST_VNUM` sub-conditions | `mud/combat/assist.py:check_assist` | **`elif` chain skips ASSIST_ALIGN/ASSIST_VNUM when ASSIST_RACE flag set but race doesn't match.** Python used `elif rch_off_flags & OffFlag.ASSIST_RACE` for each NPC assist condition. When `ASSIST_RACE` flag is set but the inner race predicate fails, Python's `elif` chain exits without checking `ASSIST_ALIGN` or `ASSIST_VNUM`. ROM C evaluates each `||` operand independently; a false race-predicate does not prevent ASSIST_ALIGN from firing. Secondary effect: the `number_bits(1)` skip draw and `number_range` target-selection loop are gated behind `should_assist`; a missed assist is also an MM RNG desync for all downstream combat events. **Fix (2.14.7):** converted `elif rch_off_flags & OffFlag.ASSIST_RACE/ALIGN/VNUM` to independent `if` checks — 3-character change per line — mirroring the flat ROM `||` semantics. Added ROM citation comment. Tests: `tests/integration/test_fight060_check_assist_elif_chain.py` (2: ASSIST_ALIGN fires when ASSIST_RACE flag set but race differs; ASSIST_VNUM fires when ASSIST_RACE flag set but race differs). Both verified red before fix, green after. | ✅ FIXED (2.14.7) |
| `FIGHT-059` | MEDIUM | `src/fight.c:864-869` `damage` — `default:` case of `switch(victim->position)`: `if (dam > victim->max_hit / 4) send_to_char("{RThat really did HURT!{x\n\r", victim); if (victim->hit < victim->max_hit / 4) send_to_char("{RYou sure are BLEEDING!{x\n\r", victim);` | `mud/combat/engine.py:apply_damage` | **Injury-feedback messages ("HURT"/"BLEEDING") missing from Python.** ROM's `damage()` switch fires in the `default:` case whenever the victim's position stays at a normal state (SLEEPING/RESTING/STANDING/FIGHTING — not MORTAL/INCAP/STUNNED/DEAD). Python had no equivalent — a victim who took a big hit or was below 25% HP received no feedback message, even when ROM would have sent one. Two independent conditions: (1) `dam > max_hit / 4` → "That really did HURT!"; (2) `victim->hit < max_hit / 4` → "You sure are BLEEDING!"; both can fire on the same hit. **Fix (2.14.6):** added the two-condition check inside `apply_damage` after `apply_position_change`, guarded by `victim.position > Position.STUNNED` (= the ROM `default:` case condition). Uses `//` (correct for non-negative operands identical to C `/`). Test: `tests/test_combat.py::test_apply_damage_hurt_and_bleeding_messages` (4 cases: HURT-only, BLEEDING-only, both, negative-guard for STUNNED position). Also updated `test_attack_damages_but_not_kill` assertion from `messages[-1]` to `any()` since HURT now appends after the hit-verb. | ✅ FIXED (2.14.6) |
| `FIGHT-061` | MEDIUM | `src/fight.c:3001-3002` `do_flee` calls `move_char(ch, door, FALSE)`; `src/act_move.c:115-193` inside `move_char`: PC-only guard, sector-average cost `(movement_loss[in]+movement_loss[out])/2`, FLYING/HASTE halve it, SLOW doubles it; if `ch->move < move` returns without moving (attempt fails) | `mud/commands/combat.py:do_flee` | **Three divergences in `do_flee` movement deduction.** (1) **Wrong formula:** Python used `max(0, char.move - c_div(char.max_move, 10))` (flat `max_move/10`) instead of the sector-average cost (FIELD→FIELD = 2, not 10). (2) **Missing PC guard:** ROM's cost block is `if (!IS_NPC(ch))` — NPCs don't pay movement on flee; Python deducted from all characters. (3) **Missing exhaustion check:** ROM's `move_char` returns without moving when `ch->move < move`, causing that attempt to fail and the loop to continue; Python deducted unconditionally, so a PC with `move=0` could always flee. **Fix (2.14.8):** added sector-based cost computation inside the 6-attempt loop, after the room is resolved but before the actual room move; added PC guard (`!is_npc`); added `if char.move < cost: continue` that mirrors `move_char`'s exhaustion early-return; added module-level `_FLEE_MOVEMENT_LOSS` dict (matches `src/act_move.c:50-52`); replaced post-loop `max(0, char.move - c_div(char.max_move, 10))` with `char.move -= flee_move_cost`. Tests: `tests/integration/test_fight061_flee_move_cost.py` (3: PC FIELD→FIELD costs 2 not 10; NPC flee doesn't deduct move; PC with move=1 < cost=2 cannot flee → PANIC). All 3 verified red before fix, green after. | ✅ FIXED (2.14.8) |
| `FIGHT-043` | MEDIUM | `src/fight.c:3010-3019` `do_flee` — on successful flee, if `!IS_NPC(ch)`: emits `"You flee from combat!\n\r"`, then if `ch->class == 2` (thief) and `number_percent() < 3 * (ch->level / 2)` emits `"You snuck away safely.\n\r"` (no XP loss), else emits `"You lost 10 exp.\n\r"` and calls `gain_exp(ch, -10)`. | `mud/commands/combat.py:do_flee` | **`do_flee` missing the entire ROM XP-penalty block for PCs.** Python `do_flee` emitted `"You flee from combat!"` on success but contained no XP deduction or thief sneak check whatsoever — a PC always escaped with full XP, a thief never got a sneak bonus. The ROM penalty applies to all non-NPC characters and the thief class (`ch_class == 2`) gets a sneak-away roll (`3 * (level / 2)` threshold) that spares them the 10 XP loss. Surfaced 2026-06-10 by the cross-file invariants probe (do_flee / do_recall stop-fighting pass). **Fix (2.13.87):** added the XP block after `messages.append("You flee from combat!")` using `getattr(char, "is_npc")` guard, `getattr(char, "ch_class", -1) == 2` thief check, `rng_mm.number_percent() < 3 * c_div(char.level, 2)` sneak roll, `gain_exp(char, -10)` deduction. Test: `tests/integration/test_fight043_flee_xp_loss.py` (5: warrior loses 10 XP, "You flee from combat!" emitted, thief sneak success preserves XP, thief sneak fail loses XP, NPC flee unaffected). Both mutation-sensitive tests (`test_non_thief_flee_loses_10_exp` and `test_thief_sneak_no_xp_loss`) verified red before fix, green after. | ✅ FIXED (2.13.87) |
| `FIGHT-062` | MEDIUM | `src/fight.c:3005-3007` `do_flee` — restores `ch->in_room = was_in` and calls `act("$n has fled!", ch, NULL, NULL, TO_ROOM)` | `mud/commands/combat.py:do_flee` | **`do_flee` "$n has fled!" broadcast bypassed the act() system.** The Python iterated `was_in.people` and called `other.desc.send(f"{char.name} has fled!")` directly: (1) baked `char.name` — no `$n` PERS masking, so an invisible fleer leaked their name (an unaided witness should see "Someone has fled!"); (2) `desc.send` skipped descriptor-less occupants — NPC witnesses got no `TRIG_ACT`, and the opponent left behind (no live descriptor in tests / many runtime paths) received nothing, also bypassing the INV-001 single-delivery channel. ROM uses `act(..., TO_ROOM)` which renders `$n` per recipient via `PERS` and dispatches `TRIG_ACT`. Surfaced 2026-06-13 by the post-REPORT-001 hand-rolled-broadcast sweep (same class as REPORT-001). **Fix (2.14.48):** replaced the loop with `act_to_room(was_in, "$n has fled!", char)` (INV-025/027 PERS masking + INV-001 single-delivery + TRIG_ACT). `char` has already moved to `now_in` so it is not in `was_in.people`. Test: `tests/integration/test_fight062_flee_broadcast.py` (descriptor-less witness receives the `$n`-rendered line). | ✅ FIXED (2.14.48) |
| `FIGHT-063` | MINOR | `src/fight.c:2946` `do_backstab` — `act("$N is hurt and suspicious ... you can't sneak up.", ch, NULL, victim, TO_CHAR)` | `mud/commands/combat.py:do_backstab` | **The "hurt and suspicious" rejection baked `victim.name` (the keyword string) lowercase instead of ROM's `$N` PERS render.** ROM `$N` = `PERS(victim, ch)` = the NPC **short_descr** (not the keyword name), and `act()` caps buf[0]. So backstabbing a wounded mob whose name keywords are "goblin sneaky" / short_descr "a sneaky goblin" showed "goblin sneaky is hurt and suspicious …" where ROM shows "A sneaky goblin is hurt and suspicious …" (short_descr + capitalized; an invisible victim would mask to "Someone"). Found applying the `$N`/PERS/ACT-CAP lens to fight.c. **Fix (2.14.57):** `act_format("$N is hurt and suspicious ... you can't sneak up.", recipient=char, actor=char, arg2=victim)`. Test: `tests/test_skill_combat_rom_parity.py::TestBackstabRomParity::test_backstab063_hurt_message_uses_pers_shortdescr_capitalized`. | ✅ FIXED (2.14.57) |
| `FIGHT-064` | MINOR | `src/fight.c:1061` `act("But $N looks so cute and cuddly...", …)` + `:2707/:2805/:2873` `act("$N is your beloved master.", ch, NULL, victim, TO_CHAR)` | `mud/commands/combat.py:_kill_safety_message` + `mud/commands/murder.py:_murder_safety_check` | **The kill/murder safety messages "$N is your beloved master." and "But $N looks so cute and cuddly..." baked `victim.name` (the keyword string) instead of ROM's `$N` PERS render.** ROM `$N` = `PERS(victim, ch)` = the NPC **short_descr** (capitalized buf[0] for the beloved-master line, which starts with `$N`). So a charmed PC killing an NPC master "wizard"/short_descr "a dark wizard" saw "wizard is your beloved master." vs ROM "A dark wizard is your beloved master." Two near-identical copies of the safety logic (do_kill in combat.py, do_murder in murder.py) both had the bug — 4 sites total. (PC masters render the same since PERS = name; literal "don't own that monster"/"leave them alone"/"Pick on someone" lines are ROM `send_to_char`, faithful.) Found applying the `$N`/PERS/ACT-CAP lens to the is_safe family. **Fix (2.14.58):** all four via `act_format("$N …", recipient=char, actor=char, arg2=victim)`. Test: `tests/test_combat.py::test_fight064_beloved_master_message_uses_pers_shortdescr_capitalized` (NPC master → "A dark wizard …"); existing PC-master / `.lower()` pet tests stay green. | ✅ FIXED (2.14.58) |
| `FIGHT-065` | MINOR | `src/fight.c:3175` `do_disarm` — `send_to_char("Your opponent is not wielding a weapon.\n\r", ch)` (LITERAL, not `act("$N …")`) | `mud/skills/handlers.py:disarm` (~3349) | **The `disarm` skill handler baked the victim keyword into the "no weapon" message instead of ROM's literal.** ROM `do_disarm` emits a fixed `send_to_char("Your opponent is not wielding a weapon.\n\r", ch)` when the victim has no wielded weapon — there is no `$N`/PERS render at all. The `handlers.py:disarm` path (the duplicate skill-handler that `mob_hit`/`_mob_offensive_skill` dispatches, distinct from the already-correct player command `combat.py:do_disarm` at :1218) used `f"{_character_name(victim)} is not wielding a weapon."`, so an NPC-vs-NPC disarm against an unarmed "goblin" showed "goblin is not wielding a weapon." vs ROM "Your opponent is not wielding a weapon." Listed in the MAGIC-022 baked-name batch but verified as a **literal** fix, not a `$N` conversion. Found applying the act()-rendering lens; ROM source check showed `send_to_char`, not `act`. **Fix (2.14.70):** literal restored. Test: `tests/integration/test_fight065_disarm_no_weapon_literal.py`. | ✅ FIXED (2.14.70) |
| `TRIP-001` | MINOR | `src/fight.c` `do_trip` — `act("$S feet aren't on the ground.", …)`, `act("$N is already down.", …)`, `act("$N is your beloved master.", …)` (all TO_CHAR) | `mud/skills/handlers.py:trip` | **Three `do_trip` blocking messages baked literal "Their"/"They" or the keyword name instead of ROM's act() pronouns/PERS.** ROM renders `$S` = the victim's possessive (his/her/its — "His feet aren't on the ground.", capitalized buf[0]), `$N` = `PERS(victim)` = NPC short_descr (capitalized: "A sneaky goblin is already down."), and "$N is your beloved master." The Python `trip` handler used `"Their feet aren't on the ground."`, `f"{_character_name(victim)} is already down."` (keyword name), and `"They are your beloved master."` (note also "are" vs ROM "is"). Same class as TELL-008/GIVE-002/FIGHT-064; the handler already used `act_format` for its other lines (disarm/dirt-kick), so this was an inconsistency. Found applying the `act()`-rendering lens to fight.c skill commands. **Fix (2.14.59):** all three via `act_format("$S …"/"$N …", recipient=caster, actor=caster, arg2=victim)`. Test: `tests/test_skills_combat.py::test_trip001_messages_use_rom_pronoun_and_pers` (male flyer → "His feet …"; already-down NPC → "A sneaky goblin is already down."). | ✅ FIXED (2.14.59) |

| `FIGHT-066` | MINOR | ✅ FIXED (2.14.90) | `src/fight.c:1316-1370` `check_parry`/`check_dodge`/`check_shield_block` each emit TWO act() lines — TO_VICT "You parry/dodge/block $n's attack." + TO_CHAR "$N parries/dodges/blocks your attack." | `mud/combat/engine.py:attack_round` (~705-709) returns `f"{victim.name} parries your attack."` / `f"{victim.name} dodges your attack."` / `f"{victim.name} blocks your attack with a shield."` | **`attack_round`'s defense-return string bakes `victim.name` where ROM uses `$N` = PERS(victim) capitalized.** The `check_parry`/`check_dodge`/`check_shield_block` functions ALREADY deliver both ROM lines correctly to the player via `_push_message` with `pers()` + `capitalize_act_line` (engine.py:1641/1643, 1677/1679, 1597/1599 — FIGHT-031/032). So the baked `attack_round` return at ~705-709 is a SECONDARY string that disagrees with the correctly-pushed line ("goblin …" vs "A green goblin …"). **Severity confirmed LATENT (2026-06-13 trace):** every `multi_hit` caller discards the returned `results` list (`game_loop.py:1659`, `mob_cmds.py:1057/1076`, `spec_funs.py:908`, `assist.py:72/85/159`, `aggressive.py:119`, `murder.py:100`), and the player-facing defense tests (`test_combat_defenses_prob.py`, `test_combat.py:446`) read the **pushed** line from `char.messages` (via `deliver_kill`/`process_command`), NOT the return — so this baked string is **never delivered to a player and not asserted by any test**. It is NOT a double-delivery (the correct pushed line is single-delivered by check_parry/dodge/shield_block). Risk is purely latent: a future refactor that starts delivering `attack_round`'s return would reintroduce the baked name. Surfaced by extending the act()-lens from spell handlers (MAGIC-025..044, exhausted) to `mud/combat/`. **Fix (2.14.90):** the three returns now use `capitalize_act_line(f"{pers(victim, attacker)} parries/dodges/blocks …")`, matching the already-pushed lines. As predicted, no existing test needed re-baselining — `test_combat_defenses_prob.py` reads the pushed line and the PC-name return tests render identically for a visible PC. Test: `tests/integration/test_fight066_defense_return_pers.py` (NPC victim → "A green goblin parries your attack." not "goblin …"). | ✅ FIXED (2.14.90) |
| `FIGHT-067` | MEDIUM | `src/fight.c:2405-2419` `do_bash` entry gates — `if (is_safe(ch,victim)) return;` (2405); `if (IS_NPC(victim) && victim->fighting != NULL && !is_same_group(ch, victim->fighting)) "Kill stealing is not permitted." return;` (2408-2413); `if (IS_AFFECTED(ch,AFF_CHARM) && ch->master==victim) act("But $N is your friend!", ...) return;` (2415-2419) | `mud/commands/combat.py:do_bash` (~`:440-443`) | **`do_bash` missing the entire ROM entry-level offensive-target gate block.** ROM gates the bash on `is_safe`, kill-steal, and charm-friend at command entry — *before* computing chance, applying WAIT_STATE, dazing the victim, or knocking them to RESTING. Python `do_bash` had none of these; it relied solely on `apply_damage` re-checking `is_safe` (FIGHT-002), which is **downstream and silent** — it suppresses only the HP damage. **Confirmed observable:** in a ROOM_SAFE room, forcing the success roll, Python still applied 24-pulse WAIT_STATE to the attacker, set victim `daze`, knocked the victim to RESTING, and broadcast "sends you sprawling" — a real griefing divergence (a player could lag + floor another character in a safe room, town square, etc.). **Fix (2.14.100):** inserted the ROM gate block after the position check / before the wait check in `do_bash`: `is_safe(char, victim)` → `return ""` (silent, consistent with FIGHT-002's downstream re-check); kill-steal → "Kill stealing is not permitted."; charm-friend → `act_format("But $N is your friend!", recipient=char, actor=char, arg2=victim)` (`$N` PERS, FIGHT-064 precedent). Test: `tests/integration/test_fight067_bash_safe_room_entry_gate.py` (ROOM_SAFE + forced success → no attacker lag, no victim daze, no knockdown). Verified red (`assert 24 == 0`) before fix, green after. | ✅ FIXED (2.14.100) |

### Cold-path divergence hunt — OPEN (filed 2026-07-03, not yet closed)

Surfaced by a parallel cold-path combat/update divergence hunt (same session
that closed GET-015 / PICK-003 / GL-045). **`FIGHT-081` is independently
verified against ROM C by the filer; `FIGHT-082..087` are hunter-reported with
exact cites and MUST be re-verified against ROM C before closing** (per AGENTS.md
"re-verify any status claim against ROM C source"). Prioritize FIGHT-081 (HIGH,
affects every swing) and FIGHT-082 (HIGH).

- **`FIGHT-081` — ✅ FIXED (2.14.221) — AC modifiers applied at wrong
  scale/order in `attack_round`.** `mud/combat/engine.py:552-596` vs ROM `one_hit`
  `src/fight.c:480-503`. ROM divides AC by 10 **first** (`victim_ac = GET_AC(victim,
  AC_x) / 10`), then applies the `< -15` rescale and the `-4/+4/+6` (invis /
  pos<FIGHTING / pos<RESTING) modifiers **on the /10 scale**. Python applied the
  modifiers and the rescale to the **raw** AC and divided by 10 last (`vac =
  c_div(victim_ac, 10)`, old line 596). Two effects: (a) the -4/+4/+6 position/invis
  modifiers became ~0.4/0.4/0.6 AC after the trailing /10 (≈10× too weak); (b) the
  `< -15` rescale fired for any raw AC < -15 (almost every armored character) instead
  of ROM's post-/10 < -15, drastically changing effective AC. Worked example (raw
  AC_BASH = -100): ROM effective victim_ac = -10; old Python = -3 → victim ~7 AC points
  easier to hit. Pervasive (shared PC+NPC melee, every swing); didn't surface in
  `combat_melee_rounds` because that mob's AC sits in the non-divergent band.
  Sub-divergence (also fixed): Python gated the -4 on `AffectFlag.INVISIBLE`; ROM
  uses `!can_see(ch, victim)` (`fight.c:496`) — broader (blind/dark/hide, withheld
  from a detect-invis attacker). **Fix (2.14.221):** extracted `_compute_victim_ac`
  mirroring ROM `src/fight.c:480-503` — `c_div(get_ac, 10)` first, then the `< -15`
  rescale, then `not can_see_character(attacker, victim)` -4 / position +4/+6, all
  on the /10 scale; the hit test now uses `victim_ac` directly (dropped the trailing
  `c_div(victim_ac, 10)`). Test: `tests/integration/test_fight081_ac_scale_order.py`
  (3 — -100 AC scale, -200 AC rescale band, invis -4 gated on can_see not the affect).
  As predicted, no existing combat test needed re-baselining (3387 pass). HIGH.
- **`FIGHT-082` — ✅ FIXED (2.14.222) — `do_trip` cluster.**
  `mud/commands/combat.py:1234-1283` vs `src/fight.c:2711-2753`. All four
  hunter-reported sub-claims re-verified against ROM C and confirmed: (a) damage
  upper bound had a spurious `skill_level // 20` term (ROM `number_range(2, 2 +
  2*victim->size)`, `:2744`); (b) missing OFF_FAST/AFF_HASTE speed modifier
  (`chance += 10` self / `-= 20` victim, `:2722-2726`); (c) wrong wait/daze values
  — success attacker was `PULSE_VIOLENCE`(12) not `beats`(24), failure was
  `PULSE_VIOLENCE` not `beats*2/3`(16), self-trip was `2*PULSE_VIOLENCE`(24) not
  `2*beats`(48), and the victim got **WAIT** where ROM `DAZE_STATE`s it
  (`2*PULSE_VIOLENCE`, `:2741`). **Fix (2.14.222):** read `trip_beats` from
  `skill_registry.get("trip").beats` (raw, no haste/slow — CAST-010 precedent);
  added the ROM speed-modifier block; success now `DAZE`s the victim and WAITs the
  attacker for raw `beats`, failure WAITs `beats*2//3`, self-trip WAITs `2*beats`;
  dropped the `skill_level // 20` damage term. Test:
  `tests/integration/test_fight082_do_trip_cluster.py` (5 — raw beats + DAZE,
  failure 2/3-beats, self-trip 2×beats, damage bound has no skill term, haste +10
  chance boundary). Note: a tripped normal victim ends at `POS_FIGHTING`, not
  `RESTING` — ROM `damage()` (`:743-744`) re-seats a `timer<=4` victim after
  trip's `POS_RESTING`. HIGH.
- **`FIGHT-083` — ✅ FIXED (2.14.224) — `dirt_kicking` missing the
  `if (chance % 5 == 0) chance += 1` anti-false-zero hack (`src/fight.c:2566-2568`)
  and using `if chance <= 0` where ROM's post-terrain `if (chance == 0)` (`:2604`)
  uniquely means water/air.** `mud/skills/handlers.py:dirt_kicking`. Both re-verified
  against ROM C. Caused a wrong "There isn't any dirt to kick." on dry land for
  weak/low-dex kickers + skipped wait/miss. **Fix (2.14.224):** inserted the
  `c_mod(chance, 5) == 0 → chance += 1` hack after the level modifier / before the
  terrain switch (c_mod: chance can be negative from dex/level mods; the `== 0` test
  is sign-invariant), and changed the post-terrain gate from `chance <= 0` to
  `chance == 0` so a negative dry-land chance proceeds to the guaranteed-miss
  WAIT_STATE as in ROM. Test: `tests/integration/test_fight083_dirt_kick_false_zero.py`
  (3 — false-zero at skill 20 INSIDE, negative-chance dry land, water still "no dirt").
- **`FIGHT-084` — ✅ FIXED (2.14.225) — `check_parry` visibility
  operands inverted.** `mud/combat/engine.py:check_parry` used `not victim.can_see(attacker)`;
  ROM `src/fight.c:1311` is `!can_see(ch, victim)` (attacker→victim). `can_see`
  is not symmetric. Re-verified against ROM. Also found the `getattr(victim,
  "can_see", lambda x: True)` fallback made the halving **inert** (no `can_see`
  method → always True → never halved). **Fix (2.14.225):** `not
  can_see_character(attacker, victim)` — correct direction and functional. Test:
  `tests/integration/test_fight084_parry_visibility_direction.py` (3 — halved when
  attacker blind to victim, not halved when attacker sees, direction-lock via an
  invisible attacker who still sees a visible victim). The identical inert-lambda in
  `check_dodge` (`:1650`, direction already correct per ROM `:1363`) filed as FIGHT-089.
- **`FIGHT-089` — ✅ FIXED (2.14.228) — `check_dodge` visibility halving was inert.**
  `mud/combat/engine.py:check_dodge` used `not getattr(victim, "can_see",
  lambda x: True)(attacker)`. The direction is correct (ROM `src/fight.c:1363`
  `!can_see(victim, ch)` = victim→attacker), but the `lambda x: True` fallback (there
  is no `can_see` method on the runtime entities) meant the `chance /= 2` **never
  fired** — a defender who couldn't see the attacker dodged at full chance instead of
  half. Surfaced 2026-07-03 while closing FIGHT-084 (same dead-lambda class).
  **Fix (2.14.228):** `not can_see_character(victim, attacker)` — functional and
  correct direction. Test: `tests/integration/test_fight089_dodge_visibility_direction.py`
  (3 — halved when victim blind to attacker; not halved when victim sees; direction-lock
  via an invisible victim who still sees a visible attacker).
- **`FIGHT-085` — ✅ FIXED (2.14.226) — skill wait-states were
  haste/slow-adjusted; ROM `WAIT_STATE` uses raw beats.** `mud/skills/registry.py`
  (`_compute_skill_lag`) halved lag under AFF_HASTE / doubled under AFF_SLOW;
  ROM's `WAIT_STATE` macro (`src/merc.h:2116`) is a bare `UMAX` applying raw
  `skill_table[sn].beats` — haste/slow only change the number of attacks
  (`multi_hit`), never a skill's lag. Re-verified against ROM. **Fix (2.14.226):**
  deleted the HASTE/SLOW scaling branches; `_compute_skill_lag` now returns
  `max(1, base_lag)`. Removed the now-dead `c_div` / `AffectFlag` imports. Test:
  `tests/integration/test_fight085_skill_wait_raw_beats.py` (3 — baseline/haste/slow
  all == raw beats); the pre-existing `test_skills.py::test_skill_wait_adjusts_for_haste_and_slow`
  asserted the wrong scaling and was rewritten as
  `test_skill_wait_is_raw_beats_regardless_of_haste_slow`. Affected
  do_bash/do_kick/do_backstab/do_rescue and every registry `use()` skill.
- **`FIGHT-086` — ✅ FIXED (2.14.227) — backstab THAC0 bonus
  restored.** ROM `one_hit` `src/fight.c:474-475`: `if (dt == gsn_backstab) thac0 -=
  10 * (100 - get_skill(ch, gsn_backstab));` (near-auto-hit below skill 100).
  Python `compute_thac0` is a pure helper with no `dt`, so the branch belongs in
  `attack_round` right after THAC0 is interpolated/skill-adjusted. **Fix (2.14.227):**
  added `if _normalize_dt(dt) == "backstab": th -= 10 * (100 - _backstab_skill(attacker))`
  in `mud/combat/engine.py:attack_round`, plus a `_backstab_skill` helper mirroring
  ROM `get_skill` (`src/handler.c:346`) for the backstab sn — PC learned percent,
  NPC ACT_THIEF `20 + 2*level`, other NPC `0`. Plain `*`/`-` (no division → no
  c_div). Test: `tests/integration/test_fight086_backstab_thac0.py` (2 — backstab
  hits where a normal attack misses at the same diceroll; `_backstab_skill` matches
  ROM get_skill for PC/NPC-thief/NPC-plain). The systemic daze/drunk get_skill
  modifiers (`src/handler.c:434-442`) are still unported — filed as **HANDLER-008**.
- **`FIGHT-087` — ✅ FIXED (2.14.229) — `disarm` floored unarmed hand-to-hand to
  1 and used a non-ROM gate.** `mud/skills/handlers.py:disarm` fetched hand-to-hand
  via `_skill_percent` (skills-dict only → 0 for NPCs) and papered over it with
  `max(hand_to_hand, 1)`, so an unarmed NPC disarmer computed `chance * 1 / 150`
  instead of ROM's `chance * (40+2*level) / 150` (`src/fight.c:3188`) — a
  near-guaranteed failure. The unarmed gate also used `hth<=0 AND !OFF_DISARM`
  where ROM (`src/fight.c:3160-3164`) is `hth==0 OR (IS_NPC AND !OFF_DISARM)`.
  **Fix (2.14.229):** added `_hand_to_hand_skill` (PC learned / NPC `40+2*level`,
  a partial get_skill mirror like `engine._backstab_skill`), dropped the floor
  (raw `hth`; the gate guarantees `hth != 0` on the unarmed path), and aligned the
  gate boolean to ROM. Test: `tests/integration/test_fight087_disarm_unarmed_hth.py`
  (2 — unarmed NPC disarm succeeds at a roll where the floor path fails;
  `_hand_to_hand_skill` matches ROM get_skill for PC/NPC). Note: the sibling
  disarm-**skill** NPC lookup (`skill = _skill_percent(caster, "disarm")`,
  handlers.py) is another get_skill NPC-formula gap on the same function — part of
  **HANDLER-008**'s surface, not fixed here (an NPC with OFF_DISARM should get
  `20+3*level`).
- **`FIGHT-088` — ✅ FIXED (2.14.230) — `do_trip` used plain return strings and
  omitted the failure `damage(0)` call.** Surfaced 2026-07-03 while closing
  FIGHT-082 (full `do_trip` read). (a) ROM success emits three `act()` lines —
  `"$n trips you and you go down!"` TO_VICT, `"You trip $N and $N goes down!"`
  TO_CHAR, `"$n trips $N, sending $M to the ground."` TO_NOTVICT (`src/fight.c:2735-2738`);
  Python `mud/commands/combat.py` returned a single baked
  `f"You trip {victim.name} and they go down!"` string with no room broadcast
  and no `$N`/`$M` PERS rendering (act()-render class, sibling of FIGHT-073). (b)
  ROM failure calls `damage(ch, victim, 0, gsn_trip, DAM_BASH, TRUE)` (`:2749`)
  before the wait — delivering the standard miss combat message and starting the
  fight — whereas Python's failure branch only set the wait and returned
  `"You try to trip them but miss."` (no `damage(0)` call, so no miss dam_message,
  no fight-start on a cold trip). **Fix (2.14.230):** success now pushes TO_VICT via
  `send_to_char_buffered`, broadcasts TO_NOTVICT via `act_to_room` (`$M` → victim's
  objective pronoun), and returns the TO_CHAR `act_format` line; the success damage
  now passes `dt="trip"`. Failure now calls `apply_damage(char, victim, 0,
  DamageType.BASH, dt="trip")` before the `beats*2//3` wait and returns its
  (unpushed, single-delivery) TO_CHAR miss line. Test:
  `tests/integration/test_fight088_do_trip_act_render.py` (2 — cold miss starts the
  fight via `victim.fighting is char`; success broadcasts the `$M`-gendered
  TO_NOTVICT to a bystander). Filed **FIGHT-090** (divergent duplicate trip impls).

- **`FIGHT-090` — ✅ FIXED (2.14.240) — `do_trip` and `skill_handlers.trip` were
  divergent duplicate implementations of ROM `do_trip`.** `mud/commands/combat.py:do_trip`
  is now the single canonical implementation; `mud/skills/handlers.py:trip` is a thin
  delegate (`do_trip(caster, "", victim=target)`). Unifying surfaced **three ROM-parity
  fixes that had landed only on the `skill_handlers.trip` copy** and were missing from
  do_trip — all merged in: (1) the self-trip `{5`colour + `act("$n trips over $s own
  feet!")` room broadcast (FIGHT-039); (2) PERS gate messages — flying `act("$S feet
  aren't on the ground.")` and position `act("$N is already down.")` (TRIP-001), which
  do_trip had baked as "Their"/"They"; (3) `check_improve(ch, gsn_trip, TRUE/FALSE, 1)`
  on success/failure, which do_trip omitted entirely (trip never improved via the
  command). Delivery model unified to ROM's: do_trip returns the TO_CHAR line and pushes
  TO_VICT / broadcasts TO_NOTVICT. Retired 5 redundant `TestTripRomParity::test_trip_handler_*`
  tests (behaviours covered by `test_fight082_do_trip_cluster` + `test_fight071` + the new
  improve coverage); updated the 5 delivery-model tests. Also removed the old handler's
  **non-ROM** post-damage RESTING re-set (ROM sets RESTING before `damage()` only) and its
  self-before-skill-check ordering (ROM gates on the trip skill first). Follow-up: do_trip's
  NPC trip chance hardcodes `skill_level = 100` (stale "mirror do_kick" comment — do_kick
  now uses `get_skill`); it should use `get_skill` `10+3*level` — filed under **HANDLER-008**.

- ~~**`FIGHT-090` (original filing)**~~ Superseded above. The `"trip"` command routes to `mud/commands/combat.py:do_trip`;
  the `"trip"` *skill* (`data/skills.json` `"function": "trip"`) routes to
  `mud/skills/handlers.py:trip` (~`:7999`) via the registry — both are live, each
  with its own gate ordering, chance math, and message wording. `skill_handlers.trip`
  has the failure `damage(0)` (`:8114`) but still bakes the success act lines
  (`:8086-8091`, `"sending them to the ground"`, no `$M`); `do_trip` (now FIGHT-088)
  has the correct act render. They should be unified to one implementation (the
  command delegating to the handler, or vice versa) so a future ROM-parity fix
  can't land on one path and miss the other. Not fixed here — this is a refactor,
  not a gap-closer.

- **`FIGHT-091` — ✅ FIXED (2.14.231) — an NPC's kick chance was 0 (skills dict),
  so NPC kicks never landed.** ROM `do_kick` (`src/fight.c:3125`) rolls
  `get_skill(ch, gsn_kick) > number_percent()`; for an NPC `get_skill` returns
  `10 + 3*level` (`src/handler.c:410`). Python `do_kick` read the kick percent from
  the skills dict — empty for mobs — so `chance == 0` and an aggressive OFF_KICK mob
  could never land a kick. **Fix (2.14.231):** NPC branch now computes
  `max(0, min(100, 10 + 3*level))`; PC branch unchanged (learned percent). Test:
  `tests/integration/test_fight091_npc_kick_skill.py` (NPC kick lands at a roll where
  the 0-chance path always missed). The do_kick site of the **HANDLER-008** get_skill
  NPC-formula class (4th after backstab/hand-to-hand/disarm-skill); the daze/drunk
  modifiers remain deferred to the unified get_skill port.

- **`FIGHT-092` — ✅ FIXED (2.14.256) — an invisible attacker was never revealed
  by attacking.** ROM `damage()` (`src/fight.c:763-769`, "Inviso attacks ... not.")
  strips `gsn_invis`/`gsn_mass_invis` and `AFF_INVISIBLE` from the **attacker** and
  broadcasts `act("$n fades into existence.", ch, NULL, NULL, TO_ROOM)` on every
  attack that reaches `damage()` — so an invisible caster/thief becomes visible the
  instant it lands a hit. The port's `mud/combat/engine.py:apply_damage` had **no
  equivalent**, so an invisible attacker stayed hidden through an entire fight,
  unseeable/untargetable by players lacking DETECT_INVIS. **Fix (2.14.256):** added
  the strip after the parry/dodge/shield-block early-returns (mirroring ROM, where
  those resolve in `one_hit` before `damage()` is entered) and before the RIV/immune
  modifiers (so it fires even on a 0-damage immune hit): `remove_spell_effect("invis")`
  + `remove_spell_effect("mass invis")` + clear the `AFF_INVISIBLE` bit, then
  `_broadcast_pos_change(attacker, "{name} fades into existence.")` (PERS per-listener,
  actor-excluded, first-letter cap). Surfaced 2026-07-04 by a `damage()` side-effect
  probe in the autonomous loop. Test: `tests/integration/test_invisibility_combat.py::test_invisible_attacker_revealed_on_connecting_hit`.
- **`FIGHT-093` — ✅ FIXED (2.14.269) — the `dam > 1200 && dt >= TYPE_HIT` loophole
  cap + weapon-extract cheat penalty were absent from `apply_damage`.** ROM `damage()`
  (`src/fight.c:697-713`, "Stop up any residual loopholes.") clamps any physical hit
  (`dt >= TYPE_HIT`) whose raw damage exceeds 1200 back to 1200 — *before* the >35/>80
  reduction — and, if the attacker is not immortal, sends "You really shouldn't cheat."
  and `extract_obj`s the attacker's wielded weapon (`get_eq_char(ch, WEAR_WIELD)`).
  Python `mud/combat/engine.py:apply_damage` had no equivalent, so an out-of-range
  damage input passed straight into the reduction with no cap and no penalty. **Fix
  (2.14.269):** added the cap block immediately after the `POS_DEAD` guard, gated by
  the existing `_should_check_weapon_defenses(dt)` helper (which is exactly ROM's
  `dt >= TYPE_HIT`, `fight.c:793`/`:700`); non-immortal attackers get the cheat message
  via `_push_message` and lose their weapon via `mud.game_loop._extract_obj` (the ROM
  `extract_obj` mirror). Test: `tests/integration/test_fight093_damage_1200_loophole.py`
  (2 — non-immortal physical >1200 → cap + weapon extract + cheat message; spell dt
  below TYPE_HIT → no penalty). Verified red before fix, green after. **Remaining →
  FIGHT-094 (OPEN):** the `check_killer(ch, victim)` call at `fight.c:733` is still
  absent from `apply_damage` — Python calls `check_killer` from the command layer
  (do_kill/murder/backstab/…) instead of inside `damage()`, so any damage path not
  routed through one of those verbs (mob one_hit, some spells) misses PK-flagging.
  This is a broader PK-subsystem placement question (risk of double-flagging on the
  command paths that already call it); filed for separate assessment.

### Follow-ups (FIGHT-022 / FIGHT-023 / FIGHT-025 / FIGHT-026 — filed, not yet closed)

- **`FIGHT-068` — `do_bash` checked `victim == ch` before `position < POS_FIGHTING` (order swap vs ROM), ✅ FIXED (2.14.105).** Surfaced 2026-06-14 while closing FIGHT-067 (full `do_bash` read, `src/fight.c:2392-2403`). ROM checks `position < POS_FIGHTING` ("You'll have to let $M get back up first.", `:2392-2397`) **before** `victim == ch` ("You try to bash your brains out, but fail.", `:2399-2403`). Python `mud/commands/combat.py:do_bash` checked `victim is char` **before** the position check. Observable only in the edge case of bashing *yourself* while you are sitting/sleeping (`position < FIGHTING`): ROM emits the position line, Python emitted "You try to bash your brains out, but fail." **Fix (2.14.105):** swapped the two early-return blocks so the position check precedes the self-target check. Test: `tests/integration/test_fight068_bash_position_before_self.py` (self-bash while POS_SITTING → position gate wins). Verified red before fix, green after. Residual: the position message is the literal `"You'll have to let them get back up first."`, where ROM renders `act("...$M...")` (victim objective pronoun) — filed as **FIGHT-075** below (act()-render class, sibling of FIGHT-073).

- **`FIGHT-069` — `do_trip` / `do_dirt` missing the kill-steal gate, ✅ FIXED (2.14.101).** Surfaced 2026-06-14 while closing FIGHT-067; verified by reading ROM `do_dirt` (`src/fight.c:2489-2639`) and `do_trip` (`:2641-2754`) top-to-bottom. Both ROM functions have a kill-steal gate (`IS_NPC(victim) && victim->fighting != NULL && !is_same_group(ch, victim->fighting)` → "Kill stealing is not permitted.", `do_dirt:2537-2542` / `do_trip:2678-2683`) that lives **outside** `is_safe`. Python `do_dirt`/`do_trip` ported the safety gate as a single call to `_kill_safety_message` — which is `do_kill`'s is_safe-messages + beloved-master composite and does **not** contain the kill-steal block (`do_kill` re-adds it separately at `combat.py:141`). So `do_dirt`/`do_trip` let a player dirt-kick/trip an NPC that an ungrouped third party was already fighting — a real kill-steal divergence ROM forbids. **Fix (2.14.101):** added the explicit kill-steal block to both `do_dirt` and `do_trip` immediately after the `_kill_safety_message` call (matching ROM's kill-steal-after-is_safe ordering: in `do_dirt` after the dirt-specific gates, in `do_trip` before the flying/position/self checks), mirroring `do_kill:141` and the FIGHT-067 `do_bash` block. Test: `tests/integration/test_fight069_dirt_trip_kill_steal_gate.py` (2 — NPC victim already fighting an ungrouped rival → both reject). Verified red (both proceeded) before fix, green after. Residual `do_dirt`/`do_trip` gate divergences (charm-message text + ordering, `is_safe` message surfacing) tracked as FIGHT-071 and FIGHT-070 below.

- **`FIGHT-071` — `do_dirt`/`do_trip` charm gate uses do_kill's message + wrong order (borrowed via `_kill_safety_message`), ✅ FIXED (2.14.102).** Surfaced 2026-06-14 while closing FIGHT-069. Because `do_dirt`/`do_trip` routed their charm-master check through `_kill_safety_message` (do_kill's composite), two divergences existed: (1) **wrong message** — ROM `do_dirt` charm is `act("But $N is such a good friend!", …)` (`src/fight.c:2546`) and ROM `do_trip` charm is `act("$N is your beloved master.", …)` (`:2707`); `_kill_safety_message` always returned the beloved-master line (`combat.py:67`), so a charmed PC dirt-kicking its master saw "… is your beloved master." instead of ROM's "But … is such a good friend!". (2) **wrong order** — `_kill_safety_message` checked charm (`:64`) *before* its is_safe body, and in `do_trip` the whole call ran before the flying/position/`victim==ch` checks, but ROM `do_trip` checks charm **last** (`:2705`, after flying/position/self). **Fix (2.14.102):** added an `include_charm: bool = True` parameter to `_kill_safety_message` (do_kill keeps the default → byte-identical); `do_dirt`/`do_trip` now call it with `include_charm=False` and carry their own correctly-worded, correctly-ordered charm gate — `do_dirt` after the kill-steal block (`"But $N is such a good friend!"`, `src/fight.c:2544-2548`), `do_trip` after the `victim==ch` check (`"$N is your beloved master."`, `:2705-2709`) so the flying/position/self lines fire first. Mirrors the FIGHT-067 `do_bash` per-command-gate pattern. Test: `tests/integration/test_fight071_dirt_trip_charm_gate.py` (3 — do_dirt good-friend message; do_trip flying-before-charm order; do_trip beloved-master message). Verified red (do_dirt/do_trip both returned "beloved master" / charm-before-flying) before fix, green after.

- **`FIGHT-072` — `do_dirt` checked `victim == ch` before `AFF_BLIND` (order swap vs ROM), ✅ FIXED (2.14.106).** Surfaced 2026-06-14 while closing FIGHT-071 (full `do_dirt` read, `src/fight.c:2522-2532`). ROM checks `IS_AFFECTED(victim, AFF_BLIND)` ("$E's already been blinded.", `:2522-2526`) **before** `victim == ch` ("Very funny.", `:2528-2532`). Python `mud/commands/combat.py:do_dirt` checked `victim is char` ("Very funny.") **before** the BLIND check. Observable only when dirt-kicking *yourself* while already blind: ROM emits the blind line, Python emitted "Very funny." **Fix (2.14.106):** swapped the two early-return blocks so the BLIND check precedes the self-target check. Test: `tests/integration/test_fight072_dirt_blind_before_self.py` (self dirt-kick while AFF_BLIND → blind gate wins). Verified red before fix, green after. Sibling of FIGHT-068. (The blind message's literal "They're" vs ROM's `$E` render is FIGHT-073 below.)

- **`FIGHT-073` — `do_dirt` BLIND message used literal "They're" instead of ROM's `$E` pronoun + wording, ✅ FIXED (2.14.107).** Surfaced 2026-06-14 while closing FIGHT-071. ROM `do_dirt` emits `act("$E's already been blinded.", ch, NULL, victim, TO_CHAR)` (`src/fight.c:2524`) — `$E` = subjective pronoun of victim (He/She/It, capitalized buf[0] → "He's already been blinded." for a male NPC, PERS/can_see aware). Python `mud/commands/combat.py:do_dirt` returned the literal `"They're already blinded."` — no `$E` rendering, wrong wording. **Fix (2.14.107):** `act_format("$E's already been blinded.", recipient=char, actor=char, arg2=victim)` (act_format caps the first letter via INV-029). Test: `tests/integration/test_fight073_dirt_blind_pronoun_message.py` (blind male victim → "He's already been blinded."). Verified red before fix, green after. Same act()-rendering class as TRIP-001/FIGHT-064.

- **`FIGHT-077` — `is_safe` carried a fabricated NPC-attacker level gate, silently stopping aggressive mobs from attacking low-level players, ✅ FIXED (2.14.115).** Surfaced 2026-06-15 while debugging a user report that aggressive monsters were not proactively attacking. `mud/combat/safety.py:is_safe`, in the NPC-attacking-PC branch, had `char_level = ...; victim_level = ...; if victim_level < char_level - 10: return True`. ROM `is_safe` "killing players" / `IS_NPC(ch)` branch (`src/fight.c:1075-1093`) contains **only** a safe-room check and a charmed-pet-owner check — there is **no level-difference gate anywhere in is_safe for an NPC attacking a PC**. Effect: any mob more than 10 levels above a player was "safe", so `apply_damage` (which re-checks `is_safe` at entry, `src/fight.c:731`) returned `""` with no damage and no fighting state — aggression, mob-initiated melee, and (via the same gate) any NPC offensive action against a much-lower-level PC silently no-op'd. Reproduced live: Eddol (level 1) vs the Cave Dweller (level 13) in room 2316 → `is_safe(mob, Eddol)` returned `True`, mob never aggressed; a level-5 test PC in the same room WAS attacked (5 ≥ 13−10) which is why the bug hid from mid-level test characters. This is the missed level-gate facet of **INV-050** (silent `is_safe` over-block — its note listed `is_ghost`/`GAIN` over-blocks but missed this one); the faithful message-returning mirror `_kill_safety_message` (`mud/commands/combat.py`) already correctly omits any level gate. **Fix (2.14.115):** deleted the four-line gate; `is_safe`'s NPC-attacker branch now mirrors ROM exactly (charmed-pet-owner guard only). Test: `tests/integration/test_fight077_is_safe_no_npc_level_gate.py` (level-13 mob vs level-1 PC not safe; level-50 vs level-1 not safe). Verified red before fix, green after. CRITICAL (player-facing: aggression dead for all low-level PCs).

- **`FIGHT-078` — `make_corpse` drops phantom silver into an NPC corpse when the mob carried silver but no gold, ✅ FIXED (2.14.203).** Surfaced 2026-06-20 by the new `death_corpse_loot_sacrifice` differential scenario (`tools/diff_harness/FINDINGS.md` FINDING-038). ROM `make_corpse` (`src/fight.c:1473`) gates the NPC corpse's money object on **`ch->gold > 0`** alone — `if (ch->gold > 0) { obj_to_obj(create_money(ch->gold, ch->silver), corpse); ch->gold = 0; ch->silver = 0; }`. A mob carrying silver but **zero gold** therefore creates **no** money object; the silver is simply lost when the mob is extracted. Python `mud/combat/death.py:make_corpse` used a single unified gate `if gold > 0 or silver > 0:` for **both** NPC and PC corpses, so a silver-only NPC corpse contained a phantom `"N silver coins"` object that ROM never creates (observable via `look in corpse` / `get all corpse`). **Fix (2.14.203):** the NPC branch now gates on `gold > 0` (ROM fight.c:1473); the sibling PC branch is closed by FIGHT-079. Test: `tests/integration/test_fight078_npc_corpse_money_gate.py` (silver-only NPC → no money object in corpse; gold-bearing NPC → money object present, regression-guarding the existing gold>0 path). Verified red before fix, green after, plus the `death_corpse_loot_sacrifice` differential replay. MINOR (loot inflation; phantom silver was lootable).

- **`FIGHT-079` — `make_corpse` PC-corpse money used full-coins-on-`>0` instead of ROM's clan-only half-coins-on-`>1`, ✅ FIXED (2.14.207).** Surfaced while reading `src/fight.c:1483-1495` to fix FIGHT-078; the original filed note had the clan/non-clan branch interpretation reversed, so the source was re-read before closure. ROM PC corpse: `if (!is_clan(ch)) { corpse->owner = str_dup(ch->name); } else { corpse->owner = NULL; if (ch->gold > 1 || ch->silver > 1) { obj_to_obj(create_money(ch->gold/2, ch->silver/2), corpse); ch->gold -= ch->gold/2; ch->silver -= ch->silver/2; } }` — non-clan PC corpses are owner-locked and transfer **no** coins; clan PC corpses are unowned and drop **half** the coins (rounded down) only when gold or silver is greater than 1, with the player retaining the remainder. Python `make_corpse` previously dropped a full money object for every PC corpse on `gold > 0 or silver > 0` and zeroed both balances. **Fix (2.14.207):** PC money creation is now clan-only, uses the ROM `> 1` gate and integer-truncating half split, leaves non-clan coins untouched, clears `PLR_CANLOOT`, and sets corpse owner exactly per ROM. Tests: `tests/integration/test_money_objects.py::test_non_clan_pc_death_keeps_coins_and_owned_corpse_has_no_money`, `tests/integration/test_money_objects.py::test_clan_pc_death_drops_half_coins_and_keeps_remainder`, plus corrected stale `tests/integration/test_death_and_corpses.py` non-clan expectations. ROM ref `src/fight.c:1483-1495`; Python `mud/combat/death.py:444-476`.

- **`FIGHT-080` — death `PLR_AUTOLOOT`/`PLR_AUTOGOLD` emitted a fabricated summary instead of ROM `do_get` output, ✅ FIXED (2.14.208; autosplit output follow-up 2.14.215).** Surfaced by the new `death_auto_gold` / `death_auto_loot` differential scenarios (FINDING-040). ROM `damage` after `raw_kill` (`src/fight.c:945-967`) resolves auto actions by calling `do_get(ch, "all corpse")` for `PLR_AUTOLOOT` and `do_get(ch, "all.gcash corpse")` for `PLR_AUTOGOLD` when autoloot is off, so the killer sees the real object-by-object get lines (`"You get a hooded brass lantern from the corpse of the janitor."`, `"You get 17 silver coins and 2 gold coins from the corpse of the janitor."`). Python `_auto_collect_loot` / `_auto_collect_coins` manually moved objects/coins and pushed `"You quickly gather the loot from the corpse."`, losing item names and coin totals. **Fix (2.14.208):** both death auto branches now call `mud.commands.inventory.do_get` with the same arguments ROM passes, then deliver that returned line block through `_push_message`; autogold still gates on visible `gcash` and remains suppressed when autoloot is set. **Follow-up (2.14.215):** the mixed `PLR_AUTOLOOT` + `PLR_AUTOSPLIT` C-oracle scenario exposed that `mud.commands.inventory._get_obj` called `do_split` for money but dropped the actor's returned TO_CHAR lines; `do_get` now appends those split lines after the matching "You get ..." line, mirroring ROM `get_obj` (`src/act_obj.c:162-184`). Tests: `tests/test_differential_smoke.py` scenarios `death_auto_gold`, `death_auto_loot`, `death_autogold_autosplit`, and `death_autoloot_autosplit`, `tests/test_combat_death.py::test_auto_flags_trigger_and_wiznet_logs`, and `tests/test_diff_harness_unit.py` meta-command coverage. ROM ref `src/fight.c:945-967`; Python `mud/combat/engine.py:_auto_collect_loot` / `_auto_collect_coins`, `mud/commands/inventory.py:_get_obj`.

- **`FIGHT-076` — `check_assist` PC autoassist gate used the silent-bool `is_safe`, dropping ROM's context line + inheriting its over/under-block (INV-050), ✅ FIXED (2.14.111).** Surfaced 2026-06-14 working the INV-050 caller list. ROM `check_assist` (`src/fight.c:131`) gates the PC/charmed auto-assist on `is_same_group(ch,rch) && !is_safe(rch, victim)`. ROM `is_safe(rch, victim)` writes its OWN rejection line to **rch** (the autoassister) via `send_to_char` BEFORE returning TRUE (e.g. "Join a clan if you want to kill players." for a non-clan PC autoassister attacking a PC). Python `mud/combat/assist.py:check_assist` (~`:84`) routed through the **silent bool** `combat.safety.is_safe`, which (a) wrote no message and (b) lacks the PC-vs-PC clan ladder — so a non-clan PC group member **wrongly auto-assisted** in PvP, silently. **Fix (2.14.111):** converged onto the faithful mirror `_kill_safety_message(rch, victim)` (do_bash/consider/cast pattern) — `None` → rch assists; a non-None string → rch is blocked and sees the line via the module's `_send_to_char`. Function-local import avoids an engine→command cycle. Removed the now-unused module-level `is_safe` import (two `test_fight060` tests monkeypatched it defensively though the NPC-assist branch never calls it — vestigial lines removed). Charmed-mob `rch` `send_to_char` no-ops (NPC, no descriptor) so only PC autoassisters observe the line. Test: `tests/test_combat_assist.py::TestPlayerAutoAssist::test_autoassist_blocked_by_is_safe_clan_ladder`. Verified red before, green after. MINOR.

- **`FIGHT-075` — `do_bash` position-gate message uses literal "them" instead of ROM's `$M` pronoun, ✅ FIXED (2.14.108).** Surfaced 2026-06-14 while closing FIGHT-068 (the order swap). ROM `do_bash` emits `act("You'll have to let $M get back up first.", ch, NULL, victim, TO_CHAR)` (`src/fight.c:2394`) — `$M` = objective pronoun of victim (him/her/it, PERS/can_see aware). Python `mud/commands/combat.py:do_bash` (~`:446`) returned the literal `"You'll have to let them get back up first."` — no `$M` rendering. Same act()-rendering class as FIGHT-073/TRIP-001/FIGHT-064. **Fix (2.14.108):** `act_format("You'll have to let $M get back up first.", recipient=char, actor=char, arg2=victim)`. Test: `tests/integration/test_fight075_bash_position_pronoun_message.py` (male sitting victim → "him"). Verified red before fix, green after. MINOR.

- **`FIGHT-074` — `do_kill` charm "beloved master" checked before is_safe body + kill-steal (order vs ROM), ✅ FIXED (v2.14.103).** Surfaced 2026-06-14 while closing FIGHT-071 (full `do_kill` read, `src/fight.c`). ROM `do_kill` order is is_safe → kill-steal → charm "beloved master". But Python `_kill_safety_message` (with the default `include_charm=True` that `do_kill` used) checked the charm gate *before* its is_safe body (room-safe/shop/pet/clan lines) AND before do_kill's separate kill-steal gate. Observable when a charmed PC targets its master where the master is also in a safe room or being kill-stolen: ROM emits "Not in this room." / "Kill stealing is not permitted.", Python emitted "… is your beloved master." **Fix (Option B, full removal):** removed the `include_charm` param and the charm branch from `_kill_safety_message` entirely so it is a pure ROM `is_safe()` mirror (no charm); all three offensive verbs now carry their own charm gate at the ROM-specified position — do_kill after its kill-steal block (`src/fight.c:2803-2807`), do_dirt/do_trip already correct from FIGHT-071. The bundled (mis-ordered) charm gate is gone. Tests: `tests/integration/test_fight074_kill_charm_order.py` (safe-room order, kill-steal order — both red before; beloved-master guard).

- **`FIGHT-070` — `do_bash` `is_safe` block swallows ROM's context message ("Not in this room." / shopkeeper / pet / …), ✅ FIXED (2.14.104).** Surfaced 2026-06-14 as the FIGHT-067 message-surfacing residual. ROM `is_safe` (`src/fight.c:1018-1124`) sends a *specific* `send_to_char`/`act` line for each block reason ("Not in this room." for ROOM_SAFE `:1034`, "The shopkeeper wouldn't like that." `:1040`, "But $N looks so cute and cuddly..." pet `:1061`, clan lines, …); ROM `do_bash` calls `is_safe` for its side-effect message then `return`s. Python's canonical `mud/combat/safety.py:is_safe` returns `bool` and emits **no** message (it's the FIGHT-002 silent-suppression port), so FIGHT-067's `is_safe(char, victim) → return ""` was behavior-faithful on the lag/daze/knockdown axis but **dropped the player-facing reason string**. **Fix (2.14.104):** routed `do_bash`'s entry gate through `_kill_safety_message` (combat.py:463) — the faithful ROM is_safe() mirror (since FIGHT-074 it contains no command-specific extras) — instead of the silent bool. The mirror also corrects WHICH cases block: the silent bool both over-blocks (`is_ghost`, `ActFlag.GAIN` — neither in ROM is_safe) and under-blocks (no immortal/victim-fighting-back bypass, no PC-vs-PC clan ladder). That bidirectional divergence of `mud/combat/safety.py:is_safe`, plus the ~8 other callers still on the silent bool, is now tracked as **INV-050** in `docs/parity/CROSS_FILE_INVARIANTS_TRACKER.md`; do_bash is the first gate converged onto the faithful mirror. Test: `tests/integration/test_fight070_bash_is_safe_message.py` (bashing an NPC in a ROOM_SAFE room → "Not in this room."). Verified red (`'' != 'Not in this room.'`) before the fix, green after. One pre-existing test (`tests/test_skill_combat_rom_parity.py::TestBashRomParity::test_bash_pc_dodge_penalty_applied`) was riding the bool's *under-blocking* (two non-clan PCs bashing each other); its setup now gives both combatants `clan = 1` to clear ROM's PC-vs-PC clan gate (AGENTS.md: a test contradicting ROM C is a test bug).

- **`ACT-CAP-001` — non-combat act() output not capitalized (⚠️ PARTIAL).** ROM `act_new` capitalizes *all* act() output, but FIGHT-025 only fixed the combat chokepoint `render_for`. Progress: **`mud/utils/act.py:act_format` closed by INV-029 (2.11.38)** via the shared `capitalize_act_line` helper; **the combat-engine broadcast/defense sites closed by FIGHT-031** (`_broadcast_pos_change` + parry/dodge/shield-block + flaming victim); **`mud/net/protocol.py:broadcast_room` closed (2.11.40)** — the ~64-caller `act(TO_ROOM)` delivery boundary now caps the baked line once at function entry (9-assertion re-baseline). **Now CLOSED:** `broadcast_global` channel callers are capped at each call site (ACT-CAP-004, 2.11.43); only the weather path remains uncapped (correct — ROM sends weather via `send_to_char`). Cross-ref INV-029 cousins.

- **`ACT-CAP-003` — communication command output capitalized (do_say, do_tell, do_reply, do_shout, do_yell, do_emote), ✅ FIXED (2.11.42).** Surfaced as INV-029 cousin — `do_say`/`do_tell`/`do_shout`/`do_yell`/`do_emote` build f-strings for per-listener and TO_CHAR lines that bypass `act_format` and `capitalize_act_line`. In ROM, these all route through `act()`/`act_new()` which caps the first visible letter. Fixed by applying `capitalize_act_line` to all six output sites: `do_say` per-listener + TO_CHAR, `_handle_buffered_tell` (TO_VICT) + `do_tell` TO_CHAR, `do_shout` per-listener + TO_CHAR, `do_yell` per-listener + TO_CHAR, `do_emote` per-listener + TO_CHAR. PERS (`pers()`) rendering is unchanged — only the capitalization layer was added. Re-baselined 4 stale lowercase assertions (`test_tell_parity`, `test_say_parity`, `test_shout_yell_parity`, `test_emote_parity`). Test: `tests/integration/test_act_cap_003_communication_capitalize.py` (6).      **CLOSED (ACT-CAP-004, 2.11.43):** `broadcast_global` channel callers now capped at each call site. Only the weather path remains uncapped (correct — ROM `send_to_char` does not cap).

- **`ACT-CAP-004` — broadcast_global channel callers capitalized, ✅ FIXED (2.11.43).** All nine `broadcast_global` channel callers in `mud/commands/communication.py` (auction, gossip, grats, quote, question, answer, music, clan, immtalk) route through ROM `act_new()` which caps the first visible letter. Each now applies `capitalize_act_line` to both the `broadcast_global` message and the TO_CHAR return. The `broadcast_global` function itself is NOT capped because ROM weather (`src/update.c:weather_update`) delivers via `send_to_char` (no `act_new` cap) — weather messages remain uncapped, matching ROM. Test: `tests/integration/test_act_cap_004_broadcast_global_capitalize.py` (6). With ACT-CAP-001/002/003/004, the INV-029 `act_new` first-letter-cap invariant is now fully enforced across all delivery surfaces: `act_format`, `imm_commands` f-strings, combat `render_for`/`_broadcast_pos_change`/defense lines, `broadcast_room`, `Room.broadcast`, `_message_room`, TO_ALL caster legs, `do_say`/`do_tell`/`do_shout`/`do_yell`/`do_emote`, and the nine `broadcast_global` channel callers. Only `broadcast_global` weather remains uncapped (correct — ROM sends weather via `send_to_char`).

- **`FIGHT-032` — defense TO_CHAR/TO_VICT lines bypass PERS (raw `name`), ✅ FIXED (2.11.44).** Surfaced 2026-05-30 while closing FIGHT-031. ROM `check_parry`/`check_shield_block`/`check_dodge` (`src/fight.c:1317-1370`) deliver their lines via `act("$N parries your attack.", ch, NULL, victim, TO_CHAR)` / `act("You parry $n's attack.", ..., TO_VICT)`, so `$N`/`$n` resolve through `PERS(victim/ch, looker)` — an NPC defender renders its `short_descr`, and an *invisible* attacker/defender is masked to "someone". Python used `getattr(victim, "name", "Something")` f-strings — no `can_see` masking, no `short_descr` for NPCs. Fix: route all six defense messages (parry/shield_block/dodge × TO_VICT/TO_CHAR) through `pers()` per recipient, matching `_broadcast_pos_change` / `render_for` pattern. Test: `tests/integration/test_fight_032_defense_pers.py` (7 — parry invisible attacker/defender/NPC, shield_block invisible attacker/defender, dodge invisible attacker/defender).

- **`FIGHT-034` — auto-split per-member line not capitalized (and bypasses PERS), ✅ FIXED (2.11.45).** Surfaced 2026-05-30 by the advisor while closing FIGHT-031. ROM `do_split` (`src/act_comm.c:1946-1962`) delivers the per-group-member share line via `act("$n splits %d silver coins.  Your share is %d silver.", ch, NULL, gch, TO_VICT)`, so ROM act_new caps the first char (`src/comm.c:2376`) and `$n` masks through PERS. Python `mud/combat/engine.py:_auto_split` (the AUTOSPLIT loot path, ~`:1208`) built `split_message = f"$n splits ..."` and routed it through `mud/models/social.py:expand_placeholders`, which substitutes `$n` → raw `getattr(actor, "name", "")` (no `capitalize_act_line`, no `can_see` masking). Same pattern in `mud/commands/group_commands.py:do_split` (the manual `split` command, `:460-474`) — `f"{char.name} splits ..."` baked `char.name` directly. Fix: both sites now route through `pers(actor, member)` per recipient + `capitalize_act_line`, matching ROM `act(TO_VICT)`. Test: `tests/integration/test_fight_034_auto_split_pers_cap.py` (5 — NPC short_descr, invisible splitter PERS mask, silver PERS+cap, gold PERS+cap, mixed PERS+cap). Cross-ref INV-029, INV-027.

- **`FIGHT-033` — WEAPON_FROST/SHOCKING victim lines drop the `$p` weapon name, ✅ FIXED (2.11.44).** Surfaced 2026-05-30. ROM renders the TO_CHAR victim line with the weapon: FROST `act("The cold touch of $p surrounds you with ice.", ...)` (`:664`), SHOCKING `act("You are shocked by $p.", ...)` (`:675`). Python emitted `"The cold touch surrounds you with ice."` (missing `$p`) and `"You are shocked by the weapon."` (generic "the weapon" instead of `$p`). Fix: thread `weapon_name` into both TO_CHAR templates, matching the ROM `$p` substitution. FIGHT-031 capitalized these lines (the leading word is "The"/"You" which are already capital); FIGHT-033 fixes the text content divergence. Test: `tests/integration/test_fight_033_frost_shocking_weapon_name.py` (2).

- **`SHOP-PET-001` — ❌ N/A (premise-incorrect, verified 2026-05-29).** Original
  claim: `mud/commands/shop.py:193` `pet.dam_type = _coerce(getattr(template, "dam_type", 0))`
  reads `proto.dam_type` (which the loader leaves at 0), so a bought pet gets
  `dam_type == 0` → attack-table index 0 → noun "hit". **The premise is factually
  wrong:** `template` is not the proto — it is the kennel `MobInstance` returned by
  `_find_pet_template` (iterating `room.people`). In gameplay that instance is created
  by `apply_resets` → `spawn_mob` → `MobInstance.from_prototype`, which already resolves
  the damtype word to a non-zero attack-table index on the *instance* (the `.are` word
  on `proto.damage_type` → `attack_lookup`, FIGHT-023; ROM does this on the proto at
  load, `src/db2.c:270`). So `getattr(template, "dam_type", 0)` reads the resolved
  index, and `_clone_pet_character` copies it. Verified end-to-end: a bought pet of a
  proto with damtype "beating" gets `dam_type == 13` and renders noun "beating", never 0/"hit".
  Regression guard: `tests/integration/test_shop_pet_001_dam_type_resolution.py`.
  Reclassified N/A — no code change (cf. ARITH-110 N/A precedent). The *real* residual
  divergence is filed as SHOP-PET-002 below.

- **`SHOP-PET-002` — ✅ FIXED (2.11.28) — `_clone_pet_character` cloned the template instead of `create_mobile(pIndexData)`.** ROM `do_buy` (`src/act_obj.c:2613`) does `pet = create_mobile(pet->pIndexData)` — a **fresh** mobile re-rolled from the index, not a copy of the kennel template. Python's `mud/commands/shop.py:_clone_pet_character` instead copied the template `MobInstance`'s runtime fields (HP, mana, gold, the already-rolled dam_type, etc.). Divergences: (a) the random-default dam_type for a **no-word proto** was *cloned* from the template, where ROM re-rolls `number_range(1,3)` per `create_mobile` (so a bought pet of a no-word proto could never differ from its kennel template, unlike ROM); (b) the bought pet did **not advance the spawn RNG stream** the way `create_mobile` does (ROM draws gold→hp→mana→damtype→sex, `src/db.c:2047-2113`), so any RNG consumer ordered after a pet purchase was desynced vs ROM; (c) HP/mana/gold were inherited from the template rather than freshly rolled. Surfaced 2026-05-29 while verifying SHOP-PET-001. **Fix:** `_handle_pet_shop_purchase` now creates the pet via `spawn_mob(proto.vnum)` (the `create_mobile` equivalent — single source of truth for the ROM draw order), reapplying the ROM `do_buy` overrides on the fresh mob: `name`←`player_name` + `short_descr` from the index (`src/db.c:2038-2039`, compensating for `from_prototype` setting `name`←`short_descr`), `ACT_PET`, `AFF_CHARM`, `comm` assigned outright (`src/act_obj.c:2614-2616`). This also unifies the bought-pet type (`MobInstance`) with the already-`MobInstance` reload path (`_deserialize_pet`). The now-dead `_clone_pet_character` was removed. Test `tests/integration/test_shop_pet_002_create_mobile_reroll.py` (bought pet's random fields match a fresh `create_mobile` from the same seed + the purchase advances the RNG stream identically — a clone draws nothing, the deterministic discriminator). Existing `test_pet_shop_purchase_creates_charmed_pet` `has_comm_flag` assertions reconciled to `comm`-bit checks (the pet is now a `MobInstance`, which keys `comm` as a raw int).

- **NPC `do_X` RNG-draw fidelity unverified.** `_mob_offensive_skill` invokes the real command handlers on mobs, but whether each handler's success roll + downstream draws (e.g. `do_trip`'s `number_percent`/`number_range`, daze/position/damage) match ROM `get_skill`-driven NPC behavior is unverified. The treat-as-100 NPC skill value (mirrors existing `do_kick`/`do_berserk`) may diverge from ROM's `get_skill` NPC value, changing success→downstream branching and hence draw count. The differential can't catch this — the drunk #3064 (the only differential mob) has no `OFF_` flags, so nothing exercises the dispatch.
- **`do_backstab("")` bails for NPC callers** where ROM passes a target (`do_backstab` returns "Backstab whom?" on empty args). Flagged-mob backstab fidelity gap; no crash.
- **NPC `get_skill` value.** Whether NPC offensive skills should be 100 (current pattern) vs ROM's level-based `get_skill` NPC value is a pre-existing question that also affects shipped `do_kick`/`do_berserk` — not introduced here.

## Phase 4 — Closures

1. `FIGHT-001` — ✅ closed with `tests/integration/test_fight_c_do_kill_parity.py`.
2. `FIGHT-002` — ✅ closed with `tests/integration/test_fight_c_safe_room_damage_gate.py`.
3. `FIGHT-003` — ✅ closed with `tests/integration/test_character_advancement.py::test_player_kill_applies_rom_death_penalty`.

## Phase 5 — Completion summary

`fight.c`'s combat-control regressions (FIGHT-001/002/003) are closed.
The 2026-05-23 PERS sweep opened five new gaps on the
position-change broadcast surface (`_position_change_message`):
FIGHT-004..008. All five are the same pattern the channel-message
arc just normalized (`pers()` helper from `mud/world/vision.py`),
plus colour-wrap and wording corrections on the POS_DEAD case.

**Out of scope for this sub-sweep, deferred to a follow-up cluster
(reserve FIGHT-009..013 for them):** weapon-proc broadcasts at
`mud/combat/engine.py:1496` (poison), `:1510` (vampiric), `:1531`
(flaming), `:1541` (frost), `:1551` (shocking) all have analogous
PERS gaps and some have wording divergences vs ROM
`src/fight.c:614-675` (e.g. ROM `"$p freezes $n."` rendered by
Python as `"X is frozen by Y."`). Same fix pattern, different
session.

Recent verification locked by
`tests/integration/test_fight_c_do_kill_parity.py`,
`tests/integration/test_fight_c_safe_room_damage_gate.py`, and
`tests/integration/test_character_advancement.py::test_player_kill_applies_rom_death_penalty`.

## Notes — test fragility (✅ hardening pass DONE, 2.11.8)

**Resolved 2026-05-28 (2.11.8).** After a clean parallel full-suite run flaked
`test_one_hit_uses_equipped_weapon`, the unseeded hit/miss-outcome tests in
`tests/test_combat.py` were hardened by pinning the ROM `number_bits(5)` attack
roll per test: `lambda *_: 19` (nat-19, always hits) for hit-/damage-/kill-
asserting tests, `lambda *_: 0` (nat-0, always misses) for `test_attack_misses_target`.
Tests hardened: `test_one_hit_uses_equipped_weapon`, `test_attack_damages_but_not_kill`,
`test_attack_kills_target`, `test_attack_misses_target`, `test_multi_hit_single_attack`,
`test_multi_hit_second_attack`, `test_multi_hit_victim_dies_early`,
`test_riv_scaling_applies_before_side_effects`, `test_poison_weapon_applies_affect`
(plus `test_multi_hit_with_haste`, already pinned). Tests whose only assertion is
`assert_attack_message` (which holds for both hit and miss messages) or that count
attacks rather than hits were left as-is — they are not outcome-brittle. Verified:
`tests/test_combat.py` 32/32 across 3 serial runs + clean full parallel suite.

### Historical (the gap this pass closed)

Surfaced while closing FIGHT-019: the unseeded combat tests in
`tests/test_combat.py` assert **exact** hit/miss and damage outcomes
while controlling the *old* percent knob (`number_percent → 1/100`) or
relying on `hitroll = ±100` rather than pinning the ROM attack roll
(`number_bits(5)`). They pass only by RNG-stream position and flip when
any upstream change alters draw counts (FIGHT-019's `number_bits`
re-roll loop did exactly this to `test_multi_hit_with_haste`). The ones
that actually broke were re-baselined (pin `number_bits → 19` / `0`);
the rest (`test_multi_hit_second_attack`, `test_multi_hit_third_attack`,
and the `number_percent → 1` hit-knob tests) still pass on stream
position and were intentionally left untouched to keep the FIGHT-019
commit scoped. A follow-up hardening pass should pin `number_bits` (and
`number_range` where exact damage is asserted) in these so they are
deterministic under the ROM model rather than incidentally green.

# update.c Parity Audit

**ROM C source**: `src/update.c`  
**Python modules**: `mud/game_loop.py`, `mud/advancement.py`  
**Audited**: 2026-05-17  
**Auditor**: OpenCode session  

---

## Scope

`src/update.c` contains eleven functions:

| ROM C function | Python equivalent | Notes |
|---|---|---|
| `advance_level()` | `advance_level()` | mud/advancement.py |
| `gain_exp()` | `gain_exp()` | mud/advancement.py |
| `hit_gain()` | `hit_gain()` | game_loop.py |
| `mana_gain()` | `mana_gain()` | game_loop.py |
| `move_gain()` | `move_gain()` | game_loop.py |
| `mobile_update()` | `mobile_update()` | mud/ai.py |
| `weather_update()` | `weather_tick()` + `time_tick()` | split — intentional divergence |
| `char_update()` | `char_update()` | game_loop.py |
| `obj_update()` | `obj_update()` | game_loop.py |
| `aggr_update()` | `aggressive_update()` | mud/ai.py |
| `update_handler()` | `game_tick()` | game_loop.py |

---

## Gap Table

| ID | Severity | ROM C location | Description | Status |
|---|---|---|---|---|
| GL-001 | N/A | — | `hit_gain` heal_rate, furniture value[3], NPC/PC branches | ✅ CORRECT |
| GL-002 | N/A | — | `move_gain` heal_rate, furniture value[3] | ✅ CORRECT |
| GL-003 | N/A | — | `game_tick` / `update_handler` ordering fixed this session | ✅ FIXED (this session) |
| GL-004 | BUG | update.c:297 | `mana_gain` uses `heal_rate` instead of `mana_rate` for room scaling | ✅ FIXED |
| GL-005 | BUG | update.c:300 | `mana_gain` furniture bonus uses `value[3]` instead of `value[4]` | ✅ FIXED |
| GL-006 | N/A | — | `update_handler` tick ordering corrected this session | ✅ FIXED (this session) |
| GL-007 | N/A | — | `run_npc_specs` moved inside PULSE_MOBILE gate this session | ✅ FIXED (this session) |
| GL-008 | N/A | — | `tail_chain` — ROM C memory chain; Python GC handles this | N/A |
| GL-009 | GAP | update.c:688-696 | NPC wanders-home: if NPC is outside its zone, 5% chance → act + extract_char | ✅ FIXED |
| GL-010 | BUG | update.c:762-786 | `tick_spell_effects` removed merged `spell_effects` entries even when a later same-type `AffectData` remained active, and did not honor ROM same-type wear-off suppression semantics. | ✅ FIXED |
| GL-011 | GAP | update.c:794-846 | Plague tick: spread to room, drain mana/move, damage(ch,ch,dam,gsn_plague) | ✅ FIXED |
| GL-012 | GAP | update.c:848-862 | Poison tick: act shiver message + damage(ch,ch,level/10+1,gsn_poison) | ✅ FIXED |
| GL-013 | GAP | update.c:864-867 | POS_INCAP tick: 50% chance damage(ch,ch,1,TYPE_UNDEFINED) | ✅ FIXED |
| GL-014 | GAP | update.c:868-871 | POS_MORTAL tick: damage(ch,ch,1,TYPE_UNDEFINED) every tick | ✅ FIXED |
| GL-015 | BUG | update.c:741-744 | `_idle_to_limbo` sets `fighting=None` directly; ROM calls `stop_fighting(ch,TRUE)` | ✅ FIXED |
| GL-016 | N/A | — | Immortal timer/light ordering — Python equivalent is logically correct | ✅ CORRECT |
| GL-017 | BUG | update.c:939-957 | `_tick_object_affects` broadcast duplicate wear-off messages for consecutive zero-duration same-type affects instead of matching ROM’s one-message suppression rule. | ✅ FIXED |
| GL-018 | GAP | update.c:1017-1023 | Pit-corpse suppression: items inside a no-take pit suppress room decay message | ✅ FIXED |
| GL-019 | DIVERGENCE | update.c:530-556 | Time advancement is inside `weather_update` in ROM C; Python splits into `time_tick()` | INTENTIONAL |
| GL-020 | N/A | — | `weather_info.change` RNG expression — Python 3-call equivalent produces same distribution | ✅ CORRECT |
| GL-021 | GAP | update.c:1186 | Point pulse emits `wiznet("TICK!", NULL, NULL, WIZ_TICKS, 0, 0)` before weather/char/obj update work. | ✅ FIXED |
| GL-022 | BUG | update.c:128-139 | `gain_exp()` sent the level-up banner after `advance_level()` and skipped ROM `log_string("%s gained level %d")` entirely. | ✅ FIXED |
| GL-023 | BUG | update.c:61-139 | `advance_level()` / `gain_exp()` XP-path verification was not represented in this audit even though the ROM functions live in `update.c`. | ✅ FIXED — audit coverage now explicitly includes `mud/advancement.py`; targeted tests lock message order, log-before-wiznet ordering, and death-floor behavior. |
| GL-024 | BUG | update.c:818-819 | Plague tick: ROM does `if (af->level == 1) continue;` — a level-1 plague affect skips the entire spread + mana/move drain + `damage()` block that tick. Python `mud/game_loop.py:_char_update_tick_effects` gated only the *spread* on `if af_level > 1:`; the drain and `damage()` still ran when `af_level == 1`. **FIX (2.9.80):** moved the drain + `damage()` block inside the `if af_level > 1:` guard so a level-1 plague prints the writhe messages then goes dormant (no spread, drain, or damage), mirroring ROM's `continue`. Test: `tests/integration/test_gl_024_level1_plague_dormant.py`. | ✅ FIXED |
| GL-025 | BUG | update.c:721-862 | `char_update` operation order: ROM processes PC worn-light decay, idle timer handling, and condition decay before affect expiry and plague/poison/incap/mortal damage. Python ran affect expiry and damage first, so a lethal poison/plague tick could move equipment into the corpse before a one-tick light burned out, skipping `--room->light`, burnout messages, and `extract_obj`. **FIX (2.11.57):** moved the PC light/timer/condition block before `tick_spell_effects()` and `_char_update_tick_effects()` while preserving affect ticks for connected PCs and immortals. Test: `tests/test_game_loop.py::test_char_update_decays_light_before_lethal_poison_tick`. | ✅ FIXED |
| GL-026 | BUG (RNG parity) | update.c:765-768 | Affect-tick level-fade RNG-consumption order. ROM `if (number_range(0,4) == 0 && paf->level > 0) paf->level--;` — C `&&` is left-to-right short-circuit and `number_range` advances MM state as a side effect, so the roll is consumed **unconditionally for every `duration>0` affect**, regardless of level; `level>0` is only tested afterwards. Python `mud/affects/engine.py:tick_spell_effects` had the operands **swapped** (`if level > 0 and number_range(0,4) == 0`), skipping the roll whenever `level == 0` — and level reaches 0 naturally via the fade mechanic on long-lived affects. Each skipped roll desynced the global RNG stream for every downstream consumer in the same tick (the immediately-following plague/poison `damage()` in `_char_update_tick_effects`) and beyond. **FIX (2.12.6):** reorder to roll first (`fades = number_range(0,4) == 0; if fades and level > 0: …`), mirroring ROM's left-operand-first evaluation. Narrow contract locked: K affects with `duration>0` consume exactly K rolls in list order, independent of level. Test: `tests/integration/test_gl026_affect_tick_rng_consumption.py` (4: level-0 + multi red-first, level>0 + permanent controls). | ✅ FIXED |
| GL-028 | BUG (crash / tick-abort) | update.c:762-786 | **An expiring spell effect on a mob crashed the whole game tick.** The dict-only fallback of `tick_spell_effects` (`mud/affects/engine.py`) calls `character.remove_spell_effect(name)` on expiry, but **`MobInstance` had no `remove_spell_effect`** — so the moment any mob's spell effect expired, `tick_spell_effects` raised `AttributeError`. Reachable via the normal cast path: ~40 spell handlers call `target.apply_spell_effect(effect)`, and for a mob that resolves to `MobInstance.apply_spell_effect` (writes `spell_effects`, no `affected` mirror) → the mob ticks through the fallback. **Severity confirmed empirically on the real `char_update` path** (`tests/integration/test_gl028_mob_spell_effect_tick.py`): neither the per-character loop body nor the `char_update()` call in `game_tick` is wrapped in try/except, so the exception **propagates out of the entire tick** — every character after the mob in `character_registry` is skipped that tick, along with `obj_update`/`pump_idle`/`aggressive_update`. **FIX (2.12.7):** added `MobInstance.remove_spell_effect` (`mud/spawning/templates.py`), symmetric to `MobInstance.apply_spell_effect` — unwinds exactly hitroll/damroll/affect_flag (the mob model never applies ac/saving/stat/sex mods — **superseded: GL-032 (2.12.11) extended both methods to ac/saving/stat/sex, so this parenthetical describes the 2.12.7 state only**). Test: `tests/integration/test_gl028_mob_spell_effect_tick.py` (real cast → tick-to-expiry, no exception escapes, effect removed). Surfaced 2026-05-31 while scoping GL-027. | ✅ FIXED |
| GL-027 | BUG (RNG parity + timing) | update.c:762-768 | The **dict-only fallback** of `tick_spell_effects` (`mud/affects/engine.py`) is a divergent re-implementation of ROM's affect loop, reached by `MobInstance` NPCs (no `affected` list — see GL-028). Two remaining divergences vs ROM / the main `affected`-list path: **(1) RNG desync** — it decrements `spell_effects` durations but calls `number_range` **zero times** and never fades level, so ROM rolls one `number_range(0,4)` per `duration>0` mob affect while Python rolls none (the GL-026 contract, total desync for mobs). **(2) off-by-one expiry** — it expires a `duration==1` affect on the *same* tick (`1→0→remove`), whereas ROM and the main path decrement-and-stay, expiring only when `duration==0` is hit on a *later* tick. Fix direction (decide before patching fallback internals — option b makes a/timing fixes throwaway): (a) make the fallback faithfully mirror ROM's per-affect loop (roll + fade + decrement-and-stay), or (b) give `MobInstance` a real `affected` list + sync so mobs collapse onto the main GL-026 path and the fallback can be retired. The GL-028 crash blocked exercising this; now unblocked. Surfaced 2026-05-31 alongside GL-026. **FIX (2.12.8) — option (b):** gave `MobInstance` a real `affected` list (`mud/spawning/templates.py`) and a shared `mud.models.character.sync_spell_effect_to_affected` helper (extracted from `Character._sync_spell_effect_to_affected` so the two never drift). `MobInstance.apply_spell_effect` now mirrors shadow `AffectData` into `self.affected` (and mirrors Character's remove-then-apply on a re-cast, fixing a latent double-apply), so modifier-bearing mob affects route through the main `affected`-list tick path — one `number_range(0,4)` roll per `duration>0` affect (GL-026 contract) and decrement-and-stay. `remove_spell_effect` drops the shadow. Tests: `tests/integration/test_gl027_mob_affect_tick_parity.py` (RNG-stream + decrement-and-stay + flag-only-alongside-modifier orphan guard). **Closed together with GL-029** (the flag-only base-entry), so every active `spell_effect` now mirrors ≥1 `AffectData` and the dict-only fallback is no longer reachable via the normal apply path. | ✅ FIXED |
| GL-029 | BUG (RNG parity + timing + orphan-freeze) | update.c:762-768 | **Residual of GL-027, caught by adversarial review before push.** `sync_spell_effect_to_affected` (`mud/models/character.py`) emitted a shadow `AffectData` only for each **non-zero numeric modifier** (ac/hitroll/damroll/saving/stat). A spell effect carrying **only an affect_flag** (sanctuary/sleep/fly/invis/…) or no modifier produced **no** shadow, with two failure modes: (a) if it was the character's only active affect it still ticked through the fallback (zero RNG, off-by-one expiry); (b) **worse — a regression GL-027 itself introduced for mobs:** if a *modifier-bearing* affect was also active, `affected` was non-empty so the tick took the main path, which only visits affects present in `affected` — the flag-only effect was never decremented and its duration **froze** for every tick the modifier-bearing affect stayed alive (effectively permanent under sustained combat re-casts). Pre-GL-027 mobs always hit the fallback, which iterated all `spell_effects` and decremented even flag-only ones. **FIX (2.12.8):** the shared helper now emits one base `AffectData` (`location=APPLY_NONE`, `modifier=0`, `bitvector=flag`) when no modifier shadow was created, so flag-only effects tick on the main path for both PCs and mobs. The deferral rationale ("shifts PC RNG streams") was an untested assumption — full suite stayed green (5126 passed), so it was closed in the same commit, not deferred. Test: `tests/integration/test_gl027_mob_affect_tick_parity.py::test_flag_only_affect_ticks_alongside_modifier_affect`. Surfaced 2026-05-31 by advisor review while closing GL-027 (option b). | ✅ FIXED |
| GL-030 | BUG (crash / save) | save.c:449-523 (`fwrite_pet`) | **Regression from GL-027 — charmed-pet save crashed.** GL-027 gave `MobInstance` a real `affected` list whose shadow `AffectData` are keyed by spell **name** (a `str`, e.g. `"bless"`). `_serialize_pet` (`mud/db/serializers.py`, ROM `fwrite_pet`) was written for integer-SN affects and did `if affect.type < 0: continue` — `"bless" < 0` raises `TypeError` in Python 3, so saving any charmed pet that had been buffed/debuffed crashed the character save. **FIX (2.12.9):** guard the loop with `if not isinstance(affect_type, int) or affect_type < 0: continue` — string-named (spell_effects-managed) shadows are skipped (they're regenerated by `apply_spell_effect`), integer-SN raw affects still round-trip. Surfaced 2026-05-31 verifying the pet round-trip after GL-027. Test: `tests/test_pet_save_affect_roundtrip.py`. | ✅ FIXED |
| GL-031 | GAP (persistence) | save.c:449-523 (`fwrite_pet`) + load_char_obj pet block | **Pre-existing — a charmed pet's spell-cast buffs are not persisted across save/reload.** `MobInstance.spell_effects` is never serialized; `_serialize_pet` only emits integer-SN affects from `affected`, and GL-030 (correctly) skips the string-named spell_effects shadows. So a pet buffed with bless/armor/sanctuary loses those on reload. Predates GL-027 (mob `spell_effects` were never saved; GL-027 only added the shadows GL-030 now skips). ROM `fwrite_pet` saves the pet's affects by SN (`src/save.c:511-521`), so this is a real parity gap. **FIX (2.12.10):** added a `PetSpellEffectSave` dataclass + `PetSave.spell_effects` field; `_serialize_pet` now serializes `pet.spell_effects` and `_deserialize_pet` restores them. **Correction to the proposed fix:** restore is *data-only* (re-register the `SpellEffect` + mirror its shadow via `sync_spell_effect_to_affected`), NOT `apply_spell_effect` — ROM `fread_pet` (`src/save.c:1544-1573`) links each affect onto `pet->affected` **without** `affect_modify`, because the saved `ACs`/`Hit`/`AMod` lines already fold in the affect bonuses; re-applying via `apply_spell_effect` would double-count the modifiers against the already-modified saved stats. Surfaced 2026-05-31 while closing GL-030. Test: `tests/integration/test_gl031_pet_spell_effect_persistence.py`. | ✅ FIXED |
| GL-032 | GAP (mob affect application) | handler.c:1198-1310 (`affect_modify`) ↔ `MobInstance.apply_spell_effect` | **`MobInstance.apply_spell_effect` (`mud/spawning/templates.py`) is a "simplified" affect applier that only applies `hitroll_mod`/`damroll_mod`/`affect_flag` — it ignores `ac_mod`, `saving_throw_mod`, `stat_modifiers`, and `sex_delta`.** ROM applies every affect location uniformly through `affect_modify` (`src/handler.c`), so a charmed pet (an NPC) buffed with armor (AC), giant strength (STR), or a save-modifying spell DOES get the AC/stat/save benefit. In the Python port those locations are no-ops on a mob — both live and (consistently) on reload, since GL-031 persists the effect data without re-applying. So a charmed pet currently gains nothing from armor/giant-strength/sanctuary-AC etc. except hitroll/damroll. `Character.apply_spell_effect` applies all locations; `MobInstance`'s does not. Surfaced 2026-05-31 by advisor review while closing GL-031 (the ac_mod/stat_modifier round-trip assertions were non-mutating precisely because the mob applier ignores them). **FIX (2.12.11):** gave `MobInstance` a `mod_stat` list (ROM stores stat affects in `ch->mod_stat[]` uniformly for PCs/NPCs, `src/handler.c:1072-1086`); `get_curr_stat` now reads `perm_stat + mod_stat`; `apply_spell_effect` now applies — and merges, per `affect_join` — `ac_mod` (across all four armor classes), `saving_throw_mod`, `stat_modifiers` (→ `mod_stat` via new `_apply_stat_modifier`), and `sex_delta` (clamped to the `Sex` range via new `_shift_sex`); `remove_spell_effect` unwinds all four symmetrically. The save/reload round-trip is already correct (folded-in runtime `armor`/`saving_throw`/`mod_stat`/`sex` are serialized by `_serialize_pet`, and GL-031's data-only restore re-counts nothing) — bonus counted exactly once. Test: `tests/integration/test_gl032_mob_affect_application.py` (live ac/save/stat/sex apply, symmetric removal, round-trip-counted-once). | ✅ FIXED |
| GL-036 | BUG (crash / tick-abort) | fight.c `mob_hit` off-skill switch → act_info.c (`is_affected(ch, gsn_berserk)`) ↔ `MobInstance.has_spell_effect` | **A berserker mob fighting a player crashed the game tick every round.** `MobInstance` (mud/spawning/templates.py) defined `has_affect`/`apply_spell_effect`/`remove_spell_effect` but **not** `has_spell_effect`. ROM routes an OFF_BERSERK mob through `mob_hit` → `do_berserk`, and `mud/commands/combat.py:do_berserk` guards on `char.has_spell_effect("berserk")`; on a mob actor that raised `AttributeError`, and because neither the per-character combat loop nor the `violence_tick()`/`char_update()` call in `game_tick` is wrapped in try/except, the exception propagated out of the **entire tick** (same blast radius as GL-028 — every character after the mob is skipped, plus obj_update/pump_idle). Member of the `MobInstance`-method-completeness family (GL-028 `remove_spell_effect`, GL-032 ac/save/stat/sex). **FIX (2.12.87):** added `MobInstance.has_spell_effect` returning `name in self.spell_effects`, symmetric with `Character.has_spell_effect`. Surfaced 2026-06-03 from a live player report (Eddol@127.0.0.1, repeated game-loop crashes). Test: `tests/integration/test_mob_berserk_has_spell_effect.py`. | ✅ FIXED |
| GL-035 | GAP (connected idle autoquit) | update.c:682-683 + 897-900 (`ch_quit` → `do_function(ch, &do_quit, "")`) | **A connected (non-link-dead) player who idles past 30 ticks is NOT auto-quit.** ROM's `do_quit` synchronously extracts the char and `close_socket`s the descriptor; QuickMUD's `_auto_quit_character` (`mud/game_loop.py`) runs inside the **synchronous** `game_tick()` and cannot `await conn.close()` to tear down a live descriptor/session, so INV-038 deliberately gates the autoquit append to `desc is None` (link-dead) only. A connected idler still climbs the timer and disappears into the void at `>= 12` (`_idle_to_limbo`, ROM-correct, stays connected), but never auto-quits — it parks in limbo connected until it types or the socket drops. Closing it faithfully needs a tick→connection disconnect signal that wakes the parked `_read_player_command`/`handle_connection` coroutine (close the transport so `readline` returns `None` → the existing `finally` cleanup runs INV-009/020 teardown), across telnet/SSH/websocket. Surfaced 2026-06-03 by advisor review while closing INV-038 (the connected-autoquit path was newly reachable and would otherwise zombie the session: char removed from registry while the coroutine stays parked). **FIX (2.12.94):** `char_update`'s autoquit gate dropped the `desc is None` restriction (ROM sets `ch_quit` for any `timer > 30` regardless of link state), and `_auto_quit_character` now branches: a CONNECTED idler (`desc` + `connection` set) has its live connection closed via a scheduled `asyncio.create_task(conn.close())` (new `_schedule_connection_close` helper, mirroring the `MESSAGE_DELIVERY` fire-and-forget pattern) — the parked `readline` returns `None` → the playing loop's `finally` runs the full INV-009/020 teardown; the tick does NOT also extract (avoids the double-remove race). Link-dead idlers keep the synchronous extract. No running loop (sync test) → falls through to the synchronous extract. Test: `tests/integration/test_inv038_idle_timer_input_reset.py::test_connected_idle_player_autoquits_via_async_close` (fake conn, real event loop, asserts `close()` fired and the tick did not sync-extract). **Wake-chain proven (2.12.96):** `::test_server_side_close_wakes_parked_readline` exercises a real `TelnetStream` over a connected `socket.socketpair` — a server-side `close()` wakes our own parked `readline()` with EOF (returns `None`), which is the load-bearing assumption that the playing-loop `finally` then runs. **Follow-up GL-037** (OPEN): the close-transport path runs the clean-disconnect `finally`, which does NOT replicate ROM `do_quit`'s "Alas, all good things must come to an end." (TO_CHAR) / "$n has left the game." (TO_ROOM) messaging. | ✅ FIXED (2.12.94) |
| GL-034 | DIVERGENCE (idle autoquit fan-out) | update.c:665-683 + 897-900 (`ch_quit`) | **ROM auto-quits at most ONE idle character per `char_update` tick.** `ch_quit` is a single `CHAR_DATA *` overwritten each loop iteration (`if (ch->timer > 30) ch_quit = ch;`), so after the main loop it holds the **last** character in `char_list` whose pre-increment timer exceeded 30; the second loop then quits only `ch == ch_quit`. Python `mud/game_loop.py:char_update` collects **all** `timer > 30` characters into `autoquit_candidates` and quits every one the same tick (INV-038 preserved this list-based fan-out while fixing the connected-player timer-reset bug). Practical impact is tiny — two PCs must be simultaneously idle ≥31 ticks — but it is a genuine ROM divergence: ROM staggers idle autoquits one-per-tick (last-in-list first), Python quits them en masse. Closing it faithfully means selecting only the last `character_registry` entry with pre-increment `timer > 30`. Surfaced 2026-06-03 while closing INV-038 (advisor-flagged; kept out of the INV-038 commit to stay narrow). **FIX (2.12.95):** `char_update` now tracks a single `autoquit_candidate` instead of an `autoquit_candidates` list, and quits at most that one after the loop. **Ordering corrected post-review (2.12.96):** ROM PREPENDS new chars to `char_list` (`src/nanny.c:757-758`, `src/db.c:2256-2257`: `ch->next = char_list; char_list = ch`), so the list runs newest→oldest and walking head→tail leaves `ch_quit` on the tail = the **OLDEST** idle char. `character_registry` is the reverse (`append` → oldest→newest), so the oldest idle char is the **FIRST** match — the selection is **first-wins** (`and autoquit_candidate is None`), not last-wins, otherwise it quits the newest idler (reversed from ROM). Combined with GL-035 (2.12.94) the candidate population is ROM-faithful (any `timer > 30`, link-dead or connected). Test: `tests/integration/test_inv038_idle_timer_input_reset.py::test_only_one_idle_player_autoquits_per_tick` (two idlers → only the oldest/first quits this tick, the newer next tick). | ✅ FIXED (2.12.95; ordering corrected 2.12.96) |
| GL-037 | DIVERGENCE (autoquit messaging) | act_comm.c:1481-1482 (`do_quit` — `send_to_char("Alas, all good things must come to an end.")` + `act("$n has left the game.", TO_ROOM)`) ↔ `mud/game_loop.py:_auto_quit_character` connected leg → `mud/net/connection.py` playing-loop `finally` | **Idle autoquit of a connected player does not emit ROM's `do_quit` farewell/room messaging.** GL-035 closes the connected-autoquit *teardown* by scheduling a transport close so the playing loop's `finally` (the clean-disconnect path) runs; that path saves + extracts + closes but never sends "Alas, all good things must come to an end." to the quitter nor broadcasts "$n has left the game." to the room. ROM `do_quit` (`src/act_comm.c:1481-1482`) sends both before `extract_char`. Low impact (the quitter is idle/gone, and the idler was already voided to limbo at timer 12 so the room broadcast would land in limbo anyway), but a genuine messaging divergence. Closing it means emitting the two ROM lines on the idle-autoquit path specifically (not the generic socket-drop disconnect, which is ROM's link-death, not `do_quit`). Surfaced 2026-06-03 by advisor review while closing GL-035. **FIX (2.12.97):** `_auto_quit_character` now emits both ROM `do_quit` lines at the top of the function — before the connected/link-dead branch and before scheduling the transport close — so the TO_CHAR send task is queued ahead of the close and the TO_ROOM broadcast lands in the current (limbo) room. On the link-dead leg the TO_CHAR routes to the discarded mailbox (no socket), a harmless no-op equivalent to ROM `send_to_char`'s `desc==NULL` short-circuit; the TO_ROOM still fires. Verified no double-broadcast: the clean-disconnect `finally` (`mud/net/connection.py:2037`) sends neither line. Test: `tests/integration/test_inv038_idle_timer_input_reset.py::test_connected_idle_autoquit_emits_do_quit_messaging` (recording fake conn asserts "Alas…" reached the quitter; mailbox witness asserts "Idler has left the game."). | ✅ FIXED (2.12.97) |
| GL-033 | DIVERGENCE (stat floor) | handler.c:868-874 (`get_curr_stat`) ↔ `MobInstance.get_curr_stat` | **`MobInstance.get_curr_stat` clamped the lower bound to `0` (`max(0, min(25, perm+mod))`); ROM `get_curr_stat` uses `URANGE(3, …)` — minimum 3 — uniformly for PCs and NPCs (`src/handler.c:872`).** Latent before GL-032 (a mob built via `from_prototype` has `perm_stat ≥ ~11`, never below 3), but GL-032's new `mod_stat` made sub-3 effective stats reachable via a negative stat affect (e.g. a STR-draining debuff). Directly-constructed zero-stat fixtures also fed the wrong effective NPC STR/DEX into bash/disarm/dirt-kick chance formulas. **FIX (2.12.12):** raised `MobInstance.get_curr_stat` to the ROM floor of 3 (max remains 25 for NPCs) and re-derived the affected skill-combat expectations against ROM `get_curr_stat`: bash larger-victim chance 69, NPC zero-skill bash chance 99, disarm DEX-vs-STR term `3 - 2*3`, and inside-terrain dirt-kick chance 27. Tests: `tests/integration/test_get_curr_stat_floor_three.py` (NPC floor cases) and `tests/test_skill_combat_rom_parity.py`. | ✅ FIXED |
| GL-038 | BUG (affect gate) | update.c:793 `if (is_affected(ch, gsn_plague))` ↔ `mud/game_loop.py:_char_update_tick_effects` | **`_char_update_tick_effects` gated the plague tick on `has_affect(AffectFlag.PLAGUE)` (bitmask) instead of ROM's `is_affected(ch, gsn_plague)` (spell affect list).** ROM's `is_affected` scans `ch->affected`; `IS_AFFECTED`/`has_affect` checks the bitmask. A mob or PC with innate `AFF_PLAGUE` set via `affected_by` (e.g. area-file assignment) but no spell-affect entry would: in C — skip the tick block entirely (bitmask persists, regen ÷8 fires every pulse); in Python — enter the block, print writhe messages, find no spell entry, and *clear the bitmask* (the orphan-clearing branch at line 748-751), losing the ÷8 divisor after pulse 1. This is also reachable in normal play via `__add_affect`. **FIX (2.13.79):** Changed the gate from `has_affect(AffectFlag.PLAGUE)` (bitmask) to a spell-list scan (`for af in character.affected: if spell_name == "plague"`), matching ROM's `is_affected(ch, gsn_plague)`. Removed the unreachable orphan-clearing branch. Writhe messages now print only when a spell affect entry exists, matching C. Regen divisors in `hit_gain`/`mana_gain`/`move_gain` remain on `has_affect` (bitmask) — the asymmetry is intentional and matches ROM. Scenario: `char_update_regen_plague` (C oracle: HP +1/mana +2/move +3 per pulse with orphan PLAGUE bit, flag persists across all pulses). Tests: `test_drive_python_replay_plague_affect_divides_regen_by_eight` + smoke scenario. | ✅ FIXED |
| GL-042 | DIVERGENCE (iteration order) | update.c:416 (`mobile_update` walks `char_list`) + db.c:2256-2257 (`create_mobile` head-inserts) | **`mobile_update` (`mud/ai/__init__.py:268`) iterates `character_registry` oldest-first; ROM walks `char_list` newest-first.** Same class as GL-040/GL-041 (INV-045). RNG-bearing sites inside the loop: shop-wealth replenishment `number_range(1,20)` ×2, scavenger 1/64 roll, wander door roll, spec_fun dispatch — all consume the shared Mitchell-Moore stream in reversed mob order vs C whenever ≥2 NPCs are live. **FIX (2.14.12):** walk reversed + extracted-skip, mirroring the GL-040/GL-041 pattern. Test: `tests/integration/test_mob_ai.py::TestMobileUpdateIterationOrder` (two shopkeepers, shop-wealth roll pairs land newest-first). | ✅ FIXED (2.14.12) |
| GL-043 | DIVERGENCE (iteration order) | update.c:1087-1092 (`aggr_update` walks `char_list` for the PC `wch`) + db.c:2256-2257 / nanny.c:757-758 (head-insert) | **`aggressive_update` (`mud/ai/aggressive.py:57`) iterates `character_registry` oldest-first; ROM walks `char_list` newest-first.** Last RNG-bearing offender in INV-045's site inventory. RNG sites inside the loop: the `number_bits(1)` aggression coin (update.c:1107) and the `number_range(0, count)` reservoir victim roll (update.c:1126) — with ≥2 non-immortal PCs live, the shared Mitchell-Moore stream is consumed in reversed watcher order vs C, desyncing every subsequent roll. **FIX (2.14.15):** reversed walk + extracted-skip (`multi_hit` can extract a later watcher mid-tick; ROM's `wch_next` saved pointer never revisits an extracted char), mirroring GL-040/041/042. Filed 2026-06-12 from the INV-045 offender inventory. Test: `tests/integration/test_mob_ai.py::TestAggressiveUpdateIterationOrder` (scripted coins [1,0]; the newest watcher's aggressor must land the attack). | ✅ FIXED (2.14.15) |
| GL-041 | DIVERGENCE (iteration order) | update.c:661-786 (`char_update` walks `char_list`) + db.c:2256-2257 / nanny.c:757-758 (head-insert) | **`char_update` main walk (`mud/game_loop.py:869`) iterates `character_registry` oldest-first; ROM walks `char_list` newest-first.** Same class as GL-040 (INV-045). RNG-bearing sites inside the loop: GL-009 wander-home `number_percent()`, per-affect `number_range(0,4)` fade rolls (`tick_spell_effects`), plague/poison damage rolls, INCAP coin-flip — all consume the shared Mitchell-Moore stream in reversed character order vs C whenever ≥2 characters are live. NOTE: the GL-034 autoquit selection compensates for the forward walk with first-wins (`autoquit_candidate is None` guard); reversing the walk requires flipping it to last-wins (plain overwrite) so `ch_quit` still lands on the OLDEST idler. Fix: iterate `list(reversed(character_registry))`, skip characters no longer in the registry (mirrors ROM's `extract_char` re-linking on mid-tick extraction), and flip the autoquit guard. **FIX (2.14.11):** walk reversed + extracted-skip; autoquit selection flipped first-wins → plain overwrite (last-wins on the newest→oldest walk = oldest idler, exactly ROM's `ch_quit = ch`). GL-034's one-per-tick test stays green. Test: `tests/integration/test_update_c_parity.py::TestCharUpdateIterationOrder`. | ✅ FIXED (2.14.11) |
| GL-040 | DIVERGENCE (iteration order) | update.c:919 (`obj_update` walks `object_list`) + db.c:2482-2483 (`create_object` head-inserts) | **`obj_update` iterates `object_registry` oldest-first; ROM walks `object_list` newest-first.** ROM `create_object` prepends every new object (`obj->next = object_list; object_list = obj`), so `obj_update`'s walk visits the most-recently-created object first. Python `mud/game_loop.py:obj_update` iterates `list(object_registry)` in append (creation) order — the reverse. Same divergence class as the `violence_update` char-order fix (FINDING-009 facet 3), where `violence_tick` already iterates `list(reversed(character_registry))`. Observable: (a) the per-affect `number_range(0,4)` level-fade roll consumes the shared Mitchell-Moore stream in reversed object order, so fade outcomes diverge from C whenever ≥2 objects carry timed affects; (b) same-tick decay messages broadcast in reverse order; (c) oldest-first means a decaying container is visited BEFORE its (newer) contents, so the recursively-extracted contents are then ticked from the stale loop snapshot — in ROM the newer contents are always visited first, while still live. (The mirror edge — an OLDER object placed into a NEWER container — is use-after-free territory in ROM C itself (`obj_next` dangling) and has no defined behavior to port.) **FIX (2.14.10):** `obj_update` iterates `list(reversed(object_registry))` and skips objects no longer in the registry (mirrors ROM's extract_obj list re-linking — a removed object is never revisited). Tests: `tests/integration/test_update_c_parity.py::TestObjUpdateIterationOrder` (newest-first same-tick decay message order + extracted-content-not-ticked). | ✅ FIXED (2.14.10) |
| GL-046 | BUG (RNG parity) | update.c:824,829-841 | **Char-side plague-spread twin of GL-045/GL-026 — three RNG desyncs in one loop.** `_char_update_tick_effects`'s plague-spread block (`mud/game_loop.py:789-819`) diverged from ROM's `for (vch = ch->in_room->people …)` infection loop three ways: **(1) duration draw count** — ROM computes `plague.duration = number_range(1, 2*plague.level)` **once** before the loop (`src/update.c:824`), so every victim infected that tick shares one duration; the port drew `number_range` per-infected-victim inside the loop (0 draws if nobody infected, N if N infected). **(2) save draw coverage** — ROM's `if (!saves_spell(…) && !IS_IMMORTAL && !IS_AFFECTED(PLAGUE) && number_bits(4)==0)` (`:832-834`) evaluates `saves_spell` (a `number_percent` draw) as the FIRST `&&` operand, so it fires for **every** room occupant incl. the carrier, immortals and already-plagued bystanders (they fail a later RNG-free term); the port pre-`continue`d those occupants with no save draw. **(3) number_bits ordering** — ROM draws `number_bits(4)` LAST (only once the save fails and the victim is non-immortal/non-plagued); the port drew it FIRST, before the save. Each divergence shifts the shared Mitchell-Moore stream for every downstream consumer in the pulse (regen, later chars' affect fades, combat) whenever a plagued char is in a populated room. **FIX (2.14.243):** duration drawn once before the loop; loop reordered to `saves_spell` → immortal → plagued → `number_bits`, mirroring ROM's operand order and draw count exactly. Surfaced 2026-07-03 by a cold-path char_update divergence hunt (same sweep as GL-045). Test: `tests/integration/test_gl046_plague_spread_rng_order.py` (2: save drawn per-occupant + number_bits gated behind it; duration drawn once and shared by co-infected victims). | ✅ FIXED (2.14.243) |
| GL-045 | BUG (RNG parity) | update.c:933 | **Object-side twin of GL-026.** `obj_update`'s per-affect level-fade (`mud/game_loop.py:_tick_object_affects`) had the fade-roll operands **swapped**: `if level > 0 and number_range(0,4) == 0` short-circuits, so a `level==0` object affect (with `duration>0`) skipped the `number_range(0,4)` draw entirely. ROM `if (number_range(0,4) == 0 && paf->level > 0)` (`src/update.c:933`) evaluates the draw as the LEFT `&&` operand → it is consumed **unconditionally** for every `duration>0` object affect; `level>0` only gates the decrement. Each skipped roll desynced the shared Mitchell-Moore stream for every downstream consumer in the pulse — the identical hazard GL-026 fixed on the *character* path (`mud/affects/engine.py:65`, whose comment documents it), left un-fixed on the object path. **FIX (2.14.220):** roll first (`fades = number_range(0,4) == 0; level = …; if fades and level > 0: …`), mirroring GL-026 and ROM's left-operand-first evaluation. Surfaced 2026-07-03 by a cold-path combat/update divergence hunt. Test: `tests/test_obj_update_affect_fade_rng.py` (2: level-0 draws exactly one `number_range(0,4)`; level>0 unchanged). | ✅ FIXED (2.14.220) |
| GL-044 | BUG (wrong RNG primitive) | update.c:498 (`(door = number_bits(5)) <= 5`) vs db.c:3541 (`number_door`) | **`mobile_update` wander drew its direction with `number_door()` instead of `number_bits(5)`.** ROM `update.c:498` rolls a SINGLE 5-bit value (`number_bits(5)`, range 0–31) and **aborts the entire wander when it exceeds 5** — so an otherwise-eligible mob only wanders on 6/32 of those ticks. Python `mud/ai/__init__.py:_maybe_wander` used `rng_mm.number_door()`, which is the *do_flee/do_mpflee* primitive (`src/db.c:3541`): `while ((door = number_mm() & 7) > 5);` — it re-rolls with a 3-bit mask until ≤5 and therefore **always returns a valid 0–5 direction, never aborting**. Two divergences: (a) the mob wandered on every gated tick (~5× too often, since it never bails on a >5 roll); (b) `number_door`'s reroll loop consumes a *variable* number of words from the shared Mitchell-Moore stream (vs exactly one for `number_bits(5)`), desyncing every downstream roll that tick — combat hits, saves, later mobs' scavenge/wander. The wrong attribution comment cited `mob_cmds.c:1274` (the flee path) rather than the actual `update.c:498` wander site. **FIX (2.14.30):** `door = rng_mm.number_bits(5); if door > 5: return`, mirroring ROM exactly. Test: `tests/integration/test_mob_ai.py::TestMobileWanderUsesNumberBits5::test_wander_aborts_when_number_bits5_exceeds_five` (door roll 10 > 5 → mob stays put). | ✅ FIXED (2.14.30) |
| GL-039 | BUG (spill gate) | update.c:1025-1026 | **`obj_update` used prototype `wear_flags` instead of runtime `wear_loc` to decide whether to spill a floating container's contents.** ROM `update.c:1025-1026` checks `obj->item_type == ITEM_CORPSE_PC \|\| obj->wear_loc == WEAR_FLOAT` — both are runtime-state checks. Python had an extra branch `elif item_type == CONTAINER and wear_flags & WEAR_FLOAT: should_spill = True` that used the prototype's wear capability flag instead of the live `wear_loc`. A floating-disc-capable container NOT currently floating (dropped in a room, `wear_loc == WEAR_NONE`) would incorrectly dump its contents into the room rather than having them destroyed alongside the container by the recursive `extract_obj` call. The message selection (`_object_decay_message`) was already correct — it uses `wear_flags` to pick "flickers and vanishes" vs "crumbles into dust", matching ROM's `CAN_WEAR(obj, ITEM_WEAR_FLOAT)` check; only the spill gate was wrong. **FIX (2.14.9):** removed the extra `elif` branch; `should_spill` now only triggers for `ITEM_CORPSE_PC` or `wear_loc == WEAR_FLOAT`, mirroring ROM exactly. `test_obj_update_spills_floating_container` in `tests/test_game_loop.py` updated to set `wear_loc=WearLocation.FLOAT` (was not set, asserting the wrong pre-fix behavior). | ✅ FIXED (2.14.9) |

---

## Integration Tests

See `tests/integration/test_update_c_parity.py`, `tests/test_advancement.py`, and `tests/integration/test_character_advancement.py`.

| Test | Gap(s) covered |
|---|---|
| `test_mana_gain_uses_mana_rate` | GL-004 |
| `test_mana_gain_furniture_uses_value4` | GL-005 |
| `test_idle_to_limbo_calls_stop_fighting` | GL-015 |
| `test_npc_wanders_home_out_of_zone` | GL-009 |
| `test_plague_tick_damages_and_spreads` | GL-011 |
| `test_poison_tick_deals_damage` | GL-012 |
| `test_incap_tick_damage` | GL-013 |
| `test_mortal_tick_damage` | GL-014 |
| `test_pit_corpse_suppresses_decay_message` | GL-018 |
| `test_char_update_keeps_spell_effect_when_same_type_affect_remains` | GL-010 |
| `test_obj_update_suppresses_duplicate_same_type_wear_off_messages` | GL-017 |
| `test_point_pulse_emits_tick_wiznet_before_updates` | GL-021 |
| `test_gain_exp_sends_level_message_before_advance_level_gains` | GL-022 |
| `test_gain_exp_logs_level_gain_before_wiznet` | GL-022 |
| `test_xp_loss_on_death` / `test_player_kill_applies_rom_death_penalty` | GL-023 |
| `test_char_update_decays_light_before_lethal_poison_tick` | GL-025 |
| `test_gl026_affect_tick_rng_consumption.py` (4 tests) | GL-026 |
| `test_gl028_mob_spell_effect_tick.py` | GL-028 |
| `test_drive_python_replay_plague_affect_divides_regen_by_eight` + smoke `char_update_regen_plague` | GL-038 |
| smoke `char_update_regen_slow` + `test_drive_python_replay_slow_affect_halves_regen_by_two` | GL-004/005 (AFF_SLOW regen ÷2) |

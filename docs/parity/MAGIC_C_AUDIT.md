# magic.c — Spell Engine Parity Audit

**Status**: 🔄 STUB — created 2026-05-24 to track CAST-001. Full
file-level audit of `src/magic.c` (3000+ lines covering `do_cast`,
`say_spell`, `cast_spell`, `obj_cast_spell`, `do_recite`/`do_brand`,
`do_dispel`, plus the per-spell handlers) is **not yet performed**.
Run `/rom-parity-audit magic.c` to populate the full gap table.

## Gap Table

| Gap ID | Severity | ROM C | Python | Description | Status |
|--------|----------|-------|--------|-------------|--------|
| `MAGIC-009` | IMPORTANT | src/magic.c:1033-1203 (`spell_cancellation`) + :231-261 (`check_dispel`/`saves_dispel`) | mud/skills/handlers.py:1868 (`cancellation`) + `_cancel_effect` (~1901) | **Cancellation strips every spell effect unconditionally — it skips ROM's per-effect `check_dispel`/`saves_dispel` roll and ignores the caster `level` entirely.** ROM `spell_cancellation` runs `if (check_dispel(level, victim, skill_lookup("<spell>")))` for each dispellable affect; `check_dispel` → `saves_dispel(dis_level, af->level, af->duration)` is a level-vs-effect roll that can **fail** (leaving the effect, decrementing `af->level--`). The ROM comment `/* unlike dispel magic, the victim gets NO save */` refers only to the **absent upfront `saves_spell`** (the wholesale wisdom save `spell_dispel_magic` grants the victim) — it does **not** mean the per-effect `saves_dispel` rolls are skipped. Python misread "NO save" as "remove all effects unconditionally": `_cancel_effect` does `target.remove_spell_effect(name)` with no roll, so `level` (computed as `caster.level + 2`, F841-unused) is never consulted. Net: a level-1 caster reliably cancels a level-50 effect (ROM: save ~95% → usually fails). The fix infra already exists and is the proven pattern — `dispel_magic` (handlers.py:3526), `cure_blindness`, `cure_poison`, `slow` all call `check_dispel(level, target, name)`; only `cancellation` diverged. **False-parity tests guard the bug:** `tests/test_spell_cancellation_rom_parity.py::test_cancellation_no_save` (level-1 caster vs level-5 effect → asserts `result is True`; ROM save≈60% → usually False), `::test_cancellation_removes_multiple_effects` (asserts `len(spell_effects)==0`), `::test_cancellation_level_bonus` (probabilistic, asserts True) — all encode unconditional removal and need RNG-controlled rewrites. Surfaced 2026-06-03 via the F841 review during the ruff-residual cleanup (`level` unused was the breadcrumb). Fix direction: `_cancel_effect` → `return check_dispel(level, target, effect_name)` (also drops its now-duplicate wear-off send, which `check_dispel` handles). **Fix (2.12.92):** `_cancel_effect` now delegates to `check_dispel(level, target, effect_name)` (the proven `dispel_magic`/`cure_blindness`/`slow` pattern), so each effect rolls `saves_dispel(dis_level, af.level, af.duration)` and can fail (decrementing `af.level`); the unconditional strip + duplicate wear-off send are gone; `level` is now used (noqa removed). RED test `tests/test_spell_cancellation_rom_parity.py::test_cancellation_respects_saves_dispel` (low-level caster vs level-50 effect, forced save → effect survives, `result is False`); the 3 false-parity legacy tests realigned to ROM via RNG control; the INV-025 PERS-masking tests force the dispel to succeed. `dispel_magic` unaffected (already used `check_dispel`). | ✅ FIXED (2.12.92) |
| `CAST-001` | CRITICAL | src/magic.c:301-536 | mud/commands/combat.py:704 (`do_cast`) | `do_cast` target resolution did not honor `skill.target` (ROM `TAR_*`). With no `arg2`, Python defaulted `target = char` for every spell, so an offensive spell cast mid-combat (`cast 'magic missile'` while `ch->fighting != NULL`) hit the caster instead of the fighting victim (`Your magic missile scratches you.`). ROM `TAR_CHAR_OFFENSIVE` defaults `victim = ch->fighting` and errors `"Cast the spell on whom?"` when not fighting; `TAR_CHAR_DEFENSIVE` defaults to self; `TAR_CHAR_SELF` accepts only self; `TAR_IGNORE` ignores arg2. Fix: dispatch on `skill.target` (`"victim"` / `"character_or_object"` → fighting default; `"friendly"` → self default; `"self"`/`"ignore"` → caster). Tests: `tests/test_skills_spells_cast_listing.py::test_do_cast_offensive_no_target_defaults_to_fighting_victim`, `::test_do_cast_offensive_no_target_no_fight_errors`. | ✅ FIXED (2.8.76) |
| `MAGIC-002` | MEDIUM | src/magic.c:753-777 (`spell_armor`) | mud/skills/handlers.py:1335 (`armor`) | **Affect spells emit no ROM success message when cast through `do_cast`.** Surfaced by the differential harness `affect_armor` scenario (FINDING-015). ROM `spell_armor` sends `"You feel someone protecting you."` to the victim on success (and `act("$N is protected by your magic.")` to the caster when `ch != victim`); the Python `armor` handler applies the −20 AC affect but is **silent** on success. Since `do_cast` is silent on a successful cast (`CAST`/FINDING-013 — all output comes from the spell function), the line is dropped entirely. The affect itself is correct (affects/eff_ac/mana all converge in the differential); only the player-facing message diverges. The Python already-affected branch also sends the non-ROM `"They are already protected."` vs ROM's `"You are already armored."`. **Class, partially:** `bless` (handlers.py:1484) was likewise **genuinely silent** on success — fixed (2.11.21, mirrors `spell_bless` `src/magic.c:836-865`: "You feel righteous." TO_VICT, "You grant $N the favor of your god." TO_CHAR cross-target, "You are already blessed." / `act("$N already has divine favor.")` already-affected incl. the `POS_FIGHTING` quirk). The object-target branch of `spell_bless` (`TAR_OBJ_CHAR_DEF` obj case, `src/magic.c:788-834`) is **now implemented** — INV-030 (2.11.55): already-blessed check, evil-dispel branch, clean-object affect via `affect_to_obj`, and saving_throw -= 1 for worn objects. CAST-006 (2.11.52) already routes defensive object targets through `get_obj_carry`. **Correction to the prior framing:** `shield`/`sanctuary` are *not* silent — they deliver via the divergent `char.messages.append` mailbox channel (works for connected PCs via the connection drain, but bystander room broadcasts arrive late instead of immediately, and the differential harness reading the async descriptor sees them as empty). That is a **wrong-channel** divergence, not silent-on-success; tracked separately as **MAGIC-003** (INV-001 SINGLE-DELIVERY family). | ✅ FIXED (armor 2.11.20, bless 2.11.21) — `shield`/`sanctuary`/`weaken`/`blindness` channel residual → MAGIC-003 |
| `MAGIC-003` | LOW | src/magic.c:4296-4297 (`spell_sanctuary` `send_to_char` + `act` TO_ROOM), :4326-4327 (`spell_shield`), :888-889 (`spell_blindness`), :4580-4581 (`spell_weaken`) | mud/skills/handlers.py: `shield` (7113), `sanctuary` (7071), `weaken` (7907 room-broadcast leg), `blindness` (1505) | **Affect spells deliver their success / room-broadcast messages via the divergent `char.messages.append` mailbox channel instead of the canonical `push_message` (`_send_to_char`) / `broadcast_room`.** Surfaced while scoping the MAGIC-002 sweep: most affect-buff handlers (armor, all `detect_*`, fly, frenzy, giant_strength, haste, …) already use `_send_to_char`, but `shield`/`sanctuary` (self + room legs), `blindness` (self + room legs), and `weaken` (room leg only — self leg is correct) still call `occupant.messages.append(...)` / `target.messages.append(...)` directly. Per `mud/utils/messaging.py` (DUPL-002) and AGENTS.md "Message Delivery", `char.messages` is a **test/disconnected fallback only**; a connected PC receives via the async `send_to_char` task. The current code still *delivers once* to a connected PC (the `connection.py` read loop drains `char.messages` after each command), so it is not double-delivery (INV-001) and not silent — but a TO_ROOM broadcast to a connected *bystander* arrives on that bystander's next prompt rather than immediately, and the differential harness (reading the descriptor) sees nothing. Fix (2.11.24): each leg now routes through `_send_to_char` (self/vict + the per-occupant room loop), mirroring the `fly`/`giant_strength` pattern — `shield` (self + room + already-affected), `sanctuary` (self + room + already-affected), `blindness` (self + room), `weaken` (room leg; self leg was already correct). `_send_to_char` (`send_to_char_buffered`) keeps the mailbox fallback for disconnected/test chars, so the existing mailbox-reading tests stay green. Connected-PC test: `tests/integration/test_magic_003_affect_message_channel.py` (asserts the line lands on the connection's immediate async channel with the mailbox left empty; mailbox-fallback half pre-passes — the channel half fails before the fix). INV-001 SINGLE-DELIVERY family. | ✅ FIXED (2.11.24) |
| `CAST-002` | MEDIUM | src/magic.c:514-535 (`TAR_OBJ_CHAR_DEF`) vs :466-512 (`TAR_OBJ_CHAR_OFF`); src/const.c (per-spell `TAR_*`) | mud/commands/combat.py (`do_cast` target dispatch) | **`do_cast` maps the `"character_or_object"` skill-target string to the *offensive* default (fighting victim; `"Cast the spell on whom?"` when not fighting), but it conflates two distinct ROM target types.** The 5 skills.json spells tagged `character_or_object` split by ROM `TAR_*`: **defensive** `TAR_OBJ_CHAR_DEF` (no-arg → **self**, `src/magic.c:514-519`) = `bless`, `invisibility`, `remove curse`; **offensive** `TAR_OBJ_CHAR_OFF` (no-arg → fighting victim, error if not fighting, `src/magic.c:466-473`) = `curse`, `poison`. Python applies the offensive default to all five, so a no-arg `cast bless` / `cast invis` / `cast 'remove curse'` errors `"Cast the spell on whom?"` instead of defaulting to self. Surfaced while writing the MAGIC-002 bless tests (the self-cast had to be exercised at the handler level because `do_cast(caster, "bless")` errors). **Fix (2.11.22):** split the JSON target vocabulary — `convert_skills_to_json.py` now emits `defensive_character_or_object` for `TAR_OBJ_CHAR_DEF` and `offensive_character_or_object` for `TAR_OBJ_CHAR_OFF` (restoring ROM's 1:1 `TAR_*` mapping); `data/skills.json` updated for the 5 spells; `do_cast` routes the defensive string with `friendly` (self default, `src/magic.c:514-519`) and the offensive string with `victim` (fighting default, byte-identical to before). `mob_cmds.py` `_TARGET_STRINGS` maps both new strings (ROM `do_mpcast` collapses DEF/OFF, `src/mob_cmds.c:1060-1065`). Tests: `tests/test_skills_spells_cast_listing.py::test_do_cast_defensive_obj_char_no_target_defaults_to_self` (+ offensive-unchanged guard). The TAR_OBJ_CHAR_DEF/OFF object-target legs and the distinct OFF no-fight wording remain deferred (Scope Notes; CAST-003). CAST-001 sibling. | ✅ FIXED (2.11.22) |
| `CAST-003` | LOW | src/magic.c:471 (`TAR_OBJ_CHAR_OFF` no-target error) vs :376 (`TAR_CHAR_OFFENSIVE`) | mud/commands/combat.py (`do_cast` offensive branch) | **The offensive object/char no-fight error uses the wrong ROM wording.** ROM `TAR_OBJ_CHAR_OFF` (`curse`/`poison`) errors `"Cast the spell on whom or what?"` (`src/magic.c:471`) on a no-arg cast with no `ch->fighting`, distinct from `TAR_CHAR_OFFENSIVE`'s `"Cast the spell on whom?"` (`src/magic.c:376`). Python's `do_cast` routes `offensive_character_or_object` through the same branch as `victim`, so both emit `"Cast the spell on whom?"` — `curse`/`poison` get the `whom?` wording instead of `whom or what?`. Surfaced while de-conflating the two `TAR_OBJ_CHAR_*` types for CAST-002; left byte-identical there to keep CAST-002 a clean single-gap commit. Fix (2.11.23): the offensive object/char no-fight branch in `do_cast` now returns its own ROM error string `"Cast the spell on whom or what?"`; the `victim` (`TAR_CHAR_OFFENSIVE`) branch keeps `"Cast the spell on whom?"`. The `test_do_cast_offensive_obj_char_no_target_no_fight_still_errors` guard was tightened from a `whom` substring check to assert the exact wording. CAST-002 sibling. | ✅ FIXED (2.11.23) |
| `CAST-004` | MEDIUM | src/magic.c:449-465 (`TAR_OBJ_INV`) | mud/commands/combat.py (`do_cast` object-target branch) | **`do_cast` did not route object-only spell targets (`identify`, `enchant armor`, `enchant weapon`, `fireproof`, `create water`, `detect poison`, `recharge`).** ROM `TAR_OBJ_INV` requires an explicit target name and resolves it via `get_obj_carry(ch, target_name, ch)`, erroring `"What should the spell be cast upon?"` with no arg and `"You are not carrying that."` when the object is not found (`src/magic.c:449-465`). Python previously fell through to `target = char` for target type `"object"`. Fix (2.11.52): `do_cast` now dispatches `skill_target_type == "object"` through its own branch that validates `target_name`, resolves via `get_obj_carry`, and passes the object to the spell handler. Tests: `tests/integration/test_do_cast_object_target.py::TestCastObjectTarget` (3/3). | ✅ FIXED (2.11.52) |
| `CAST-005` | MEDIUM | src/magic.c:502-506 (`TAR_OBJ_CHAR_OFF` object fallback) | mud/commands/combat.py (`do_cast` offensive branch) | **`do_cast` offensive character/object branch did not fall back to object search.** ROM `TAR_OBJ_CHAR_OFF` first tries `get_char_room(ch, target_name)`; if no character matches, it tries `get_obj_here(ch, target_name)` (`src/magic.c:502-506`), erroring `"You don't see that here."` only when neither is found (`:536`). Python previously returned `"They aren't here."` immediately on character lookup failure, making offensive object-targeted spells like `curse sword` and `poison food` unreachable from the command surface. Fix (2.11.52): when a named target resolves to no character and `skill_target_type == "offensive_character_or_object"`, `do_cast` now falls back to `get_obj_here` before erroring. The ROM `"You don't see that here."` wording is used. Tests: `tests/integration/test_do_cast_object_target.py::TestCastOffensiveObjectFallback` (3/3). | ✅ FIXED (2.11.52) |
| `CAST-006` | MEDIUM | src/magic.c:525-529 (`TAR_OBJ_CHAR_DEF` object fallback) | mud/commands/combat.py (`do_cast` defensive branch) | **`do_cast` defensive character/object branch did not fall back to object search.** ROM `TAR_OBJ_CHAR_DEF` first tries `get_char_room(ch, target_name)`; if no character matches, it tries `get_obj_carry(ch, target_name, ch)` (`src/magic.c:525-529`), erroring `"You don't see that here."` only when neither is found (`:532`). Python previously returned `"They aren't here."` immediately on character lookup failure, making defensive object-targeted spells like `bless sword`, `invisibility cloak`, `remove curse potion` unreachable from the command surface. Fix (2.11.52): when a named target resolves to no character and `skill_target_type == "defensive_character_or_object"`, `do_cast` now falls back to `get_obj_carry` before erroring. The ROM `"You don't see that here."` wording is used. Tests: `tests/integration/test_do_cast_object_target.py::TestCastDefensiveObjectFallback` (4/4). | ✅ FIXED (2.11.52) |
| `CAST-007` | CRITICAL | src/magic.c:395-413 (`TAR_CHAR_OFFENSIVE`), :481-495 (`TAR_OBJ_CHAR_OFF`) | mud/commands/combat.py (`do_cast`) | **`do_cast` did not enforce PK safety gates (`is_safe`, `is_safe_spell`, `check_killer`) or the AFF_CHARM master gate for offensive spell targets.** ROM `TAR_CHAR_OFFENSIVE` (lines 400-412) checks `is_safe(ch, victim)` and calls `check_killer(ch, victim)` for non-NPC casters, then blocks charmed casters from attacking their master (`AFF_CHARM && ch->master == victim`). ROM `TAR_OBJ_CHAR_OFF` (lines 484-495) mirrors these with `is_safe_spell` instead of `is_safe`, same `check_killer` and charm gate. Python's `do_cast` previously resolved the target and immediately deducted mana, bypassing all three gates. Fix (2.11.53): after target resolution and before mana deduction, `do_cast` now checks: (1) `is_safe` / `is_safe_spell` for PC casters against character targets, returning `"Not on that target."` on failure; (2) `check_killer` for PC casters (which also strips charm via `stop_follower` when the resolved victim is a PC); (3) the AFF_CHARM master gate for all casters (`is_safe`/`is_safe_spell` and `check_killer` are PC-only per ROM's `!IS_NPC(ch)` guard; the charm gate at :408-412 is unconditional). Object targets (`target_obj is not None`) bypass all three gates per ROM — the safety checks and charm gate only apply to character targets. Tests: `tests/integration/test_do_cast_pk_gates.py` (17 tests: safe-room blocking, check_killer flagging, NPC skip, non-clan skip, charm gate for NPC/PC casters, charm stripping by check_killer for PC-on-PC, defensive spell immunity, object target bypass). | ✅ FIXED (2.11.53) |
| `CAST-008` | IMPORTANT | src/magic.c:551-558 (success/fail mana deduction) | mud/commands/combat.py (`do_cast`) | **`do_cast` over-charged mana on a failed cast.** ROM deducts EITHER `ch->mana -= mana / 2;` on failure (line 553) OR `ch->mana -= mana;` on success (line 557) — never both, and mana is NOT touched before the concentration roll. Python deducted the full `mana_cost` unconditionally before the roll, then an ADDITIONAL `c_div(mana_cost, 2)` in the failure branch, so a failed cast cost 1.5× — e.g. a level-1 magic missile (cost 50) took 75 mana on failure (100→25) instead of 25 (100→75). Successful casts coincidentally matched (full cost). Surfaced via in-game play ("magic missile costs 75 of my 100 mana" was a failed cast over-deducting); originally mis-triaged as ROM-correct until the deduction ORDER was read. Fix: removed the upfront deduction; failure now deducts `mana_cost // 2` only and success deducts the full `mana_cost`, mirroring ROM's if/else. Tests: `tests/integration/test_spell_casting.py::TestCastManaDeductionCAST008` (failed cast = mana/2, successful cast = full). | ✅ FIXED |
| `CAST-009` | IMPORTANT | src/magic.c:551-554 (failure branch) | mud/commands/combat.py (`do_cast`) | **Spells only improve on success; a failed cast never trains the skill.** ROM calls `check_improve(ch, sn, FALSE, 1)` inside the failure branch (`magic.c:553`) — failing a spell is a valid path to improving it (core progression). Python's failure branch `return`s "You lost your concentration." before reaching the lone `_check_improve(...)` call, which only runs on success; the `success` arg passed there is consequently always `True` (vestigial). Surfaced 2026-05-31 by advisor review while closing CAST-008 (same function); pre-existing, NOT introduced by CAST-008. Fix: call `skill_registry._check_improve(char, skill, skill.name, False)` in the failure branch before returning. | ✅ FIXED (2026-06-01) — `_check_improve(char, skill, skill.name, False)` now runs in the concentration-lost branch before the half-mana deduction (`mud/commands/combat.py` `do_cast`); test `tests/integration/test_cast_009_failed_cast_improves_skill.py`. |
| `CAST-010` | IMPORTANT | src/magic.c:547 — `WAIT_STATE(ch, skill_table[sn].beats)` | mud/commands/combat.py (`do_cast`) | **`do_cast` cast-lag used a flat `PULSE_VIOLENCE` (12) instead of the spell's own `beats`.** ROM applies `WAIT_STATE(ch, skill_table[sn].beats)` before the success roll — the per-spell cast lag. Spell beats vary: 81 spells are 12, but **34 differ** (22 at 24, 7 at 18, 4 at 36, 1 at 20 — e.g. fly=18, enchant armor=24, mass healing=36) and 19 are 0. Python applied a flat `skill_registry._apply_wait_state(char, get_pulse_violence())` (=12) for every spell, so any spell with beats≠12 got the wrong cast lag (slower spells cast too fast; beats-0 spells over-lagged). ROM uses the **raw** beats — no HASTE/SLOW adjustment for casting — so the fix reads `skill.beats` directly (NOT `_compute_skill_lag`, which would halve under HASTE). `_apply_wait_state` treats beats=0 as a UMAX no-op, matching ROM `WAIT_STATE(ch, 0)`. Surfaced 2026-06-13 by the ROM-WAIT_STATE-site cross-check (sibling sweep of SAVE-001/PASSWORD-001). **Fix (2.14.43):** `spell_beats = int(getattr(skill, "beats", 0) or getattr(skill, "lag", 0) or 0)`; `_apply_wait_state(char, spell_beats)`. Test: `tests/integration/test_cast010_wait_state_uses_spell_beats.py` (fly beats==18). | ✅ FIXED (2.14.43) |
| `CAST-011` | IMPORTANT | src/magic.c:544-545 — `if (str_cmp(skill_table[sn].name, "ventriloquate")) say_spell(ch, sn)` | mud/commands/combat.py (`do_cast`) | **`do_cast` never broadcast the spell incantation — casting was silent to the room.** ROM calls `say_spell(ch, sn)` for *every* spell except `ventriloquate`, broadcasting `"$n utters the words, '...'"` to the room — actual words to same-class observers, garbled syllables to others (`src/magic.c:199-204`) — fired after the mana check and before WAIT_STATE/the success roll. The Python port had a complete `broadcast_spell_words` helper (`mud/skills/say_spell.py`, with class-based garbling + INV-001 single-delivery) but **`do_cast` never called it** — only a test did. So other players never saw anyone casting. **Fix (2.14.44):** `do_cast` now calls `broadcast_spell_words(char, skill.name)` before the WAIT_STATE block, gated by `skill.name.lower() != "ventriloquate"` (ROM's `str_cmp` exclusion). Placed after the PK gates so a safety-blocked cast doesn't broadcast (matches ROM, where PK gates during target resolution return before say_spell). The caster receives nothing (broadcast targets other occupants), preserving FINDING-013's silent-on-success caster contract. Surfaced 2026-06-13 probing the do_cast flow after the CAST-010 WAIT_STATE fix. Test: `tests/integration/test_cast011_say_spell_broadcast.py` (2: normal spell broadcasts, ventriloquate excluded). | ✅ FIXED (2.14.44) |
| `CAST-012` | IMPORTANT | src/magic.c:398-402 (`TAR_CHAR_OFFENSIVE` `is_safe`), src/fight.c:1018-1124 (`is_safe`) | mud/commands/combat.py (`do_cast`) | **`do_cast`'s offensive `is_safe` gate dropped ROM's context line (INV-050).** ROM `do_cast` runs `if (is_safe(ch,victim) && victim != ch) { send_to_char("Not on that target."); return; }`, and ROM `is_safe` writes its OWN rejection line via `send_to_char`/`act` BEFORE returning TRUE (e.g. "The shopkeeper wouldn't like that.", "I don't think Mota would approve.", "Not in this room.") — so a blocked offensive cast shows TWO lines: the is_safe context line, then "Not on that target.". CAST-007 wired the gate through the **silent bool** `combat.safety.is_safe` (writes no message), surfacing only "Not on that target." and inheriting the bool's bidirectional over/under-block (ROOM_SAFE for all victims, missing the PC-vs-PC clan ladder, etc.). **Fix (2.14.110):** converged the `TAR_CHAR_OFFENSIVE` ("victim") branch onto the faithful mirror `_kill_safety_message` (the do_bash FIGHT-070 / do_consider CONSIDER-002 pattern) — emits `f"{safety_message}\nNot on that target."`. The `TAR_OBJ_CHAR_OFF` (`is_safe_spell`) branch is **silent in ROM** (`src/fight.c:1126-1218` sends no messages) and is left as a bare override. Third INV-050 caller converged. Two CAST-007 tests asserting the silent-bool under-block (KILLER-flag / charm-strip on a *non-clan* PC victim, which ROM is_safe blocks before check_killer) were ROM-corrected to use a clan victim within 8 levels. Test: `tests/integration/test_do_cast_pk_gates.py::TestCastOffensiveIsSafeContextMessage` + the ROM-corrected SafeRoom/CheckKiller/CharmGate tests. | ✅ FIXED (2.14.110) |
| `CAST-013` | IMPORTANT | src/magic.c:341-345 (`if (ch->position < skill_table[sn].minimum_position)` → "You can't concentrate enough."), src/merc.h:1951 (`minimum_position` field), src/const.c skill_table (per-spell POS_*) | mud/commands/combat.py (`do_cast`), mud/skills/{metadata,registry}.py, mud/models/skill.py, mud/scripts/convert_skills_to_json.py | **`do_cast` gated on a FLAT `POS_FIGHTING` for every spell instead of each spell's own `minimum_position`.** ROM checks `ch->position < skill_table[sn].minimum_position`; the per-spell value is `POS_STANDING` for utility/buff spells (armor, bless, all detect_*, charm person, create_*, cure_disease/cure_poison, sanctuary's siblings — 55 spells) and `POS_FIGHTING` for offensive/combat spells (72). Given the `cast` command-gate is `POS_FIGHTING` (INTERP-031), by the time the per-spell gate runs the caster is FIGHTING(7) or STANDING(8), so the **observable** divergence is exactly: **Python let a fighting character cast POS_STANDING utility spells that ROM blocks** with "You can't concentrate enough." (the RESTING/SLEEPING per-spell minimums are below the command gate and thus moot; word_of_recall POS_RESTING is the only real one and is unreachable via the command path). **Fix (2.14.199):** added `Skill.minimum_position: Position` (default FIGHTING — a safe backstop identical to the old flat gate, so an unmapped spell can never regress an offensive cast); `ROM_SKILL_MIN_POSITION` map in `metadata.py` derived from `const.c` via `parse_skill_table` (POS_* token → `Position` enum, never a hardcoded int per AGENTS.md) + hand-added cancellation/harm=FIGHTING (the CONST-009 parser-skipped pair); `parse_skill_table` now emits `minimum_position`; `registry.load` sets it per skill; `do_cast` gates on `skill.minimum_position`. `data/skills.json` intentionally NOT regenerated (a regen drops cancellation/harm and clobbers CONST-008's target=friendly). Completeness anti-drift guard re-parses const.c and asserts every spell matches. Surfaced 2026-06-20 from the SESSION_STATUS "per-spell min_position" candidate. Test: `tests/integration/test_spell_casting.py::TestCast013PerSpellMinPosition` (4 — fighting blocked on STANDING spell, fighting allowed on FIGHTING spell, standing allowed on STANDING spell, full const.c completeness guard). | ✅ FIXED (2.14.199) |
| `MAGIC-008` | MEDIUM | src/magic.c:3650-3659 (`spell_invisibility`), :3837-3847 (`spell_mass_invis`) | mud/skills/handlers.py:`invis` (~5654), `mass_invis` (~6048) | **invis broadcast-order: ROM emits the fade-out room line BEFORE applying `AFF_INVISIBLE`.** ROM `spell_invisibility` does `act("$n fades out of existence.", victim, TO_ROOM)` (`:3650`) *then* `affect_to_char` (`:3659`) *then* the self `send_to_char`. The Python `invis` handler applied the affect first, then broadcast — harmless while the name was baked into the string (visibility ignored), but once the room line renders through per-recipient PERS (`act_to_room`, INV-027) the now-invisible actor would wrongly mask to "someone". Surfaced 2026-06-01 closing the INV-025 single-actor PERS sweep (the conversion exposed the latent ordering bug; two legacy invis tests asserting the real name flipped to "someone"). `mass_invis` already broadcasts before `apply_spell_effect`, so only `invis` needed reordering. Fix: `invis` now broadcasts → applies affect → self-messages, matching ROM; a lit-room witness sees the real name. Regression: `tests/integration/test_inv025_spell_self_effect_pers_masking.py::TestInvisBroadcastOrder` (invis + mass_invis broadcast-before-affect). | ✅ FIXED (2.12.30) |
| `MAGIC-004` | LOW | src/magic.c:1244-1249 (`spell_chain_lightning` first strike TO_ROOM/TO_CHAR/TO_VICT), :1270 (`"The bolt arcs to $n!"` tmp_vict TO_ROOM), :1295 (caster-hit whoops TO_ROOM) | mud/skills/handlers.py:`chain_lightning` (~2107, ~2143, ~2173) | **chain_lightning baked actor names instead of rendering `$n`/`$N` per-recipient (INV-027).** **ROM-ref + scope correction (verified 2026-06-01):** the original row was materially wrong — it claimed Python "collapses ROM's 3-way split" and "mis-targets the bolt-arc line", and described the first strike as `TO_NOTVICT` (excludes both). The real ROM `:1244` is **`TO_ROOM`** (excludes only `ch`; the victim DOES receive the third-person "arcs to $N" line, on top of its separate TO_VICT "hits you!"), and the Python port already had all three legs with the correct bounce actor (`occupant`=tmp_vict). The only real divergence was that every leg baked `caster`/`victim` names rather than rendering `$n`/`$N` through PERS, leaking the names of actors a witness cannot see. Tokens are all mid-sentence, so a masked actor renders **lowercase** "someone" (`act` only caps the buffer's first letter). **Fix (2.12.40):** converted the 5 token-bearing legs in `handlers.py:chain_lightning` — first-strike TO_ROOM via `act_to_room("$n's hand and arcs to $N", caster, arg2=target)` (no `exclude`, so the victim is still a recipient per ROM `TO_ROOM`), first-strike TO_CHAR/TO_VICT via `act_format`, chain-bounce TO_ROOM `"The bolt arcs to $n!"` and caster-hit whoops via `act_to_room`. No-token legs (bounce/fizzle/grounds-out/struck-self) left as-is. Removed dead `caster_name`/`victim_name` locals. Test: `tests/integration/test_magic004_chain_lightning_pers_masking.py` (4 — first-strike room/vict mask an invisible caster, first-strike room masks an invisible victim, bounce masks an invisible bounce target; all lowercase "someone"). | ✅ FIXED (2.12.40) |
| `MAGIC-005` | MEDIUM | src/magic.c:3981 (`spell_poison` object branch — `act("$p is coated with deadly venom.", ch, obj, NULL, TO_ALL)`) | mud/skills/handlers.py:`poison` object branch (~4008, ~4111) | **poison_weapon: wrong message text, subject, and recipient set.** ROM's poison-object branch emits a single `act("$p is coated with deadly venom.", ch, obj, NULL, TO_ALL)` — subject is the OBJECT (`$p`, can_see_obj-masked), delivered to caster AND room. Python's current text is actually ROM-correct — it emits `"{obj} is coated with deadly venom."` / `"{obj} is infused with poisonous vapors."` (`handlers.py:poison` ~6563/6503), pre-capped. **Stale-row correction (2026-06-01, MAGIC-007 review):** the original "invented text/`$n` subject" claim no longer matches the code; the ONLY remaining divergence is the same object-`$p` masking as MAGIC-007 — the room leg goes through the baked-name `_act_room` instead of `act_to_room("$p ...", ch, arg1=obj)`, so an invisible/blind-witness object name leaks. Convert both poison-object room legs to `act_to_room` with `$p`. Surfaced 2026-06-01 during the INV-025 sweep. **Fix (2.12.33):** both room legs (`handlers.py:poison` weapon `:3981` + food/drink `:3946`) converted from baked-name `_act_room` to `act_to_room("$p is coated with deadly venom."/"$p is infused with poisonous vapors.", caster, arg1=obj, exclude=caster)` — a blind/dark-room/invisible-object witness now sees "Something …". Poison does NOT set ITEM_INVIS, so the caster `_send_to_char` leg keeps the baked short_descr (weapon leg capped per ACT-CAP-002, food leg uncapped — both unchanged, MAGIC-007 precedent). Existing assertions `test_skills_debuffs.py` 153/154/190/191 still pass (sighted witness + caster). Test: `tests/integration/test_magic005_poison_object_pers_masking.py` (blind witness masks weapon + food to "Something …", sighted witness sees the short_descr). | ✅ FIXED (2.12.33) |
| `MAGIC-006` | LOW | src/magic.c:3921 (`spell_plague` — `act("$n screams in agony as plague sores erupt from $s skin.", victim, TO_ROOM)`) | mud/skills/handlers.py:`plague` (~6493) | **plague room line uses "their skin" instead of ROM `$s skin` and bakes the victim name.** ROM renders the possessive via `$s` (his/her/its) and `$n` per-recipient through PERS. Python bakes `_character_name(target)` + the literal "their skin", so it neither masks an invisible victim (INV-027) nor matches ROM's gendered possessive. Surfaced 2026-06-01 during the INV-025 sweep (excluded as a pronoun/text divergence). **Fix (2.12.34):** `handlers.py:plague` room leg converted from the baked `_act_room(f"{name} ... their skin.")` to `act_to_room("$n screams in agony as plague sores erupt from $s skin.", target, exclude=target)` — `$n` masks an invisible victim to "Someone" per recipient (INV-027), `$s` renders the gendered possessive (his/her/its). Existing `test_skills_debuffs.py::test_plague_applies_affect_and_messages` updated "their"→"its" (a sexless test character defaults to `Sex.NONE` → "its", the ROM-faithful neuter). Test: `tests/integration/test_magic006_plague_pronoun_masking.py` (male→"his", female→"her", invisible victim→"Someone"). | ✅ FIXED (2.12.34) |
| `MAGIC-007` | LOW | src/magic.c various `act("$p ...", ch, obj, ..., TO_ROOM/TO_ALL)` object-subject spell lines | mud/skills/handlers.py: acid/fire inventory-burn, portal/nexus, pick-lock, recharge, enchant_armor/enchant_weapon, bless object, fireproof, remove_curse object + inventory loop | **INV-025/INV-027 object-`$p` PERS-masking follow-up — remainder of the single-actor sweep.** These spell room lines baked an OBJECT `short_descr` (and sometimes the actor name) into the string instead of rendering ROM's `$p`/`$n` per-recipient through `act_to_room` (which applies `can_see_obj`/`can_see` masking). Fix (2.12.31): converted **every visible-object `$p` room leg** to `act_to_room("$p ..."/"$n ... $p", actor, arg1=obj, exclude=...)`, each verified against its exact ROM `act()` format string — acid/fire effect (`src/magic.c:3156/3182/3214/3240`), bless object pale-blue + holy-aura (`:808/:829`), enchant_armor (`:2358/:2370/:2418/:2427`), enchant_weapon (`:2556/:2566/:2614/:2623` — TO_ROOM leg preserves ROM's `:2557` `"explodeds!"` typo), fireproof (`:2785`), recharge (`:4158/:4195`), remove_curse object (`:4233`) + per-item `$n's $p` loop (`:4268`), pick-lock (`src/act_move.c:907/:945`), portal (`src/magic2.c:101/:155/:171`). Caster `_send_to_char` legs stay on the baked short_descr (the caster can always see a visible object they target). **Excluded** (filed separately): object-invis `"$p fades out of sight"` (`:3640`, TO_ALL — the object is genuinely ITEM_INVIS at render time so it masks the caster too → behaviorally distinct, `MAGIC-010`); poison object (`:3946/:3981` → `MAGIC-005`); plague (`:3921` → `MAGIC-006`); chain_lightning (`MAGIC-004`). Test: `tests/integration/test_magic007_object_pers_masking.py` (fireproof — blind witness masks to "something", sighted witness sees the short_descr). | ✅ FIXED (2.12.31) |

| `MAGIC-010` | LOW | src/magic.c:3640 (`spell_invisibility` object branch — `act("$p fades out of sight.", ch, obj, NULL, TO_ALL)`) | mud/skills/handlers.py:`invis` object branch (~5631) | **object-invis `$p` masks the caster too, not just the room — both legs still bake the short_descr.** ROM sets `ITEM_INVIS` via `affect_to_obj` (`:3638`) *before* the `TO_ALL` act (`:3640`), so at render time `can_see_obj(ch, obj)` is FALSE for any char without detect-invis/holylight (`src/handler.c` `can_see_obj`) — the caster AND every witness see `"Something fades out of sight."`, not the object name. Python bakes `_object_short_descr(obj)` into a shared pre-capped `message` for both the `_send_to_char(caster, ...)` leg and the `_act_room` room leg, so it leaks the name to everyone. Unlike the MAGIC-007 visible-object sites (where the caster always sees a visible target, so the baked caster leg is fine), here the object is genuinely invisible — both legs must render `$p` through `can_see_obj`. Fix: render the caster leg via `act_format(recipient=caster, actor=caster, arg1=obj)` (caps + masks) and the room leg via `act_to_room`. **Blocks updating** `tests/test_skills_buffs.py::test_invis_handles_objects_and_characters` (584/585) and `::test_invis_object_wears_off` (618/619) + `tests/integration/test_act_cap_002_room_broadcast.py` (123), which currently assert the baked name "Mysterious gem fades out of sight" — ROM-incorrect for a witness/caster without detect-invis. Surfaced 2026-06-01 by advisor review while closing MAGIC-007 (excluded from that commit as behaviorally distinct). **Fix (2.12.32):** both legs now render `$p` per-recipient — caster via `act_format("$p fades out of sight.", recipient=caster, actor=caster, arg1=obj)`, room via `act_to_room(..., arg1=obj, exclude=caster)`; dead `message`/`capitalize_act_line` locals dropped. **Also folded in `:3627`** (`act("$p is already invisible.", ch, obj, NULL, TO_CHAR)` early-out, same `ITEM_INVIS` branch / same `can_see_obj` root cause) → `_send_to_char(caster, act_format("$p is already invisible.", ...))`. Updated the pinned baked-name assertions: `test_skills_buffs.py` 584/585→"Something fades out of sight.", 589→"Something is already invisible.", 618/619→"Something fades out of sight." `test_act_cap_002_room_broadcast.py:123` only asserts `.isupper()` so "Something…" still passes (the row over-stated that blocker). Test: `tests/integration/test_magic010_object_invis_pers_masking.py` (3 — caster+witness mask, detect-invis witness sees short_descr, already-invisible early-out masks). | ✅ FIXED (2.12.32) |
| `MAGIC-015` | LOW | src/magic.c:3598 (`spell_infravision` — `act("$n's eyes glow red.", ch, NULL, NULL, TO_ROOM)`) | mud/skills/handlers.py:`infravision` (~5578) | _(Was duplicate-numbered `MAGIC-009`; renumbered to `MAGIC-015` 2.12.92 to resolve the ID collision with the cancellation gap above — the two unrelated gaps shared `MAGIC-009`.)_ **infravision room line uses the target as the `$n` actor instead of ROM's `ch` (caster).** ROM passes `ch` (the caster) as the `act()` actor, so `$n` renders the *caster's* name/visibility; the affect goes to `victim` and `"Your eyes glow red."` is sent to `victim` separately. Python passes `target` to `act_to_room(... "$n's eyes glow red.", target, ...)`, so when the spell is cast on **another** character (`caster != target`, valid for `TAR_CHAR_DEFENSIVE`) the room sees the *target's* name where ROM shows the *caster's*. Self-cast (the common case) is unaffected since `ch == victim`. ROM also broadcasts before `affect_to_char` (order), but the affect is `AFF_INFRARED` (not visibility-changing), so order is immaterial here. Surfaced 2026-06-01 by advisor review while closing the INV-025 single-actor PERS sweep — the conversion preserved the pre-existing `target` actor and cited `:3598` without noticing ROM uses `ch`. Fix: pass `caster` as the `act_to_room` actor. **Fix (2.12.93):** room leg now `act_to_room(room, "$n's eyes glow red.", caster, exclude=caster)` — actor is the caster (ROM `ch`), excluding only the caster, so a cross-cast renders the caster's name/visibility and the victim (no longer excluded) also receives the room line per ROM `TO_ROOM`. Broadcast moved before the victim's `_send_to_char` to mirror ROM order. Self-cast unchanged. Test: `tests/integration/test_magic015_infravision_caster_actor.py` (cross-cast room shows caster, victim gets both lines; self-cast). | ✅ FIXED (2.12.93) |
| `MAGIC-016` | LOW | src/magic.c:763 (`spell_armor` — `act("$N is already armored.", …)`) + :776 (`act("$N is protected by your magic.", …)`) | mud/skills/handlers.py:`armor` (~1353,1366) | **`spell_armor`'s cross-target TO_CHAR lines baked the victim's keyword `name` instead of ROM's `$N` PERS render.** ROM `$N` = `PERS(victim, ch)` = the NPC **short_descr** (capitalized buf[0]); the Python `armor` handler used `f"{_character_name(target)} is already armored./is protected by your magic."` where `_character_name` returns the keyword `name` for an NPC. So casting armor on an NPC "goblin"/short_descr "a green goblin" showed "goblin is protected by your magic." vs ROM "A green goblin is protected by your magic." (the self-cast "You are already armored."/"You feel someone protecting you." lines were already correct, PC victims render identically since PERS=name). Found applying the `act()`-rendering lens to the spell handlers. **Fix (2.14.60):** both via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`. Test: `tests/integration/test_magic_002_armor_message.py::test_magic016_armor_cross_target_npc_uses_pers_shortdescr_capitalized`. | ✅ FIXED (2.14.60) |
| `MAGIC-017` | LOW | src/magic.c `spell_shield` — `act("$N is already protected by a shield.", ch, NULL, victim, TO_CHAR)` + `act("$n is surrounded by a force shield.", victim, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`shield` (~7350, ~7372) | **Same class as MAGIC-016 (TO_CHAR baked name) + MAGIC-014 (hand-rolled TO_ROOM loop) in one handler.** The cross-target "already protected" line baked the keyword `name`; the success room broadcast used a hand-rolled `for occupant in room.people` loop baking `target.name` with a "Someone" fallback (so a visible NPC rendered "Someone …", an invisible actor leaked its name). ROM `$N`/`$n` = `PERS(victim)` = NPC short_descr, capitalized; invisible → "someone". **Fix (2.14.61):** TO_CHAR via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`; TO_ROOM via `act_to_room(room, "$n is surrounded by a force shield.", target, exclude=target)`. Test: `tests/integration/test_magic017_shield_pers.py` (NPC room broadcast → "A green goblin is surrounded by a force shield."; already-protected TO_CHAR → "A green goblin is already protected by a shield."). | ✅ FIXED (2.14.61) |
| `MAGIC-018` | LOW | src/magic.c:845 (`spell_bless` — `act("$N already has divine favor.", …, TO_CHAR)`) + :863 (`act("You grant $N the favor of your god.", …, TO_CHAR)`) | mud/skills/handlers.py:`bless` (~1607,1627) | **Same class as MAGIC-016.** Both cross-target TO_CHAR lines baked `_character_name(victim)` (the keyword `name` for an NPC) where ROM `$N` = PERS = NPC short_descr (capitalized for the "already has divine favor." line, which starts with `$N`; mid-sentence for "You grant $N …"). Casting bless on NPC "goblin"/"a green goblin" showed "goblin already has divine favor." / "You grant goblin the favor of your god." vs ROM "A green goblin already has divine favor." / "You grant a green goblin the favor of your god." Self-cast lines ("You are already blessed."/"You feel righteous.") and PC victims were already correct. **Fix (2.14.62):** both via `act_format("$N …"/"You grant $N …", recipient=caster, actor=caster, arg2=victim)`. Test: `tests/integration/test_magic018_bless_pers.py`. | ✅ FIXED (2.14.62) |
| `MAGIC-019` | LOW | src/magic.c:1608/1650/1694 — `act("$N doesn't appear to be blinded/diseased/poisoned.", ch, NULL, victim, TO_CHAR)` | mud/skills/handlers.py:`cure_blindness`/`cure_disease`/`cure_poison` (~2713,2769,2824) | **Same class as MAGIC-016.** The three cure spells' not-affected cross-target lines baked the victim's keyword `name` where ROM `$N` = PERS = NPC short_descr (capitalized). Curing a non-blind NPC "goblin"/"a green goblin" showed "goblin doesn't appear to be blinded." vs ROM "A green goblin doesn't appear to be blinded." Self-cast lines ("You aren't blind/ill/poisoned.") were already correct. **Fix (2.14.63):** all three via `act_format("$N doesn't appear to be …", recipient=caster, actor=caster, arg2=victim)`; dropped the dead `name = …` locals. Test: `tests/integration/test_magic019_cure_pers.py` (blindness/disease/poison → "A green goblin doesn't appear to be …"). **⚠️ Remaining baked-name spell-handler cluster (next agent, same `act_format` pattern):** ~~`shield`~~ (✅ MAGIC-017, 2.14.61), ~~`bless`/divine-favor~~ (✅ MAGIC-018, 2.14.62), `change_sex` (`act("$N has already had $s(?) sex changed.", …)` magic.c:1321 — note the **literal `(?)` ROM quirk** + `$s` possessive, must replicate exactly), ~~the cure_blindness/disease/poison "doesn't appear to be …" lines~~ (✅ MAGIC-019, 2.14.63), and ~~the curse-object "glows with a red/malevolent aura" lines~~ (✅ MAGIC-020, 2.14.64). ~~`change_sex`~~ (✅ MAGIC-021, 2.14.65). **The MAGIC-016 baked-name spell-handler cluster is now CLOSED** (armor/shield/bless/cures/curse/change_sex all ✅). | ✅ FIXED (2.14.64) / ✅ cluster CLOSED (MAGIC-021) |
| `MAGIC-021` | LOW | src/magic.c:1321 (`spell_change_sex` — `act("$N has already had $s(?) sex changed.", ch, NULL, victim, TO_CHAR)`) | mud/skills/handlers.py:`change_sex` (~2203) | **The already-changed cross-target line baked the victim name + literal "their" and dropped ROM's literal `$s(?)` quirk.** ROM renders `$N` = PERS(victim) (cap), then `$s` = the **CASTER's** possessive (a likely ROM bug — grammatically it should be the victim's `$S` — which the original author flagged with the literal "(?)") + the literal "(?)". So a male caster changing an already-changed NPC "goblin"/"a green goblin" sees ROM "A green goblin has already had his(?) sex changed." (the "his" is the *caster's* pronoun). The Python showed "goblin has already had their sex changed." Per the "replicate ROM quirks exactly" rule, **Fix (2.14.65):** `act_format("$N has already had $s(?) sex changed.", recipient=caster, actor=caster, arg2=victim)` — preserving the `$s`-is-caster bug + literal "(?)". Inverted `tests/test_skills_buffs.py::test_change_sex_respects_duplicates_and_saves` ("their"→"his(?)", male caster) + added `tests/integration/test_magic021_change_sex_quirk.py`. | ✅ FIXED (2.14.65) |
| `MAGIC-022` | LOW | src/magic.c `spell_protection_evil`/`_good` — `act("$N is already protected.", …)` + `act("$N is protected from evil/good.", …)` (TO_CHAR) | mud/skills/handlers.py:`protection_evil`/`protection_good` (~6853,6877,6895,6919) | **Same class as MAGIC-016**, across the protection_evil/good sibling pair (6 baked-name sites). Cross-target lines baked `_character_name(target)` where ROM `$N` = PERS = NPC short_descr (cap). Protecting an NPC "goblin"/"a green goblin" showed "goblin is protected from evil." vs ROM "A green goblin is protected from evil." **Fix (2.14.66):** all six via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`. Added room setup to the two existing buffs tests (PERS needs caster+target in a lit room). Test: `tests/integration/test_magic022_protection_pers.py`. **⚠️ NEW ENUMERATED BATCH (next agent) — the baked-name pattern is pervasive across MANY MORE spell handlers** (a grep of `mud/skills/handlers.py` for `_send_to_char(caster, f"{_character_name`/`f"{_object_short_descr`/`f"{name}`/`f"{obj` found ~15 remaining): ~~`bless` object~~ (✅ MAGIC-026, 2.14.71), ~~`faerie_fire`~~ (✅ MAGIC-027, 2.14.72 — ROM is SILENT on duplicate; Python's "already surrounded" lines were invented, now removed), ~~`disarm`~~ (✅ FIGHT-065, 2.14.70 — ROM literal "Your opponent is not wielding a weapon.", NOT `$N`), ~~`continual_light`/`fireproof`~~ (✅ MAGIC-026, 2.14.71), ~~`fly`~~ (✅ MAGIC-025, 2.14.69), ~~`frenzy`~~ (✅ MAGIC-023, 2.14.67), ~~`giant_strength`~~ (✅ MAGIC-024), ~~`haste`~~ (✅ MAGIC-024, 2.14.68), ~~`infravision`~~ (✅ MAGIC-025, 2.14.69), ~~`pass_door`~~ (✅ MAGIC-025, 2.14.69), ~~`plague` "$N seems to be unaffected"~~ (✅ MAGIC-028, 2.14.73 — was mislabeled "sanctuary(?)"), ~~`poison`-weapon `$p`~~ (✅ MAGIC-026, 2.14.71); ~~`envenom`-skill dict-return `$p`~~ (✅ MAGIC-029, 2.14.74); **BATCH FULLY CLOSED.** Each needs its ROM `act()` code verified ($N/$E/$p/$S/literal) before conversion — same `act_format` fix. | ✅ FIXED (2.14.66) / ⚠️ batch tracked |
| `MAGIC-023` | LOW | src/magic.c `spell_frenzy` — `act("$N is already in a frenzy.", …)`, `act("$N doesn't look like $e wants to fight anymore.", …)` (×2 calm/CALM branches), `act("Your god doesn't seem to like $N", …)` (TO_CHAR) | mud/skills/handlers.py:`frenzy` (~4658,4666,4674,4690) | **MAGIC-022-batch member with a second ROM quirk.** Four baked-name sites; ROM `$N` = PERS short_descr (cap). The "doesn't look like … wants to fight" line uses ROM `$e` = the **caster's** subject pronoun (`he_she[ch->sex]` — grammatically should be the victim's `$E`, a likely ROM bug), which the Python rendered as literal "they". The "Your god doesn't seem to like $N" line has **no trailing period** in ROM (preserved). **Fix (2.14.67):** all four via `act_format`, replicating the `$e`-is-caster quirk verbatim. A male caster frenzy-blocked on a calm female NPC "a green goblin" sees "A green goblin doesn't look like **he** wants to fight anymore." (the "he" is the *caster's*). Test: `tests/integration/test_magic023_frenzy_pers.py`. | ✅ FIXED (2.14.67) |
| `MAGIC-024` | LOW | src/magic.c:3028 (`spell_giant_strength` — `act("$N can't get any stronger.", …)`) + :3074 (`spell_haste` — `act("$N is already moving as fast as $E can.", …)`) (TO_CHAR) | mud/skills/handlers.py:`giant_strength` (~5011), `haste` (~5083/5092) | **MAGIC-022-batch members.** giant_strength baked `$N`; haste baked `$N` + literal "they" where ROM uses `$E` = the **victim's** subject pronoun (correct uppercase `$E` here, unlike frenzy's `$e`). Hasting an already-hasted sexless "Runner" showed "Runner is already moving as fast as they can." vs ROM "… as **it** can." (Sex.NONE → "it"). **Fix (2.14.68):** both via `act_format` ($N + $E). Re-baselined `test_skills_buffs.py::test_haste_dispels_slow_or_blocks_duplicates` ("they"→"it"). Test: `tests/integration/test_magic024_strength_haste_pers.py` (giant_strength NPC → cap; haste female NPC → "she"). | ✅ FIXED (2.14.68) |
| `MAGIC-020` | LOW | src/magic.c:1737 (`act("$p is already filled with evil.", …, TO_CHAR)`) + :1751/:1773 (`act("$p glows with a red/malevolent aura.", …, TO_ALL)`) + :1758 (`act("The holy aura of $p is too powerful…", …, TO_CHAR)`) | mud/skills/handlers.py:`curse` object branch (~2890,2899,2903,2907) | **`spell_curse`'s object branch baked the lowercase `obj.short_descr` (no buf[0] cap) and sent the aura lines TO_CHAR-only where ROM uses TO_ALL (room + caster).** Cursing "a silver dagger" showed "a silver dagger glows with a malevolent aura." to the caster only; ROM shows "**A** silver dagger glows with a malevolent aura." to the **whole room**. **Fix (2.14.64):** TO_CHAR lines via `act_format("$p …", recipient=caster, actor=caster, arg1=obj)` (caps + `can_see_obj` masking); the two TO_ALL aura lines deliver to the caster via `act_format` AND to the room via `act_to_room(caster.room, "$p …", caster, arg1=obj)`. Test: `tests/integration/test_magic020_curse_object_pers.py` (caster + room observer both see "A silver dagger glows with a malevolent aura."; already-evil → "A cursed blade is already filled with evil.").
| `MAGIC-011` | LOW | src/magic.c:3946 (`spell_poison` food/drink branch — `act("$p is infused with poisonous vapors.", ch, obj, NULL, TO_ALL)`) | mud/skills/handlers.py:`poison` food/drink branch (~6546) | **poison food/drink caster leg is NOT capitalized — a missed site under the CLOSED ACT-CAP-002 invariant.** ROM `act(..., TO_ALL)` caps `buf[0]` for *every* recipient including `ch` (`src/comm.c:2376`), so the caster sees `"Loaf of bread is infused with poisonous vapors."`. Python's food caster leg does `_send_to_char(caster, f"{_object_short_descr(obj)} is infused with poisonous vapors.")` with **no** `capitalize_act_line`, so the caster sees lowercase `"loaf of bread is infused…"`. The asymmetry is the tell: the **weapon** caster leg right above it (`:3981`, MAGIC-005) *is* capped via `capitalize_act_line`. INV-029's **ACT-CAP-002** sub-row (`CROSS_FILE_INVARIANTS_TRACKER.md:53`, marked CLOSED 2.11.41) explicitly lists "the TO_ALL caster legs in object-spell handlers (invis/poison/…)" as covered — this food leg slipped through that closure (stale-✅, AGENTS.md re-verify anti-pattern). Surfaced 2026-06-01 by advisor review while closing MAGIC-005 (preserved as-is then since MAGIC-005 was scoped to the room legs). Fix: wrap the food caster leg in `capitalize_act_line` (matching the weapon leg) and re-baseline `test_skills_debuffs.py:190` ("loaf of bread"→"Loaf of bread"); update the ACT-CAP-002 trail to note the missed leg now closed. **Fix (2.12.36):** wrapped the food caster leg in `capitalize_act_line` (`handlers.py:poison` ~6561), matching the weapon leg; re-baselined `test_skills_debuffs.py:190` and `test_magic005_poison_object_pers_masking.py:113` ("loaf of bread"→"Loaf of bread"). Test: `tests/integration/test_magic011_poison_food_caster_caps.py` (2). | ✅ FIXED (2.12.36) |
| `MAGIC-012` | LOW | src/magic.c:2961 (`spell_frenzy` success room broadcast — `act("$n gets a wild look in $s eyes!", victim, NULL, NULL, TO_ROOM)`) | mud/skills/handlers.py:`frenzy` (~4658) | **frenzy success room line baked the victim name + literal "their", missed by the INV-025 PERS-masking sweep.** ROM renders `$n` per recipient via `PERS(victim, to)` (an invisible victim masks to "someone") and `$s` as the victim's gendered possessive (his/her/its). Python used a hand-rolled `for occupant in room.people` loop emitting `f"{name} gets a wild look in their eyes!"` — no masking, wrong possessive. **Root-cause class:** the INV-025/027 sweep converted `_act_room` call sites but NOT handlers with manual room loops, so this (and MAGIC-013) slipped through. Surfaced 2026-06-01 probing FINDING-015 (verifying the "already correct" buff handlers). **Fix (2.12.43):** converted the room leg to `act_to_room(room, "$n gets a wild look in $s eyes!", target, exclude=target)`; re-baselined `test_skills_buffs.py:128` ("their"→"its", a sexless Sex.NONE test char). Test: `tests/integration/test_magic012_frenzy_room_pers_masking.py` (male→"his", invisible→"Someone"+"her"). | ✅ FIXED (2.12.43) |
| `MAGIC-013` | LOW | src/magic.c:1657-1659 (`spell_cure_disease` dispel-success — `send_to_char("Your sores vanish.\n\r", victim)` + `act("$n looks relieved as $s sores vanish.", victim, NULL, NULL, TO_ROOM)`) | mud/skills/handlers.py:`cure_disease` (~2774) | **cure_disease success room line baked the victim name + literal "their" AND uses the wrong `messages.append` mailbox channel** (not `act_to_room`/`push_message`). Two divergences in one leg: (a) no `$n` PERS masking / wrong `$s` possessive (same class as MAGIC-012), and (b) MAGIC-003-class wrong-channel (mailbox instead of immediate descriptor). Fix: replace the manual loop with `act_to_room(room, "$n looks relieved as $s sores vanish.", victim, exclude=victim)`. Surfaced 2026-06-01 alongside MAGIC-012. **Fix (2.12.44):** converted the dispel-success room leg to `act_to_room(room, "$n looks relieved as $s sores vanish.", victim, exclude=victim)` (fixes both the masking/possessive and the wrong channel); re-baselined `test_skills_healing.py:60` ("their"→"its"). Test: `tests/integration/test_magic013_cure_disease_room_pers_masking.py` (male→"his", invisible→"Someone"+"her"). **Still to verify** the dirt-kicking already-affected caster line (`handlers.py:~3200`, `"{name} already has dirt in their eyes."`) — no ROM equivalent found for that string in `src/`; confirm whether it is Python-invented before touching (carried to next session). | ✅ FIXED (2.12.44) |
| `MAGIC-025` | LOW | src/magic.c:2892 (`spell_fly` — `act("$N doesn't need your help to fly.", …)`), :3594 (`spell_infravision` — `act("$N already has infravision.\n\r", …)`), :3874 (`spell_pass_door` — `act("$N is already shifted out of phase.", …)`) (TO_CHAR) | mud/skills/handlers.py:`fly` (~4605), `infravision` (~5678), `pass_door` (~6346) | **MAGIC-022-batch members — three clean `$N` blocking lines.** All baked the victim keyword `name`/`_character_name(target)` where ROM `$N` = PERS = NPC short_descr (cap). Casting on an already-affected NPC "goblin"/"a green goblin" showed lowercase "goblin doesn't need your help to fly." vs ROM "A green goblin doesn't need your help to fly." (infravision's ROM literal carries an embedded `\n\r` — a delivery-layer artifact; the rendered text is "$N already has infravision." without it). **Fix (2.14.69):** all three via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`; removed the now-orphaned `name`/`_character_name` assignments. Test: `tests/integration/test_magic025_fly_infra_passdoor_pers.py` (NPC victim → capitalized short_descr on all three). | ✅ FIXED (2.14.69) |
| `MAGIC-026` | LOW | object `$p` blocking lines (cap buf[0]): `spell_bless` obj `src/magic.c:794` ("$p is already blessed."), `spell_continual_light` `:1483` ("$p is already glowing."), `spell_fireproof` `:2770` ("$p is already protected from burning."), `spell_poison` weapon branch `:3962` ("You can't seem to envenom $p.") + `:3968` ("$p is already envenomed.") | mud/skills/handlers.py:`bless` (~1518), `continual_light` (~2529), `fireproof` (~4481), `poison` (~6683/6692/6698) | **MAGIC-022-batch object-`$p` members.** Six `act("$p …", ch, obj, NULL, TO_CHAR)` lines baked a lowercase `_object_short_descr(obj)`; ROM caps buf[0]. Blessing an already-blessed "a glowing rune" showed "a glowing rune is already blessed." vs ROM "A glowing rune is already blessed." (The mid-sentence "You can't seem to envenom $p." caps "You", leaving the object name lowercase — matching ROM.) **Fix (2.14.71):** all six via `capitalize_act_line(f"{_object_short_descr(obj)} …")` — chosen over `act_format("$p")` to match the same-function MAGIC-011 sibling leg (`poison` :6658) and avoid `can_see_object`-masking fragility in tests (the caster always sees a targeted object, so ROM's `$p` mask never fires here). Re-baselined two missed-caps assertions (`test_skills_debuffs.py:158` envenomed, `test_skills_buffs.py:706` fireproof). Test: `tests/integration/test_magic026_object_p_capitalization.py` (4 — bless/continual_light/fireproof/poison-weapon). **Follow-up filed:** the `envenom` skill (`handlers.py:envenom` ~4012, ROM `do_envenom` `src/act_obj.c:929`) has the same `$p` cap gap on its **dict-return** paths (`:4139` "You can't seem to envenom $p.", `:4160` "$p is already envenomed.") — different delivery contract (returns `{"message": …}` not `_send_to_char`) + needs a `TO_WEAPON` affect fixture; next agent, capitalize via `capitalize_act_line` on the returned string. | ✅ FIXED (2.14.71) |
| `MAGIC-027` | LOW | `src/magic.c:2811-2812` `spell_faerie_fire` — `if (IS_AFFECTED(victim, AFF_FAERIE_FIRE)) return;` (SILENT, no message) | mud/skills/handlers.py:`faerie_fire` (~4222) | **The `faerie_fire` handler invented two non-ROM "already surrounded" messages on a duplicate cast.** ROM's `spell_faerie_fire` silently `return`s when the victim already has `AFF_FAERIE_FIRE` — it sends NOTHING to the caster or victim. The Python emitted "You are already surrounded by a pink outline." (self-cast) / `f"{name} is already surrounded by a pink outline."` (cross-cast) — both fabricated. Surfaced while scoping the MAGIC-022 batch's `faerie_fire` entry: the batch note assumed a `$N` cap conversion, but the ROM source check showed there is no message to convert — the lines must be **removed**. Distinct class from MAGIC-026 (spurious output, not missing capitalization). **Fix (2.14.72):** the already-affected branch now returns `False` silently. Re-baselined `test_skills_detection.py:293` (asserted the invented "Rogue is already surrounded …" line → now asserts `caster.messages == []`). Test: `tests/integration/test_magic027_faerie_fire_silent_duplicate.py` (cross + self cast both silent). | ✅ FIXED (2.14.72) |
| `MAGIC-028` | LOW | `src/magic.c:3905` `spell_plague` — `act("$N seems to be unaffected.", ch, NULL, victim, TO_CHAR)` | mud/skills/handlers.py:`plague` (~6589) | **MAGIC-022-batch member (the batch's "sanctuary(?)" note was misattributed — the line lives in `plague`, not `sanctuary`).** When the victim saves vs DAM_DISEASE or is an undead NPC and is not the caster, ROM renders `$N` = PERS(victim) = NPC short_descr (cap); the self-cast leg's literal "You feel momentarily ill, but it passes." was already correct. The Python baked `_character_name(victim)`, so plaguing an undead NPC "a green goblin" showed "goblin seems to be unaffected." vs ROM "A green goblin seems to be unaffected." **Fix (2.14.73):** cross-target leg via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`. Re-baselined `test_skills_debuffs.py:test_plague_respects_saves_and_undead` (FIELD→INSIDE so PERS isn't masked to "Someone" by night-dark). Test: `tests/integration/test_magic028_plague_unaffected_pers.py` (undead NPC → cap short_descr). **This CLOSES the MAGIC-022 enumerated baked-name batch** — all members resolved (MAGIC-023/024/025/026/027/028 + FIGHT-065); only the `envenom`-skill dict-return `$p` follow-up (filed under MAGIC-026) remains. | ✅ FIXED (2.14.73) |
| `MAGIC-029` | LOW | `src/act_obj.c:929` `do_envenom` — `act("$p is already envenomed.", ch, obj, NULL, TO_CHAR)` (caps buf[0]) | mud/skills/handlers.py:`envenom` (~4160) | **The `envenom` skill's already-poisoned-weapon dict message baked a lowercase `short_descr`.** Unlike the `poison`-spell weapon branch (closed in MAGIC-026, delivered via `_send_to_char`), the `envenom` skill returns its message as a `{"success": False, "message": …}` dict consumed by the command layer, so it bypassed the act() capitalization. Envenoming an already-poisoned "a serrated dagger" returned "a serrated dagger is already envenomed." vs ROM "A serrated dagger is already envenomed." The sibling "You can't seem to envenom $p." line (`:4139`) needs no change — ROM caps its buf[0] = "You", leaving the mid-sentence `$p` lowercase. **Fix (2.14.74):** the dict message now wraps `capitalize_act_line(...)`. This resolves the follow-up filed under the MAGIC-026 row. Test: `tests/integration/test_magic029_envenom_skill_already_envenomed_cap.py` (TO_WEAPON poison affect → cap). | ✅ FIXED (2.14.74) |
| `MAGIC-030` | LOW | `src/magic.c:4363-4366` `spell_sleep` — `if (IS_AFFECTED(victim, AFF_SLEEP) || (IS_NPC && ACT_UNDEAD) || (level+2) < victim->level || saves_spell(...)) return;` (SILENT, no message on any gate) | mud/skills/handlers.py:`sleep` (~7560) | **The `sleep` handler invented three non-ROM reject messages.** ROM's `spell_sleep` silently `return`s on every gate (already-asleep, undead NPC, level, saves). The Python emitted "You are already fast asleep." / `f"{name} is already fast asleep."` (self/cross) and `f"{_character_name(target)} is immune to sleep."` (undead) — all fabricated; the level and saves gates were already correctly silent. Same spurious-output class as MAGIC-027 (faerie_fire). Surfaced by a fresh act()-lens grep sweep (`_send_to_char(caster, f"{name|_character_name|...}`) after the MAGIC-022 batch closed. **Fix (2.14.75):** the already-affected and undead branches now return `False` silently. No tests asserted the invented lines. Test: `tests/integration/test_magic030_sleep_silent_gates.py` (already-affected / undead NPC / self-cast all silent). **Filed (next gap — MAGIC-031 candidate):** the same sweep found `spell_slow` (`magic.c:4396` `act("$N can't get any slower than that.")`) and `spell_stone_skin` (`magic.c:4452` `act("$N is already as hard as can be.")`) cross-target legs bake `_character_name` where ROM uses `$N` PERS cap (their self-legs — "You can't move any slower!" / "Your skin is already as hard as a rock." — are correct ROM literals); a clean two-site `$N`-cap conversion. | ✅ FIXED (2.14.75) |
| `MAGIC-031` | LOW | `src/magic.c:4396` `spell_slow` — `act("$N can't get any slower than that.", …)` + `:4452` `spell_stone_skin` — `act("$N is already as hard as can be.", …)` (TO_CHAR) | mud/skills/handlers.py:`slow` (~7622), `stone_skin` (~7852) | **slow/stone_skin cross-target reject legs baked `_character_name`.** ROM `$N` = PERS(victim) = NPC short_descr, capitalized. The self-cast legs ("You can't move any slower!" / "Your skin is already as hard as a rock.") are ROM literals (`send_to_char`) and were already correct. Casting slow on an already-slowed NPC "a green goblin" showed "goblin can't get any slower than that." vs ROM "A green goblin …". Filed under the MAGIC-030 row from the post-MAGIC-022 act()-lens sweep. **Fix (2.14.76):** both cross-legs via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`. The existing PC-target stone_skin test ("Guardian …") stays green (PERS = name). Test: `tests/integration/test_magic031_slow_stoneskin_pers.py` (already-slowed / already-stoned NPC → cap short_descr). | ✅ FIXED (2.14.76) |
| `MAGIC-032` | LOW | `src/magic.c:4282/4284` `spell_sanctuary` — self `send_to_char("You are already in sanctuary.")` / cross `act("$N is already in sanctuary.", ch, NULL, victim, TO_CHAR)` | mud/skills/handlers.py:`sanctuary` (~7386) | **sanctuary cross-target reject leg baked `target.name`.** ROM `$N` = PERS(victim) = NPC short_descr, capitalized; the self-cast leg is a ROM literal (already correct). Casting sanctuary on an already-sanctuary NPC "a green goblin" showed "goblin is already in sanctuary." vs ROM "A green goblin …". Found by the continuing post-MAGIC-022 act()-lens sweep. **Fix (2.14.77):** cross-leg via `act_format("$N …", recipient=caster, actor=caster, arg2=target)`. Test: `tests/integration/test_magic032_sanctuary_pers.py`. **Filed (next gap — MAGIC-033 candidate):** `spell_know_alignment` (`magic.c:3666-3690`) ALWAYS renders `act("$N …", ch, NULL, victim, TO_CHAR)` (7 alignment tiers) — there is **no "You …" self-variant** (a self-cast shows the caster's own name via PERS), but the Python (`handlers.py:~5867`) invented "You …" variants AND is missing the `ap > -350` tier "$N lies to $S friends." ($S = victim possessive) AND must preserve ROM's literal typo "…the embodiment of pure evil!." (double `!.`). A larger conversion than the single-line caps. | ✅ FIXED (2.14.77) |
| `MAGIC-033` | LOW | `src/magic.c:3674-3690` `spell_know_alignment` — 7 alignment-band messages, ALL emitted via a single `act(msg, ch, NULL, victim, TO_CHAR)` (`$N` = PERS victim; `$S` = victim possessive in the "lies to $S friends" tier; most-evil tier is literally "…pure evil!." with a double `!.`) | mud/skills/handlers.py:`know_alignment` (~5860) | **know_alignment diverged three ways from ROM's act() semantics.** (1) NPC victims rendered the baked keyword `name` instead of PERS short_descr (capitalized). (2) The Python invented a "You …" self-variant via `_choose(...)` — ROM has NO self-branch; `act("$N …")` on a self-cast renders the caster's own name (PERS(ch, ch)). (3) The most-evil tier dropped ROM's literal "evil!." typo (rendered "evil!"). Surfaced by the continuing post-MAGIC-022 act()-lens sweep. **Fix (2.14.78):** the whole band block now selects a `$N`/`$S` template and renders via `act_format(template, recipient=caster, actor=caster, arg2=victim)`; removed `_choose`/`name`/`possessive`. The "evil!." typo is preserved verbatim per ROM. Re-baselined the existing band tests across 3 files (added lit-room setup so PERS resolves to the PC name rather than masking to "Someone"; self-variant tests now assert the caster's name; typo cases now assert "evil!."). Test: `tests/integration/test_magic033_know_alignment_act_semantics.py` (NPC short_descr cap, self-cast name not "You", `!.` typo). | ✅ FIXED (2.14.78) |
| `MAGIC-034` | LOW | `src/magic.c` detect_* duplicate-cast rejects: detect_evil `:1846`, detect_good `:1875`, detect_hidden `:1905`, detect_invis `:1936`, detect_magic `:1968` — each `act("$N can already …", ch, NULL, victim, TO_CHAR)` | mud/skills/handlers.py:`detect_evil`/`detect_good`/`detect_hidden`/`detect_invis`/`detect_magic` (~3048/3080/3112/3144/3176) | **The 5 sibling detect_* spells baked the keyword `name` into their cross-target duplicate-cast reject.** ROM `$N` = PERS(victim) = NPC short_descr, capitalized; the self-cast legs are ROM `send_to_char` literals and were already correct. Casting detect evil on an already-detecting NPC "a green goblin" showed "goblin can already detect evil." vs ROM "A green goblin …". Found by the continuing post-MAGIC-022 act()-lens sweep. **Fix (2.14.79):** all 5 cross-legs via `act_format("$N can already …", recipient=caster, actor=caster, arg2=target)`. Re-baselined the 5 existing duplicate-gating tests in `test_spell_detection_rom_parity.py` (added lit-room setup so PERS resolves to the PC name; `test_skills_detection.py` already had a room). Test: `tests/integration/test_magic034_detect_cluster_pers.py` (parametrized, all 5 → NPC short_descr cap). **Filed (next sweep targets):** the same grep found more baked `$N` lines — "$N looks very uncomfortable." (magic.c:1801), "$N does not seem to be affected." (:2027/:2059), "$N is protected by $S evil." (:2053, TO_ROOM $S possessive) — a further act()-lens batch. | ✅ FIXED (2.14.79) |
| `MAGIC-035` | LOW | `src/magic.c:1801` `spell_curse` "$N looks very uncomfortable." + `:2027` `spell_dispel_evil` / `:2059` `spell_dispel_good` "$N does not seem to be affected." (all `act(…, ch, NULL, victim, TO_CHAR)`) | mud/skills/handlers.py:`curse` (~2965), `dispel_evil`/`dispel_good` neutral branches (~3520/~3555) | **curse + dispel_evil/good TO_CHAR reject lines baked the keyword `name`.** ROM `$N` = PERS(victim) = NPC short_descr, capitalized. Cursing an NPC "a green goblin" showed "goblin looks very uncomfortable." vs ROM "A green goblin …". Found by the continuing post-MAGIC-022 act()-lens sweep. **Fix (2.14.80):** all three TO_CHAR lines via `act_format("$N …", recipient=caster, actor=caster, arg2=victim)` (the two dispel neutral branches are identical → single `replace_all` edit). Existing PC-victim test (`test_skills_damage.py` "Paladin …") stays green (PERS = name, room present). Test: `tests/integration/test_magic035_curse_dispel_neutral_pers.py`. **Filed (next gap — MAGIC-036 candidate):** the dispel **TO_ROOM** branches — `spell_dispel_evil` is_good `act("Mota protects $N.", …, TO_ROOM)` (`:2024`) and `spell_dispel_good` is_evil `act("$N is protected by $S evil.", …, TO_ROOM)` (`:2053`, `$S` = victim possessive) — bake `name` AND the Python over-delivers to the caster (ROM TO_ROOM excludes the actor); needs PERS + `$S` + TO_ROOM exclusion semantics, a more involved fix than the TO_CHAR caps. | ✅ FIXED (2.14.80) |
| `MAGIC-036` | LOW | `src/magic.c:2024` `spell_dispel_evil` is_good `act("Mota protects $N.", ch, NULL, victim, TO_ROOM)` + `:2053` `spell_dispel_good` is_evil `act("$N is protected by $S evil.", ch, NULL, victim, TO_ROOM)` | mud/skills/handlers.py:`dispel_evil` is_good (~3509), `dispel_good` is_evil (~3557) | **The dispel TO_ROOM "protected" branches diverged three ways.** (1) baked the keyword `name` instead of `$N` PERS (NPC short_descr, cap); (2) dispel_good rendered "{name}'s evil" instead of ROM's `$S` = victim possessive (his/her/its) — an evil sexless NPC should read "…protected by its evil." not "…a green goblin's evil."; (3) ROM `act(…, TO_ROOM)` excludes the actor (caster), but the Python's hand-rolled occupant loop ALSO sent the line to the caster (over-delivery). Found by the post-MAGIC-035 act()-lens sweep continuation. **Fix (2.14.81):** both branches now use `act_to_room(room, "…", caster, arg2=victim, exclude=caster)` — per-recipient PERS/`$S`, single-delivery + TRIG_ACT, actor excluded. No tests asserted the old caster-delivery. Test: `tests/integration/test_magic036_dispel_to_room_pers.py` (witness gets the PERS line, caster does not; `$S`-possessive "its evil"). | ✅ FIXED (2.14.81) |
| `MAGIC-037` | LOW | `src/magic.c` `spell_demonfire` → `spell_curse` tail `act("$N looks very uncomfortable.", ch, NULL, victim, TO_CHAR)` (`:1801`) | mud/skills/handlers.py:`demonfire` curse-tail (~3031) | **demonfire's inline copy of the curse messaging baked the keyword `name`.** ROM `spell_demonfire` ends by calling `spell_curse(...)`, whose cross-target line is `act("$N looks very uncomfortable.", …, TO_CHAR)` — `$N` = PERS(victim) = NPC short_descr, capitalized. Demonfiring an NPC "a green goblin" showed "goblin looks very uncomfortable." vs ROM "A green goblin …". Found by the post-MAGIC-036 act()-lens sweep continuation. **Fix (2.14.82):** the curse-tail line via `act_format("$N …", recipient=caster, actor=caster, arg2=victim)`. Existing PC-victim test (`test_skills_damage.py` "Paladin …") stays green. Test: `tests/integration/test_magic037_demonfire_curse_pers.py`. **Filed (next gap — MAGIC-038 candidate):** demonfire's OWN "demons of Hell" lines (`handlers.py:~2994/2996`) bake names too — ROM `act("$n calls forth the demons of Hell upon $N!", …, TO_ROOM)` (`:2024`-region) + `act("$n has assailed you …", …, TO_VICT)`; the Python excludes the victim from the room loop (ROM TO_ROOM *includes* the victim) — a PERS + TO_ROOM-topology fix. | ✅ FIXED (2.14.82) |
| `MAGIC-038` | LOW | `src/magic.c` `spell_demonfire` — `act("$n calls forth the demons of Hell upon $N!", ch, NULL, victim, TO_ROOM)` + `act("$n has assailed you with the demons of Hell!", ch, NULL, victim, TO_VICT)` | mud/skills/handlers.py:`demonfire` (~2986) | **demonfire's "demons of Hell" broadcast baked both names AND used the wrong room topology.** ROM `$n`/`$N` = PERS(caster)/PERS(victim); the "calls forth" line is **TO_ROOM** (every occupant except the actor — so the **victim is included**), and "has assailed you" is **TO_VICT** ($n PERS to the victim). The Python baked `caster_name`/`victim_name` and excluded the victim from the room loop (TO_NOTVICT topology), so the victim never received the "calls forth" line. Filed under the MAGIC-037 row from the act()-lens sweep. **Fix (2.14.83):** "calls forth" via `act_to_room(room, …, caster, arg2=victim, exclude=caster)` (victim included); "has assailed you" via `act_format("$n …", recipient=victim, actor=caster)`; the caster literal was already correct. Re-baselined `test_skills_damage.py::test_demonfire_applies_curse_and_fire_damage` (victim now receives the TO_ROOM "calls forth" at `messages[0]` before the TO_VICT line). Test: `tests/integration/test_magic038_demonfire_demons_pers.py` (NPC short_descr PERS; victim included; caster excluded). | ✅ FIXED (2.14.83) |
| `MAGIC-039` | LOW | `src/magic.c:1388` `spell_charm_person` `act("Isn't $n just so nice?", ch, NULL, victim, TO_VICT)` + `:1390` `act("$N looks at you with adoring eyes.", ch, NULL, victim, TO_CHAR)` | mud/skills/handlers.py:`charm_person` (~2307/~2313) | **charm_person's two success messages baked names.** ROM `$n` = PERS(caster) seen by the victim (TO_VICT); `$N` = PERS(victim), capitalized (TO_CHAR). The Python baked `actor_name`/`target_name`, so charming an NPC "a green goblin" showed the caster "goblin looks at you with adoring eyes." vs ROM "A green goblin …". Found by the continuing act()-lens sweep. **Fix (2.14.84):** both via `act_format` (TO_VICT `recipient=target, actor=caster`; TO_CHAR `recipient=caster, actor=caster, arg2=target`). Existing PC-name test (`test_skills.py` "Enchanter"/"Guard") stays green (PERS = name, room present). Test: `tests/integration/test_magic039_charm_person_pers.py`. | ✅ FIXED (2.14.84) |
| `MAGIC-040` | LOW | `src/magic.c:1062` `spell_cure_blindness` `act("$n is no longer blinded.", victim, NULL, NULL, TO_ROOM)` + `:1702` `spell_cure_poison` `act("$n looks much better.", victim, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`cure_blindness` (~2734), `cure_poison` (~2849) | **cure_blindness/cure_poison room broadcasts hand-rolled an occupant loop that baked the keyword `name`.** ROM emits these via `act("$n …", victim, …, TO_ROOM)` — the actor is the **cured victim** (excluded), so `$n` = PERS(victim) = NPC short_descr, capitalized. A bystander watching an NPC "a green goblin" get cured saw "goblin is no longer blinded." vs ROM "A green goblin …". Found by the continuing act()-lens sweep. **Fix (2.14.85):** both via `act_to_room(room, "$n …", victim, exclude=victim)` (per-recipient PERS + INV-001 single-delivery + TRIG_ACT). PC-victim tests (`test_skills_healing.py` "Patient …") and the INV-025 cancellation-masking tests stay green. Test: `tests/integration/test_magic040_cure_room_broadcast_pers.py`. | ✅ FIXED (2.14.85) |
| `MAGIC-041` | LOW | `src/magic.c:1417` `spell_chill_touch` `act("$n turns blue and shivers.", victim, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`chill_touch` (~2393) | **chill_touch's room broadcast hand-rolled an occupant loop baking the keyword `name`.** ROM's actor is the chilled victim (excluded), so `$n` = PERS(victim) = NPC short_descr, capitalized. A bystander watching an NPC "a green goblin" get chilled saw "goblin turns blue and shivers." vs ROM "A green goblin …". Found by the continuing act()-lens sweep. **Fix (2.14.86):** `act_to_room(room, "$n turns blue and shivers.", target, exclude=target)` (per-recipient PERS + single-delivery + TRIG_ACT). The caster (not the victim) still receives it — TO_ROOM excludes only the actor=victim. Existing PC-target test (`test_skills.py` "Orc …", asserts both watcher AND caster see it) stays green. Test: `tests/integration/test_magic041_chill_touch_room_pers.py`. **Remaining act()-lens tail (filed):** faerie_fog "$n is revealed!" (magic.c:2850, per-revealed-char loop) + envenom room broadcasts (`handlers.py:4103/4207` "treats/coats $p with deadly poison/venom"). | ✅ FIXED (2.14.86) |
| `MAGIC-042` | LOW | `src/magic.c:2850` `spell_faerie_fog` per-revealed-char `act("$n is revealed!", ich, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`faerie_fog` (~4304) | **faerie_fog's "is revealed!" broadcast passed a pre-baked string to `_act_room`, so it never did per-recipient PERS.** ROM's actor is each revealed char (excluded), so `$n` = PERS(ich) = NPC short_descr, capitalized (and masked per-recipient). A bystander watching a hidden NPC "a green goblin" get revealed saw `_character_name`'s lowercase keyword vs ROM "A green goblin is revealed!". (The spell's other line, "$n conjures a cloud of purple smoke.", already used `act_to_room` correctly.) Found by the continuing act()-lens sweep. **Fix (2.14.87):** `act_to_room(room, "$n is revealed!", occupant, exclude=occupant)`. Existing PC-target test (`test_skills_mass.py` "Sneak is revealed!") stays green. Test: `tests/integration/test_magic042_faerie_fog_revealed_pers.py`. **Remaining act()-lens tail (filed):** envenom room broadcasts (`handlers.py:4103/4207` "treats/coats $p with deadly poison/venom" — `_character_name(caster)` baked + `$p` object). | ✅ FIXED (2.14.87) |
| `MAGIC-043` | LOW | `src/act_obj.c:887` `do_envenom` food `act("$n treats $p with deadly poison.", ch, obj, NULL, TO_ROOM)` + `:946` weapon `act("$n coats $p with deadly venom.", ch, obj, NULL, TO_ROOM)` | mud/skills/handlers.py:`envenom` food (~4099), weapon (~4203) | **envenom's two room broadcasts passed pre-baked `f"{_character_name(caster)} … {short_descr} …"` strings to `_act_room`, so they never did per-recipient PERS.** ROM `$n` = PERS(caster) (masks an invisible/NPC caster — short_descr, capitalized) and `$p` = the object. An NPC caster "a wandering alchemist" rendered the keyword "Alchemist" vs ROM "A wandering alchemist". The TO_CHAR legs ("You treat/coat $p …") are ROM `send_to_char`-equivalent literals and were already correct. Found by the continuing act()-lens sweep. **Fix (2.14.88):** both TO_ROOM lines via `act_to_room(room, "$n … $p …", caster, arg1=obj, exclude=caster)`. Test: `tests/integration/test_magic043_envenom_room_broadcast_pers.py` (NPC caster → short_descr PERS). **This clears the post-MAGIC-022 act()-lens baked-name tail in `handlers.py`.** | ✅ FIXED (2.14.88) |
| `MAGIC-044` | LOW | `src/magic.c:889` `spell_blindness` `act("$n appears to be blinded.", victim, NULL, NULL, TO_ROOM)` | mud/skills/handlers.py:`blindness` (~1672) | **blindness's room broadcast hand-rolled an occupant loop baking `target.name`** (with a `if target.name else "Someone"` ternary — so a **visible NPC** rendered "Someone" instead of its short_descr, and the cap was missing). ROM's actor is the blinded victim (excluded), so `$n` = PERS(victim) = NPC short_descr, capitalized. Blinding an NPC "a green goblin" showed bystanders "goblin appears to be blinded." (or "Someone …" with the ternary) vs ROM "A green goblin …". The MAGIC-003 fix (2.11.24) had converted the delivery *channel* to `_send_to_char` but left the hand-rolled baked-name loop. Found by the post-MAGIC-043 verification sweep. **Fix (2.14.89):** `act_to_room(room, "$n appears to be blinded.", target, exclude=target)`. Existing PC-target test (`test_skills.py` "Orc …") stays green. Test: `tests/integration/test_magic044_blindness_room_pers.py`. | ✅ FIXED (2.14.89) |
| `MAGIC-014` | LOW | `src/magic.c` single-actor `$n`-only spell room lines: create_rose `:1536`, earthquake `:2263`, giant_strength `:3041`, haste `:3104` + slow-dispel `:3088`, pass_door `:3887`, sleep `:4380`, slow `:4434` + haste-dispel `:4420`, stone_skin `:4465`, weaken `:4581` | mud/skills/handlers.py (`create_rose`, `earthquake`, `giant_strength`, `haste`, `pass_door`, `sleep`, `slow`, `stone_skin`, `weaken`) | **Batch closure of the INV-025 manual-room-loop sweep remainder (~11 `$n`-only sites).** ROM emits each as `act("$n …", actor, NULL, NULL, TO_ROOM)`; Python baked `_character_name()` into a `room.broadcast(...)` call or a hand-rolled `for occupant in room.people` loop, with a `if target.name else "Someone …"` ternary for nameless actors — so a **visible NPC** rendered the literal "Someone …" instead of its `short_descr` (ROM `$n`→`PERS`→short_descr), and an **invisible actor** leaked its name (no `can_see` gate). All `$n`-only (no `$s`), so a visible named PC renders identically; only NPC short_descr + invisible-actor masking change. **Decision:** consciously batched into one commit + one comprehensive masking test (mirroring the 2.12.30 26-site INV-025 batch), overriding the earlier "one MAGIC-NNN each" trail note — `$n`-only mechanical conversions, not per-site possessive re-baselines like MAGIC-012/013. **Fix (2.12.45):** all sites → `act_to_room(room, "$n …", actor, exclude=actor)`; pinned 2 `slow` tests (`test_skills_debuffs.py` rooms 4101/4102) from `Sector.FIELD`→`CITY` so the `$n` render is deterministic under leaked global sunlight (FIELD is dark at night → ROM-correctly masked, but non-deterministic). Test: `tests/integration/test_magic014_single_actor_room_pers_sweep.py` (haste visible+invisible, giant_strength `$n's`, earthquake caster-actor). The `trip` skill room line (`handlers.py:~7981`, ROM `src/fight.c:2701` `act("{5$n trips over $s own feet!{x", …)`) is **NOT** in this batch — it carries colour codes + `$s` possessive, filed separately as a FIGHT gap. | ✅ FIXED (2.12.45) |
| `MAGIC-045` | ✅ FIXED (2.14.223) — (a)+(c); (b)→MAGIC-046 | `spell_heat_metal` `src/magic.c:3123-3277` | `mud/skills/handlers.py:5177-5338` | **`heat_metal` hardcodes `can_drop_obj = True`, collapsing ROM's cursed-item (NODROP / inventory-locked) branches — wrong message, wrong damage, wrong item state, and RNG desync.** The port replaces every `can_drop_obj(victim, obj_lose)` gate with `if True:  # can_drop_obj` (lines 5209/5244/5284/5315, one even commented "Simplified: assume ... (no cursed items check)"). For a worn cursed (NODROP) metal weapon, ROM (`:3209-3232`) takes the **else** branch: "Your weapon sears your flesh!" + `dam += number_range(1, obj_lose->level)` (draws RNG), weapon **stays worn**. Python takes the drop branch: "You throw your red-hot weapon to the ground!", `dam += 1` (no RNG draw), and **strips the cursed weapon**. Same for worn cursed armor ("Your skin is seared", `:3167-3174`) and cursed inventory items (ROM `/2` divisor `:3195-3199/3251-3258` vs the port's `/6` drop path). Three classes at once: missing branch + wrong formula/message + RNG-count desync. **Sub-findings (same function, verify together): (b)** iteration order — Python walks `inventory + list(equipment.values())` (line 5181), ROM walks the single interleaved `victim->carrying` list (`:3134-3135`), so per-object `number_range`/`saves_spell` outcomes land on different objects (RNG draw-order divergence; confirmed structural, equip removes from inventory so not a double-count). **(c)** uses raw `target.dex` (line 5206) instead of `get_curr_stat(victim, STAT_DEX)` (`:3149-3152`) for the worn-armor `number_range(1, 2*DEX)` bound. **All three re-verified against ROM C 2026-07-03.** **Fix (2.14.223):** replaced every `if True:  # can_drop_obj` with the real `can_drop_obj` (ITEM_NODROP + immortal-PC bypass), modelled `remove_obj`'s ITEM_NOREMOVE failure ("You can't remove $p." → sears), and preserved ROM's `&&` short-circuit so the worn-armor DEX `number_range` is drawn **only** when the item is droppable (RNG-count parity). Switched the worn-armor DEX bound to `get_curr_stat(Stat.DEX)` — closes **(a)** and **(c)**. Test: `tests/test_spell_heat_metal_rom_parity.py` (3 new — worn NODROP weapon sears+stays, worn NODROP armor sears+stays, inventory NODROP item sears+stays; all verified red pre-fix, green post-fix). Sub-finding **(b)** iteration order is a structural divergence (Python's split `inventory`/`equipment` model cannot reproduce ROM's single interleaved `victim->carrying` order) — refiled as **MAGIC-046**. | ✅ FIXED (2.14.223) — (b) → MAGIC-046 |
| `MAGIC-046` | ✅ FIXED (2.14.239) | `spell_heat_metal` iteration order `src/magic.c:3134` + `remove_obj` `src/act_obj.c:1389-1390` | `mud/skills/handlers.py:heat_metal`; `mud/models/character.py:iter_carrying` | **(main) `heat_metal` iterated `inventory + list(equipment.values())`, not ROM's single interleaved `victim->carrying` list** — the per-object `number_range`/`saves_spell` draws landed on different objects than ROM. The `_carry_seq` acquisition counter (FINDING-020) already stamps every carry-list entry (head-insert = newest), so ROM's LIFO `carrying` order = descending `_carry_seq`. **Fix (2.14.239):** added `Character.iter_carrying()` (re-merges inventory + equipment sorted by descending `_carry_seq`); `heat_metal` now walks it when available (Character victims), falling back to the old combination for `MobInstance` (no `_carry_seq` tracking yet — a follow-up). **(d)** ROM's worn-item branch calls `remove_obj` (`src/act_obj.c:1389-1390`), which emits `act("$n stops using $p.", TO_ROOM)` / `act("You stop using $p.", TO_CHAR)` before the heat throw lines; the port modelled remove_obj as a boolean and dropped them — now emitted in both worn-armor and worn-weapon removed branches. Test: `tests/integration/test_magic046_heat_metal_carry_order.py` (2 — iter_carrying LIFO merge order; worn-armor removal emits the stop-using lines before the throw line). | ✅ FIXED (2.14.239) |

## Scope Notes

- **Object-targeted spells** (`"object"` JSON, ROM `TAR_OBJ_INV`) and the object branches of `TAR_OBJ_CHAR_OFF` / `TAR_OBJ_CHAR_DEF` are now routed by `do_cast` (CAST-004/005/006, fixed 2.11.52). The spell handlers that accept `Object` targets (`identify`, `enchant_armor`, `enchant_weapon`, `fireproof`, `create_water`, `detect_poison`, `recharge`) receive the resolved object from `do_cast` via `spell_func(char, target_obj)`. Handlers that only accept `Character` targets (e.g. `bless`, `curse`, `poison`, `remove_curse`, `invisibility`) will raise on the Object — the fix routes correctly; handler-side Object branches are future parity work for the per-spell audit.
- **CAST-007 ordering note (ROM `check_killer` → charm gate interaction)**: ROM `do_cast` calls `check_killer` before the `AFF_CHARM` master gate (src/magic.c:406 then :408-412). For a charmed PC caster attacking a PC, `check_killer` enters the `AFF_CHARM` branch (lines 1248-1260) and calls `stop_follower(ch)`, which strips the charm affect and clears `ch->master`. The charm gate then sees `!IS_AFFECTED(ch, AFF_CHARM)` and does not block. For a charmed PC attacking an NPC (including their NPC master), `check_killer` returns early at line 1241 (`IS_NPC(resolved_victim)`) without reaching the charm branch, so charm remains intact and the charm gate blocks the cast. For NPC casters, both `is_safe`/`is_safe_spell` and `check_killer` are skipped (`!IS_NPC(ch)` guard), but the charm gate still fires (it is outside the `!IS_NPC` block in ROM). The Python implementation mirrors this ordering exactly.
